# 7일차 (2026-08-29) — 오답의 실물 · 결합 기억(위상 STDP) · sheaf 확산과의 연결

§1~5: 체크포인트 `R1B8_bilin_r2 @123,039` (held-out 2048, 기준 완답 947) 에서의 추론·개입. §6: 그 뒤 실제로 돌린 단계 학습·방향성·자기교정 루프.
아래 §1 은 정보가 남은 결과만 적는다. 정보 없이 끝난 시도는 §4 에 한 줄씩.

## 1. 확정된 사실

### 1.1 오답의 실물 (`analysis/quadrant_view.py`, 퍼즐 1·3 전 칸)
빈칸을 (확신 = 최종 로짓 여유 ≥ 8) × (정답/오답) 으로 나누고 칸마다 궤적(커밋 스텝·뒤집힘)을 봤다.
- **확신·오답은 두 종류.** (a) 진동 중인 칸의 마지막 프레임이 오답에 걸린 것 — 궤적 `9 2 8 8 7 7 8 7 8 8 …`, 뒤집힘 19회, 커밋 스텝 121/128. 확신은 스냅샷. (b) **스텝 1~6 에 굳어 128 스텝 동안 한 번도 안 바뀐 것** — 궤적·여유·행합으로 확신·정답과 구별 불가.
- **(b) 는 예외 없이 맞바꿈 쌍/순환이다.** 퍼즐 3 행 7: `r7c1(2/9)→r7c3(9/8)→r7c4(8/7)→r7c6(7/3)→r7c2(3/2)→r7c1` — 한 행 안에서 값이 순환 치환돼 행은 유효. 확신·정답 칸은 퍼즐 3 에서 24/24, 퍼즐 1 에서 11/12 가 맞바꿈 상대 없음.
- **비확신·정답은 실체가 없다** (궤적 `4 4 4 5 5 5 5 5 4 5 4 2 5 5 5 5`, 마지막 프레임만 정답). **비확신·오답 = 미결** (뒤집힘 5~28).
- 따라서 라벨 없는 "확신" 의 올바른 정의는 여유가 아니라 **정착**(최근 32 스텝 뒤집힘 0). 정착 칸 안에서 정답/오답은 내용(순환 구조)으로만 갈린다.

### 1.2 순환은 상태를 흔들어도 도로 채워진다 (단일 칸 초기화 프로브)
정착 칸 하나의 `h_t` 만 `init_hidden` 으로 되돌리고 16 loop 계속: 정착·정답 33/34 복귀, **정착·오답 14/18 이 오답으로 복귀**(퍼즐 1 은 7/7). 오답 순환은 국소 끌개다. 오답 칸 전부를 초기화해도 퍼즐 1 은 못 풀고(26 오답), 재시작으로도 못 푼다(30). 퍼즐 3 은 재시작만으로 풀린다 → "운 나쁜 궤적" 과 "능력 밖" 이 갈린다.

### 1.3 지속되는 것은 입력 핀뿐 — 그리고 핀은 단조가 아니다
- 퍼즐 1: 정답 빈칸 29개를 **입력 토큰으로** 핀 → 남은 25 오답 0. 정착·정답 17개만 핀해도 남은 37 오답 0. 상태로 두면 안 되고 입력이면 된다 (어제 "입력 핀만 지속" 재확인).
- 그러나 2048 에서 **정답만 핀한 퍼즐 1848 개**의 완답은 878 → 868: **90 잃고 80 얻음** (`analysis/early_commit.py`, 라벨은 평가에만). 퍼즐 10: 정답 7칸 핀 후 오답 14칸(새 맞바꿈 쌍). 힌트 7개 뒤에도 빈칸 47개라 어려운 영역이고, 이 영역에서 모델은 주어진 칸 수에 단조가 아니다. 어제의 "정답 격자에서 32칸까지 비워도 99%" 는 쉬운 영역(전부 얕은 단일)의 결과라 모순이 아니다.
- 기제: 주어진 칸은 **스텝 0 부터 나가는 메시지**다. 빈칸이 나중에 굳어 보내는 것과 시점이 다르고, 이웃은 자기 제약이 닫히기 전에 표를 받는다 (§1.4).

### 1.4 모델의 조기 규칙은 hidden-single 의 연성 투표판
첫 세그먼트(스텝 8) 의 여유는 빈칸 상태가 아직 동일해 **주어진 칸의 정보만** 반영한다. 이 여유 ≥ θ 로 핀하면 (2048, 라운드 0):
θ=18 → 정 11136 / 오 237, θ=21 → 정 8942 / **오 58** — 오핀은 전부 전파깊이 ≠ 1 (논리적 단일에서는 0회 오류).
오핀의 실물 (퍼즐 3 r7c1, 모델 2/정답 9, 스텝 8 여유 19): 2 를 미는 상위 기여 = 비피어의 주어진 2 두 개 (`r8c6`, `r6c3`; +3.7, +3.5), 정답 9 를 미는 주어진 칸은 하나 (+4.1). 정답 조기확신 칸(r0c6, 깊이 1) 은 지지 주어진 칸 세 개. **모델은 "정렬된 같은 숫자 주어진 칸 수" 로 확신하고, 같은 유닛의 다른 빈칸이 배제됐는지는 보지 않는다.** 그래서 힌트가 늘면 오확신도 는다 (라운드별 오핀 237→286→287).
이 기준으로의 재입력 루프는 쓸모없다: 핀 0개인 퍼즐 583 (완답 113) — 못 푸는 퍼즐에는 조기 확신 칸이 없고, 핀 16개 이상인 퍼즐 58 은 이미 완답 57.

### 1.5 전파와 결정은 이 가중치 안에서 분리되지 않는다
쌍선형 경계를 항등으로 두면 빈칸 정확도 0.164 (우연 0.11). 게이트 배율 λ 일정 0.75 → 셀 0.57, 0.5 → 0.21. 스케줄(0.5→1 선형) → 완답 165, (첫 세그 0.7) → 938. 미세스텝 R=2 → 완답 0. 결론: 학습된 경계는 결정만이 아니라 전파의 중계(부분공간 이동·주소 갱신)까지 맡고 있으며, 추론 시 스위치로는 못 가른다. 분리하려면 학습 단계의 구조 (R8B8 쌍선형 등) 여야 한다 — 미시험.

### 1.6 모델 자신의 정착 목적함수의 gradient 는 방향 있는 흔들기다 (`analysis/selfgrad.py`)
`J(h) = Σ_s Σ_t ‖p_t^{(s)} − p_t^{(s−1)}‖² + H(p_t)` (4 세그 롤아웃, 빈칸) 의 `∇_h J` 로 최종 carry 를 상대 노름 0.1 씩 6 스텝 이동 → 16 loop 재실행 → 유효 격자면 채택.
무효 퍼즐 64 개: **gradient 12/64 유효(전부 완답) vs 같은 노름 무작위 4/64.** 단 J 의 최소와 오답 수는 따로 논다(퍼즐 3: J 최소 s3 에서 오답 25, 정답은 J=41 인 s6) — 정답 지향이 아니라 순환을 가장 싸게 깨는 방향이다.

### 1.7 주소 위상은 위치를 담는다 (`scratchpad/phase3.py`, 퍼즐 1·3)
원위상 군집도(무작위 0.64~0.68): 행 0.72, 열 0.71~0.75, 숫자 0.66~0.70. 채널별 어텐션 기여에서 같은 숫자·비피어 − 다른 숫자·비피어 가 |0.1| 을 넘는 채널 0/416. 헤드 합 `a` 에서는 갈린다(피어 −0.12~−0.19, 같은 숫자 비피어 +0.05~0.09 vs 다른 숫자 +0.03). 숫자 치환 증강 아래서 절대 숫자 코드는 존재할 수 없으므로 이건 당연하며, 주소는 그래프(행·열)를 상태로부터 조립하고 같음/다름은 값 경로에 분산돼 있다.

## 2. 결합 기억 — 위상 STDP 를 추론에 얹기 (`analysis/stdp_infer.py`)

### 2.1 무엇이 빠져 있었나
어텐션 `a_tn = D(Δ)·cos(φ_t − φ_n − ψ − θ·Δ)` 는 STDP 커널의 위상판이다: 위상차가 작을수록 크고(cos), ψ 가 순서 비대칭을 준다. 그러나 STDP 는 **가소성** — 커널값이 시냅스에 누적된다. 현재 모델은 커널값을 매 스텝 결합으로 바로 쓰고 아무것도 남기지 않는다 (η=∞ 의 퇴화 극한). §1.2·1.3 의 실패 — 상태를 흔들면 순환이 도로 채워지고, 남는 건 입력 핀뿐 — 는 결합에 기억이 없다는 것이다.

### 2.2 규칙
```
매 블록 스텝:   a = attn(h)
               w ← w + η (a − w)          w 초기 = 첫 스텝의 a       (Aoki–Aoyagi 2009 형)
               결합 = (1−λ) a + λ w        → field(a_fixed=결합)
```
η = 학습률(시정수 1/η 스텝), λ = 기억이 결합에서 차지하는 비율. 가중치는 `w` 를 전제하지 않으므로 λ 는 작아야 한다.

### 2.3 결과 (기준 947/2048)
| η \ λ | 0.25 | 0.5 | 0.75 |
|---|---|---|---|
| 0.1 | 969 (+22) | 991 (+44) | 31 |
| 0.05 | **999 (+52)**, 셀 0.7351→0.7447 | 687 | 533 |

- 학습 없이 +5.5%. 절벽이 가파르다(λ 한 단계에 붕괴) — 가중치가 `w` 를 모르는 채 얹은 것이라 안정 영역이 좁다.
- loops 스윕(512, η=λ=0.1): 기준 216/224/230/230 (loops 16/32/64/128) vs STDP 216/230/235/**237**. 기준은 64 에서 포화, 결합 기억은 갱신 횟수에 따라 계속 오른다.
- 퍼즐 단위: 퍼즐 1 오답 25→19, 퍼즐 3 26→20 (η=0.1, λ=0.5).
- 불일치량 `Σ_n |w_tn − a_tn|` 은 **정착도**를 잰다(미정착 9~10 > 정착 7~8 > 주어진 칸 6~7)이지 오답을 짚지 않는다 — 정착·정답과 정착·오답이 같다. 순환은 이력과 함께 자란다(§1.1 (b) 가 스텝 1~6 에 굳는 것과 일치).

### 2.4 다음
`w` 를 구조에 넣고 학습: `sudoku/lt.py` 에 `[B,H,81,81]` EMA 결합, 헤드별 학습 가능한 η·λ, 세그먼트 detach 안에서 BPTT. 지금 결과는 "가중치가 모르는 채 얹어도 +52" 이므로, 학습된 판이 이걸 넘는지가 판정.

## 3. sheaf 확산과 STDP 의 연결 (유도 — 이 대응을 명시한 문헌은 모름)

주소는 U(1) 다발이고 커널의 `e^{−iθ·Δ}` 는 간선 위의 평행 수송이다. 단위 위상 `z = e^{iφ}` 에 대한 **연결 sheaf 의 디리클레 에너지**(Singer–Wu connection Laplacian; Barbero et al. 2022 connection sheaf; 일반형 Hansen–Ghrist 2019)는
```
E(φ; w, α) = ½ Σ_e w_e |z_t − e^{iα_e} z_n|² = Σ_e w_e [1 − cos(φ_t − φ_n − α_e)],    α_e = ψ + θ·Δ
```
우리 `a_tn` 은 정확히 이 에너지의 간선항 `cos(·)` 이다 — **어텐션 = 연결 sheaf 의 간선별 일치도.** 같은 E 를 세 변수로 내려가면:

| 변수 | 규칙 | 이름 |
|---|---|---|
| 단면 φ | `φ̇_t = −Σ_n w_tn sin(φ_t − φ_n − α)` | sheaf 확산 = Kuramoto (α≠0: Sakaguchi) |
| 결합 w (+ `Σw²/2`) | `ẇ_tn = ε[cos(φ_t − φ_n − α) − w_tn]` | 위상 STDP = Aoki–Aoyagi (PRL 2009), Seliger–Young–Tsimring (2002). **§2.2 그대로** |
| 연결 α | `α̇_e ∝ w_e sin(φ_t − φ_n − α_e)` | 수송 학습 = neural sheaf diffusion (Bodnar 2022) 의 국소 동역학 판 |

즉 sheaf 확산과 위상 STDP 는 **같은 디리클레 에너지를 각각 단면과 결합에 대해 내려가는 것**이고, 둘을 같이 돌리면 적응 Kuramoto 망이다. 우리가 한 일은 이미 있던 연결 sheaf 어텐션 위에 결합 하강을 얹은 것.

접점 둘: (i) 결합을 상태가 즉시 다시 써서 제약을 협상하던 병리(어제 C3)는 결합에 에너지 항이 없어서였고, `w` 와 `Σw²/2` 는 결합 재작성에 비용·관성을 준다 — +52 의 이론적 자리. (ii) `(φ, w)` 의 결합 에너지가 리아푸노프 함수이려면 α 대칭(ψ=0)이어야 함은 명제 7·8 그대로이고, Aoki–Aoyagi 에서 β≠0 이 결합 에너지를 깨는 것과 같다.
한계: 이 대응은 주소 위상에 대한 것이다. 값 경로 `WᵀW h` (진폭, Stuart–Landau) 는 이 에너지 밖이고, "같은 숫자" 관계가 값 경로에 있으므로(§1.7) `⟨Wh_t, Wh_n⟩` 의 누적은 별개의 둘째 후보다.

## 4. 정보 없이 끝난 것 (재발 방지)
- 원판 확신(최종 여유)으로 커밋-재입력 루프: 2라운드 신규 커밋 정밀도 0.37 — 어제 결정화와 같은 것. 사용자가 사전에 기각한 방식.
- 라플라시안 꼴·ψ=0·3차 우물 등 REACTION.md 의 세 수정: 이론 검토에서 각각 기여 0 / 불충분 / 소거 의미론 충돌로 판정 (대화 기록).

## 5. 스크립트
`analysis/quadrant_view.py` (칸 범주·궤적·어텐션 행) · `analysis/early_commit.py` (조기 여유 핀 루프 + 깊이 대조) · `analysis/propagate_commit.py` (경계 끔 전파 + 확신 핀 루프) · `analysis/selfgrad.py` (정착 gradient 흔들기) · `analysis/stdp_infer.py` (결합 기억) · `results/json/stdp_infer_bilin_123039.json`, `early_commit_bilin_123039.json`.

---

## 6. 7일차 후반 — 단계 학습 · 방향성의 정체 · 자기교정 루프 (2026-08-29 오후~밤)

> 이 절은 §1~6 뒤에 실제로 학습을 돌리며 확인한 것이다. 처음 읽는다면 순서: PLAN.md → STAGES.md → 이 절.
> 체크포인트는 `refs/URM/checkpoints/<런>/step_*.pt` (재구축 시 사라짐; 곡선은 `results/curves/*.csv` 에 보존).

### 6.1 네 단계 학습 (모두 `core/minimal.py`, 하네스 동일, held-out 2048)
| 런 | 구조 | 9,765 eval (셀/완답) | 비고 |
|---|---|---|---|
| `R1B8_min` | minimal (7.15M) | 7,812: .668/52 | 원판과 동급 (원판 7,812: .677/53). 7,812 에서 정지 |
| `R1B8_min_psi0` | + ψ=0 | .652/55 | 60%대 정체 후 7,200 에서 재가속. 9,765 정지 |
| `R1B8_min_psi0_split` | + 주소/값 분할 (d=1248, 13.35M) | .620/26 | 11,200 에서 70% 돌파. 15,624 정지 |
| `R1B8_min_psi0_split_stdp` | + `Γ=a` STDP | .620/27 | 3단계와 동일 — `a` 가 퍼즐 안 불변이라 공허. 폐기 |
| `R1B8_min_psi0_split_stdpv` | `Γ=⟨v̂,v̂⟩` | .586/7 | 부호 없어 학습이 거부(λ 전 헤드 ↓). 폐기 |
| `R1B8_min_psi0_split_stdpp` | **`Γ=a·⟨v̂,v̂⟩`** (4단계) | .617/21 → 46,872: .702/205 | 3단계와 동일 곡선. 46,872 정지 |
| `R1B8_min_split` | ψ 자유 + 분할 | **.680/71** | 원판 근접. 21,394 정지 |
| `R1B8_bilin_same` | 원판 재학습(같은 스텝 비교용) | 3,749 정지 (미완) | 필요 시 `resume.sh` 로 7,812 까지 |

**2×2 결론** (`results/json/stages_2x2_and_directedness.json`): 지연과 완답 손실은 **ψ=0 하나**에서 온다. 분할 비용 ≈ 0.
(ψ자유+분할 7,812: .664/53 ≈ minimal .668/52; ψ0 계열은 .60~.62/15~16.)

### 6.2 분할 모델의 그래프는 첫 세그먼트에 조립된 뒤 고정된다
`|a₁₂₈ − a₈| = 0.012` (원판 0.055), 주소 블록 변화 1→8 스텝 2.1 / 8→128 스텝 0.44. 관계별 `a`(헤드합): 같은행 +0.24, 같은열 +0.21, 같은박스 −0.18, 비피어 ≈0 (거리 진동). 원판은 같은행 −0.84 등 부호가 반대 — **부호 관례는 선험적이지 않다**(어제 논의 그대로). 귀결: 값 메시지가 주소를 못 바꾸므로 `a` 의 EMA(`Γ=a`)는 상수의 기억 → 공허. 원판의 +52 는 값→주소 누출이 만든 요동을 저역통과한 것이었다. 그래서 정합한 `Γ` 는 결합 에너지 `E(h,w) = −½Σ w a⟨Wh,Wh⟩ + ½Σw²` 의 `∂/∂w` = `a·⟨v̂,v̂⟩` (그래프 부호 × 값 일치).

### 6.3 방향성(ψ)의 정체 — 위치에 고정된 진행파 방향장 (`R1B8_min_split @7,812` vs `psi0_split @7,812`)
| | ψ0+분할 | ψ자유+분할 |
|---|---|---|
| 반대칭 비율 ‖a^a‖/‖a‖ | 0 | **0.62** (학습된 ψ ≈ π/2 = 최대 반대칭 `sin` 창) |
| 순 흐름 주어진→빈칸 / 빈칸→주어진 | 9.27/2.60 | 9.16/2.58 |
| 진폭 ‖v‖ 주어진/정답빈칸/오답빈칸 | 5.56/1.66/1.58 | 6.63/2.84/2.51 |
- 대칭 모델의 방향성은 전부 **진폭**(주어진 칸이 3.3배 크게 말함)에서 나온다 = "우회". ψ 자유 모델은 결합 자체에 방향.
- 방향장: 행은 오른쪽→왼쪽(+0.069), 열은 아래→위(+0.146), 대각 우하→좌상(+0.206), 거리 3 에서 부호 반전 — 내용 의존이 아니라 위치 고정 순환류. 역할은 **이류**(희석 없는 수송). 선형 수준에서 대칭/반대칭 = 경사/회전(Helmholtz)이며, 회전 성분은 노름은 보존하되 에너지는 보존하지 않으므로 리아푸노프 함수가 사라진다. 상세 균형 예외는 우리 커널에 해당 없음.

### 6.4 오답의 조성 — 순환(컬)은 진동의 필요조건이지 충분조건이 아니다
7,812 시점: 진동 칸 비율 ψ0 2.3% / ψ자유 0.2%; 오답 중 정착형 97~100%; 정착 오답 중 맞바꿈 상대 88~90%. 원판의 진동형 오답(§1.1 (a))은 **컬 + 그래프 재협상(값→주소 누출)** 의 산물이었고, 분할이 재협상을 닫자 컬이 있어도 진동이 사라졌다. 남는 오답 = 고정 그래프 위의 좌절된 국소 최소(맞바꿈).
주의: 이 비교는 분할 7.8k 대 원판 123k 라 학습 진행도가 다르다 — 원판을 같은 스텝으로 재학습해 비교하는 것이 옳고(`R1B8_bilin_same`), 3,749 에서 정지된 상태다.

### 6.5 왜 규칙을 알아도 못 고치나 — 수학적 진술
1. 훈련은 굳힘력 `κ` > 단일 간선 위반력 을 강제한다(단일 반대 메시지에 버텨야 hidden single 이 됨). 위반 간선 하나는 문턱 아래.
2. 맞바꿈에서 규칙 에너지 `E_rule` 은 정답까지 **평탄**하다(한 칸 이동 = 위반 하나 교환). 장벽은 전부 굳힘(우물) 항. 국소 최소는 규칙이 아니라 결정이 만든다.
3. 위반 정보는 간선량 `e_tn = a_tn⟨v_t,v_n⟩` 에 있고, 칸의 합산 장에서는 잡음과 구별 불가 → 어떤 칸별 비선형성(경계·Φ)도 학습으로 못 배운다. 교정에는 간선량 → 굳힘 이득의 배선이 필요하다(`E' = E_rule + Σ κ g(r_t) V`).
4. 시험: 경계 이득을 위반량으로 조절(`viol_gate*.py`) → **실패**. (i) 학습된 부호 관례가 달라 `Σ a⟨v̂,v̂⟩` 이 정답 칸에서 더 큼, (ii) 손 규칙 위반 수로 바꿔도 굳힘 해제 → argmax 붕괴 → 위반 폭증의 양의 되먹임, (iii) 이 모델의 경계는 전파 중계도 겸해 이득을 줄이면 전파가 죽음. 즉 **반응을 경계 이득으로 구현하는 건 이 학습 모델에서 분리 불가**; 반응의 깨끗한 판은 상태 초기화(6.6).

### 6.6 자기교정 루프 (`analysis/selfcorrect.py`, 원판 123k, 2048 퍼즐, 3라운드, 오답 채택 0)
탐지 = 손 규칙 위반 칸(같은 argmax 인 피어 존재; 오답 칸 r̄ 0.91 vs 정답 칸 0.48 — 라벨 없이 가름) → 초기화 → 재전개 → 유효 격자 채택.
| 초기화 대상 | 기억 없음 | 결합 기억 (η .05, λ .25; 기억만으로 999) |
|---|---|---|
| 위반 칸의 유닛 | 1018 (+71) | — |
| **위반 칸만** | 1047 (+100) | **1198** (+251 vs 947) |
| 무작위 빈칸 (같은 개수, 탐지 없음) | 1062 (+115) | 1188 (+241) |
- **탐지의 기여는 +10~15 뿐.** 어디가 틀렸는지 알아도 되돌린 뒤 같은 오답이 재형성된다(위반 칸/퍼즐 18 로 불변).
- **이득은 섭동 × 결합 기억**: 기억이 재형성을 막아 라운드 0 채택이 53 → 129. 부분합(52+100)을 넘는 상승 효과. 어제 분기+유효성검사(1211)에 무분기로 근접.
- 분할 모델(19,530, 셀 .57)에서는 +3 — 위반이 퍼즐당 26개(유닛이면 빈칸 전부)라 초기화 = 재시작이 됨. 루프는 오답이 국소인 강한 모델에서만 의미 있다.

### 6.7 정정 — "정답 핀이 더 틀리게 한다" 는 오독이었다 (`analysis/pin_check.py`, 원판 123k, 2048)
독립 재검증(조기 확신 선택 없이 **무작위 정답 K칸 핀**, 핀 복사율 .999~1.0, 배치 구성 결정론 확인: 재실행·셔플 모두 뒤집힘 0):
| K | 완답 | 잃음/947 | 얻음 |
|---|---|---|---|
| 1 | 1219 | 62 (6.5%) | 334 |
| 3 | 1651 | 35 | 739 |
| 7 | 2005 | 6 | 1064 |
| 15 | 2047 | 0 | 1100 |
모델은 정답 힌트에 대해 **강하게 단조**다. §1.3 의 "90 잃고 80 얻음" 은 조기 확신으로 고른 핀(모델이 어차피 일찍 맞추는 중복 힌트)이라 이득이 없었던 것.
남는 6.5% 손실의 정체 = **카오스 기저율**: 논리적으로 동일한 퍼즐로 바꾸기만 해도(숫자 치환) 947 중 135 잃고 137 얻음(14.3%), 전치는 183/154(19.3%). 정답 핀 1개(6.5%)는 그보다 덜 흔든다 — 안정화. 잃은 표본에서 궤적은 스텝 0 부터 갈리고 새 오답의 위치는 핀의 피어 기저율(25%)과 같음 = 국소 효과가 아니라 궤적 전체 재편. 깊이별: 탐색 필요 칸을 핀하면 9.2%, 얕은 칸 3~5%.

### 6.8 등변성 실패 = 공짜 재시작 다양성 — 증강 합집합 (`analysis/aug_union.py`, 원판 123k)
유효 격자 = 유일해 = 정답이므로, 논리적으로 동일한 변형(숫자 치환 × 전치/밴드/스택 셔플)을 M번 돌려 유효한 것을 채택하면 오답 채택 0:
947 → 1089 → 1190 → 1268 → 1310 → 1326 → 1360 → 1380 → 1395 → 1401 → **1411 (10회, +464)**. 각 변형 단독은 926~949. 지금까지 최대(분기 1211, 기억+초기화 1198)를 넘고 비용은 추론 M회. 포화 근처(+6~10/회). 다음: 증강 합집합 × 결합 기억 × 위반 초기화 조합, 그리고 학습 중 등변성 정칙화(증강 간 출력 일치) 로 이 14~19% 를 학습이 흡수하게 하기.

### 6.9 그 밖에 확정된 것
- 위상 하나로 숫자를 들게 하면 1칸 비움도 실패(국소 최소); 같은 에너지를 9-슬롯 분포에 걸면 국소 하강만으로 단순 전파(`dist_sanity.py`, `phase_vs_dist.py`). 내용은 중첩이어야 하고 위상 가소성은 그래프에.
- 원판 step-8 `a`(피어-음 헤드 합)를 손 분포 동역학에 꽂으면 손 피어 그래프와 동등(5/0.500 vs 6/0.493) — 모델은 분리 가능한 제약 그래프를 첫 세그먼트에 만든다(`graph_transplant.py`). step 32/128 은 내용이 섞여 흐려짐.
- 4단계 유용성 시험(`stdp_eval.py`, 9,765): λ×0 −2.4%p(의존은 생김), loops·섭동 회복 이득 없음 — **판정 시점이 너무 이름**. 완답 수백부터 다시.
- wd 함정: 가소성 파라미터(`eta_raw·lam_raw·beta`)가 wd 1.0 에 걸려 sigmoid(0)=0.5 로 끌려감 → `urm_patches.diff` 제외 목록에 추가(적용됨).

### 6.10 자기지도 갱신과 지도 경사의 정렬 — 원론과 측정 (`analysis/stdp_alignment.py`)
원론: `Δw = η(Γ−w) = −η∇_w E` (E = −½Σ w a⟨v,v⟩ + ½Σw²) 이므로 `dL/dt = −η⟨∇_w L, ∇_w E⟩`. 정렬 조건 셋: (a) 에너지 = 과제(전역 최소 일치, 해 다양체 위에서만 정확), (b) EP 의 밀림 위상(자기지도 항 = 손실 경사의 자유-위상 절반), (c) 메타학습(기대값에서만). Hebb 항은 일관성 증폭기라 정렬은 **상태 조건부**(맞는 궤적에서 양, 틀린 궤적에서 음).
측정(4단계 @46,872, 512 퍼즐, 세그먼트 8 시작): 양의 정렬 비율 정답 칸 **.539** / 오답 칸 **.435** vs 무작위 방향 .497/.505 (n=16k/12k, 유의). 전체 cos ≈ 0, 칸별 cos +.003/−.004 — 부호 구조는 예측대로, 크기는 작다. 즉 STDP 항은 손실 경사와 거의 직교하되 손실 관련 성분만 조건부로 기울어 있다. (b) 의 밀림 없이 정렬을 키우려면 `Γ` 가 손실 관련 간선을 더 골라내야 한다(= 값 표현이 더 학습되거나, e-prop 흔적으로 η 를 긴 시평에서 학습).

**+52 의 기제 (원판 123k, 512 퍼즐, EMA `Γ=a`)**: EMA 없음 (216, 진동 칸 14.6%, 진동형 오답 2512, 정착형 오답 5857) → η.05 λ.25 (231, 15.2%, 2517, **5651**) → η.1 λ.5 (223, 18.5%, 3031, **5173**). 즉 기억은 진동을 줄이는 게 아니라 **정착형 오답을 줄이고**(틀린 커밋을 미결로 남김) 진동을 늘린다. 이유: `w` 는 이전 `a` 를 들고 있고, 이전 `a` 는 내용에 얽히기 전의 깨끗한 제약 그래프(6.7 의 step-8 그래프)라, 늦게 형성되는 틀린 커밋이 그래프의 지지를 덜 받는다. 정렬(1차·한 세그먼트)이 작아도 정확도가 오른 이유가 이것 — 효과는 경사 방향이 아니라 궤적의 관성(어느 그래프를 기억하는가)에 있다. 분할 모델에선 `a` 가 처음부터 고정이라 기억할 "이전" 이 없고 효과도 없다.

### 6.11 STDP 충실형 학습 — minimal + 시냅스 전담 전달 (`R1B8_min_stdp1`)
**결론에 이른 경로**: 2×2 로 손실의 범인이 ψ=0 하나임이 확정되자, 기본 구조는 minimal(ψ 자유, 분리 없음)로 돌아왔다. 그 위에 STDP 를 **충실하게** 얹는다:
```
ΔW_tn = a_tn · ⟨v̂_t, v̂_n⟩          (커널 × pre·post 활동 동시성)
w ← w + η(ΔW − w)                  (누수 누적, w₀ = a, η 헤드별 학습)
m_t = Σ_n w_tn · WᵀW v_n            (전달은 w 가 전담, λ=1 고정; a 는 규칙 안에만)
```
플래그 `stdp_lam_fixed=1.0`. 원판 체크포인트에 이 형태를 학습 없이 얹으면 붕괴(완답 0) — 가중치가 a-전달을 전제해서. 학습으로만 판정 가능.
결과 (held-out 2048, 셀/완답): 1,953 .478/0 · 3,906 .543/0 · 5,859 .593/0 · 7,812 .624/8 · 9,765 .640/32 · 11,718 **.650/46**. train 은 10,400 에서 70% 돌파. minimal(7,812 .668/52) 보다 약 1 eval 간격 늦고, ψ0 계열과 비슷한 지연. η 는 전 헤드 .10→.13 단조 상승(학습이 잊음을 빠르게 = 지연 축소 방향), β 는 product 규칙에 안 들어가 0 유지. **STDP 다운 형태는 학습된다.**

**왜 minimal 보다 느린가 (미결).** 후보 (1) 지연: w 의 시정수 1/η≈10 스텝이라 전달이 옛 관계를 따름 (2) 일치 게이팅: 증분이 ⟨v̂,v̂⟩ 라 값이 다른 쌍의 연결이 약해지는데, 제약 전파의 핵심 메시지("다른 숫자 아님")는 바로 그 쌍 사이를 지난다. 추론 조작으로는 못 가른다: 학습된 체크포인트에서 η 를 올리면(지연 제거) 오히려 붕괴(.445 → η=1 .158, 증분=a .096) — 가중치가 지연된 w 에 공적응. 초기화 문제는 아님(w₀=a). 가르려면 학습 절제: η 고정 0.5/1.0 판, 증분=a 판.

**런 종료 (2026-08-29 23:00 UTC, 사용자 결정).** 236,313 스텝(390,625 의 60%)에서 정지. 후반 eval (셀/완답): 222,642 .812/888 · 226,548 .816/943 · **228,501 .821/974** · 230,454 .822/959 · 232,407 .812/926 · 234,360 .816/941 · 236,313 .820/980. train 셀은 232k 에서 .954 로 아직 상승 중, 완답은 888~980 사이를 오감 — 미수렴. η(헤드별, 228,501): .76 .62 .57 .13 .51 .66 .61 .07 — 두 헤드는 거의 잊지 않는 장기 기억(η≈.1), 나머지는 시정수 1.5~2 스텝. 초기값 .10 에서 헤드별로 갈라진 것 자체가 결과(§6.13 로).
비교: 원판 쌍선형 `R1B8_bilin_r2`(**14.1M**) 최고 959@123,039 vs stdp1(**7.16M**, 절반) 974@228,501 · 980@236,313 — 파라미터 절반으로 같은 완답 수, 스텝은 약 2배. 파라미터↔스텝 교환이라 우열 판정 불가(원판의 228k 값이 없고, 위 2배 지연은 초반 느림이 그대로 실린 것).
보존: `checkpoints/R1B8_min_stdp1_step228501.pt` (85.9 MB: EMA `model_state_dict` + `raw_model_state_dict` + `ema_shadow`; 옵티마이저·RNG 는 GitHub 100 MB 제한으로 제외 → 재개는 fresh 모멘트로, 원판 EMA-only 재개가 1 eval 안에 회복한 전례 있음) + `checkpoints/R1B8_min_stdp1_config.yaml`. 나머지 120개(236,313 의 980 포함)는 삭제 — 삭제 시점에 234,360·236,313 eval 이 이미 나와 있었는데 정지 직전 표(232,407 까지)로 골랐다. 곡선 `results/curves/R1B8_min_stdp1_{train,eval}.csv` 는 로그 전체(236k)로 재생성.

### 6.12 원론 정리 — 세 질문에 대한 답
**(i) EMA 는 뭐였나.** 어텐션 행렬에 재귀 스텝 간 **누수 레지듀얼**을 붙인 것: `w_s = w_{s−1} + η(ΔW_s − w_{s−1})`. STDP 관점에서 매 스텝의 `a` 는 시냅스 `W` 가 아니라 증분 `ΔW` 여야 하는데, 선형/재귀 어텐션은 그것을 매번 새로 계산해 시냅스로 써 왔다. 레지듀얼(누적)이 STDP 의 "누적" 자리이고, 잊음(η)은 유계를 위한 것. 원판에서 `ΔW=a` EMA 가 +52 를 낸 것은 원판의 `a` 가 값 누출로 이미 활동을 담아 "커널 × 활동" 에 가까웠기 때문. 분할 모델에서 0 이었던 것은 `a` 에 활동이 없어서 — STDP 와 분리가 안 맞아서가 아니라 **증분에 활동이 없어서**. (DeltaNet 은 같은 레지듀얼의 증분을 델타 규칙으로 바꾼 변형.)
**(ii) 증분을 `a·⟨v̂,v̂⟩` 로 두는 근거.** STDP 의 발화율 축약(Kempter–Gerstner–van Hemmen 1999): 스파이크 쌍의 수 ∝ pre·post 발화율의 곱, 각 쌍의 부호·크기 = 타이밍 커널 → `ẇ ∝ (활동 곱) × K̄`. 위상 진동자 STDP(Aoki–Aoyagi)에서 곱이 안 보이는 건 활동이 곧 위상(발화율 일정)이라서. 우리는 활동(값)과 위상(주소)이 따로라 곱이 다시 나타난다. 결합 에너지 `E=−½Σ w a⟨Wv,Wv⟩+½Σw²` 의 `∂/∂w` 와 일치.
**(iii) "pre 활동 = v_n" 은 어디서 오나.** STDP 에서는 유도되지 않는다 — STDP 는 전달되는 신호와 가소성을 촉발하는 시각이 같은 스파이크에 실려 있어 그 구분이 없다. 우리는 커널 입력(주소 위상)과 전달 신호(값)를 갈라 놓았으므로 "활동" 자리는 **시냅스의 정의**에서 온다: 시냅스는 전달 이득이고 Hebb 항은 그 시냅스를 지나는 pre 신호와 도달하는 post 신호의 상관 → 전달 연산자가 `w_tn·WᵀW` 이므로 stalk 좌표의 `⟨Wv_t, Wv_n⟩`(정규화 `⟨v̂,v̂⟩`). 대안은 위상 진폭 `‖z‖`(정규화 전 커널 = Model 0 의 3차 항) 또는 커널만(EMA). 이 세 선택은 실험으로만 갈린다. 단서: Hebb 곱은 "같이 활동한 쌍" 을 강화하므로 "다르게 활동하는 쌍 사이를 지나야 하는" 제약 전파 메시지와 맞지 않을 수 있다 — 6.11 의 느림이 그 징후일 수 있음.
**(iv) 분리와 STDP.** 분리는 값 확산 스텝이 위상을 흔드는 것만 막는다(코드 한 줄: 주소 자리에 0 패딩). 위상은 경계·주입에서 바뀔 수 있고, 학습된 분할 모델이 경계에서 위상을 거의 안 쓴 것은 학습 결과. 분리는 위상 변화를 이산 사건(경계)으로 만들어 STDP 의 "사건" 자리와 오히려 맞지만, 사건이 실제로 일어나야(경계가 결정을 위상에 써야) 의미가 있다. STDP 가 목적이면 지금은 분리 없는 minimal 이 최단 경로이고, 분리가 산 것(스텝 단위 정확 선형성·오답 종류 단일화·그래프의 분리 가능성, 정확도 비용 0)은 별개의 자산.

### 6.12b 반복 외삽 — stdp1 vs 원판, 그리고 w 세그먼트 초기화 (`2026-08-30/analysis/extrap.py`, `results/json/extrap_stdp1_228501.json`)
학습은 16 세그먼트. 추론에서 재귀를 128 까지 계속 돌리며 세그먼트마다 채점 (held-out 2048). 손실은 **하네스 정의**(stablemax CE, 11-way, 81 칸, 학습 `lm_loss` 와 동일). 처음에 9 숫자 softmax CE 로 쟀다가 4.1(무작위 2.2 보다 큼)이 나왔는데, stablemax 학습 모델은 로짓 크기를 억제할 이유가 없어 softmax 로 재면 틀린 칸의 큰 음수 로짓이 그대로 튀는 계산 오류였다(사용자 지적). 아래는 고친 값.

| 세그먼트 | 8 | 16 | 24 | 32 | 48 | 64 | 96 | 128 | 16→128 |
|---|---|---|---|---|---|---|---|---|---|
| 원판 14.1M @123k 완답 | 826 | 947 | 981 | 992 | 1015 | 1031 | 1040 | 1049 | +102 |
| stdp1 7.16M @228k, **w 누적**(학습·하네스 방식) 완답 | 787 | 966 | 1017 | 1043 | 1060 | 1074 | 1086 | 1089 | +123 |
| stdp1, **w 세그먼트마다 초기화** 완답 | 836 | **1022** | 1058 | 1078 | 1091 | 1101 | 1110 | **1117** | +95 |
| 셀 (원판 / 누적 / 초기화) | .720/.723/.728 | .735/.739/**.746** | .739/.744/.752 | .741/.746/.753 | .745/.749/.754 | .748/.751/.756 | .749/.754/.759 | .751/.755/**.759** | |
| 손실 (원판 / 누적 / 초기화) | .492/.490/.509 | .466/.487/**.464** | .460/.474/.451 | .456/.462/.452 | .452/.458/.449 | .446/.457/.444 | .444/.451/.443 | .441/.448/**.438** | |
| 빈칸 손실 (원판 / 누적 / 초기화) | .698/.706/.734 | .662/.702/.669 | .653/.683/.650 | .646/.667/.652 | .641/.660/.647 | .633/.659/.640 | .630/.651/.639 | .625/.646/.632 | |
| 뒤집힘/세그 (원판 / 누적 / 초기화) | 6.1/5.2/6.3% | 5.1/5.5/7.0% | 4.8/5.9/6.6% | 4.7/6.2/6.7% | 4.5/6.2/6.3% | 4.3/6.0/6.5% | 4.3/5.9/6.3% | 4.2/5.9/6.3% | |

읽기.
1. 셋 다 16 에서 맞춘 판을 128 까지 거의 안 잃고(keep16: 947 / 966 / 1022→1021) 완답 = 유효 격자. 16 너머는 학습 때 본 적 없는 상태인데 세 사상 모두 계속 내려간다 — 깊은 지도 + detach 가 "받은 상태가 무엇이든 그 세그먼트 안에서 손실을 낮추라" 로 학습시킨 결과이고, 넘어오는 상태(h, w)를 쓰는 것이 곧 정렬이다. (처음에 "detach 때문에 세그먼트 간 w 는 정렬 밖" 이라 썼던 것은 철회 — 그랬다면 외삽에서 망가졌어야 한다. detach 가 빼는 건 다음 세그먼트를 위해 기억을 설계하는 경사 하나뿐.)
2. **세그먼트 경계에서 w 를 버리는 쪽이 모든 세그먼트에서 낫다**: 16 에서 +56 완답(966→1022, +5.8%), 128 에서 +28, 셀 +0.7pt, 손실 .487→.464. bf16 요동(966 vs 하네스 974)보다 훨씬 크다. 누적 쪽의 더 큰 외삽 이득(+123 vs +95)은 16 에서 낮게 출발한 걸 만회한 것. 즉 세그먼트를 넘어오는 w 는 **지연된 그래프**(앞 세그먼트 상태들의 평균)이고, 경계에서 현재 상태로 다시 계산한 `Γ = a·⟨v̂,v̂⟩` 가 더 낫다. (h 후반 상태, w 갓 계산) 조합은 학습 때 없었는데도 더 좋으므로, 기억의 값어치 < 지연 비용. 세그먼트 안 8 블록의 누적은 남겨 둔 상태라, 쓸모 있는 건 짧은 시평(한 세그먼트 안)의 STDP 뿐이라는 뜻.
3. 원판 대비: 파라미터 절반인 stdp1 이 16 에서 +19(누적) / +75(초기화). 손실은 원판이 여전히 낮은 편(16: .466 vs .487/.464; 빈칸 .662 vs .702/.669) — stdp1 은 틀린 칸에서 더 확신하고 뒤집힘도 더 많다(5.9~6.3% vs 4.2%).
4. §6.11 "왜 minimal 보다 느린가" 에 대한 함의: 후보 (1) 지연 쪽. 학습 자체를 세그먼트마다 초기화(세그먼트 안 STDP 만, `fresh` 를 매 세그먼트 True)로 돌리면 minimal 의 속도를 되찾으면서 +56 을 챙길 가능성 — 학습 런이므로 지시 시에만. 추론 쪽 후속은 초기화 주기 k=2,4 또는 η×2(잊음 가속) 한 개.

### 6.12c 추론 시 자기 교정 시도 — 위반 간선의 결합을 지수 성장 (CTDS 형, `2026-08-30/analysis/ctds.py`, `2026-08-30/results/json/ctds/`)
질문: 규칙이 가중치에 있다면(핀 실험의 단조성), 추가 학습 없이 모델이 자기 위반을 스스로 감지하고 고칠 수 있는가. 이론적 처방은 Ercsey-Ravasz & Toroczkai 의 연속시간 SAT 풀이: 빠른 변수는 `E(h,w)` 하강, 느린 변수 `g_e` 는 위반 중인 절의 가중치를 지수 성장 → 위반 있는 상태는 고정점이 될 수 없어 어트랙터가 해뿐. stdp1 에서 전달이 w 를 타므로 w 가 지형 변수이고, 갱신만 EMA(Hebb·유계)에서 반-Hebb·무계로 바꾸면 되는 자리. 학습 없이 stdp1 @228,501 로 시험 (512 퍼즐, reset 모드, 워밍업 16 뒤 느린 변수 on).

**탐지기 (`2026-08-30/analysis/edge_signal.py`)**: 값 벡터 코사인은 공통 성분 탓에 비선택적(정밀도 1%). 읽기 헤드의 "같은 숫자" 확률 `pp = Σ_d p_t(d)p_n(d)` × 피어에서 음수인 헤드(0,2,4)의 반발 `relu(−Σa_h)`, 문턱 .216, 대각 제외 → 표시 6.4/퍼즐(실제 충돌 6.0), **정밀도 .71 재현율 .75**, 푼 퍼즐엔 표시 0. 유닛 정의 없이 모델 자신이 충돌을 안다. (초기 시도의 실패 원인 셋: 값 코사인 비선택성 → 주입 직후 상태에서 읽음(빈칸들이 닮아 보임) → 대각 포함(자기 반발로 확신 칸 전부 해제).)

**오답의 구조 (`2026-08-30/analysis/ctds_trace.py`, `results/logs/ctds_trace_puzzle57_rho0.05.txt`)**: 16 시점 오답 퍼즐은 1~2칸 오답이 0개, 5칸+ 가 전부(128 중 0칸 64 / 3~4칸 1 / 5+칸 63). 오답 칸은 서로 지지하는 닫힌 순환(퍼즐 57: 2:9·4:7·20:7·21:3, 정답 7·9·9·7), 안쪽 칸은 충돌 없음·확신 1.00, 충돌은 순환 경계(4↔76, 21↔12·26)에서만.

**결과** (완답/512, 세그먼트 16→128):
| 변형 | 16 | 64 | 128 | 충돌/퍼즐 16→128 | 셀 128 |
|---|---|---|---|---|---|
| 기준 (ρ=0) | 218 | 236 | 241 | 6.0→5.8 | .717 |
| 간선 성장 ρ.05 cap10 감쇠.02 | 218 | 238 | — | 6→12 (64) | .712 (64) |
| 간선 성장 ρ.05 cap2 | 218 | 244 | — | 6→9 | .721 |
| 간선 성장 ρ.05 cap30 감쇠 0 (breakout) | 218 | 237 | 239 | 6→20 | .671 |
| 간선 성장 ρ.2 cap30 감쇠 0 | 218 | 230 | 230 | 6→73 | .569 |
| 칸 굳힘 해제 u (경계 항 ×1/(1+u)) | 218 | 232 | 232 | 6→131 | .542 |
| 굳힘 해제 + 이웃 확산 | 218 | 214 | 214 | 6→218, 뒤집힘 0 (동결) | .481 |
16 에서 푼 판은 어느 변형도 잃지 않음(keep16 고정). 그러나 어느 변형도 오답 순환을 풀지 못함.

**왜 (추적으로 확인)**: 경계 충돌 간선 (4,76) 의 반발을 키우면 **틀린 4(확신 1.00, 순환이 지지)가 아니라 맞는 76(.96)이 밀려난다**. 국소 정보는 완전히 대칭 — 두 칸 모두 자기 유닛 안에서 그 숫자가 달리 갈 곳이 없다 — 이고 갈리는 것은 연쇄 뒤의 전역 결과뿐(76 은 되돌아오고, 4 는 순환을 따라 번짐). 감쇠 없는 기억(breakout)은 되돌아오려는 맞는 칸을 오히려 막는다(같은 쌍이 다시 같은 숫자가 되면 g 가 즉시 작동). CTDS 에서 이것이 풀리는 이유는 변수에 **굳힘 항이 없어** 절 힘만으로 연쇄가 순환 안쪽까지 전파되기 때문인데, 이 모델은 굳힘(쌍선형 경계)이 곧 전파 중계라서 굳힘을 빼면(u) 해당 칸이 미결정으로 무너지고 확산시키면 상태가 동결된다. 즉 **CTDS 의 전제(자유 변수 + 절 힘)를 이득 조절로는 만들 수 없다.**

**남는 결론**: 이 가중치 안에 (i) 위반 탐지(정밀도 .7)와 (ii) 규칙 전파는 있지만, (iii) 순환을 푸는 조정된 다중 칸 이동을 국소 동역학으로 만들 방법은 시험한 범위에서 없다. 추론만으로 남는 길은 탐지기를 목적함수로 쓰는 탐색(섭동 → 재정착 → 모델 자신의 위반 수가 줄면 채택), 즉 min-conflicts 형 외부 루프. 학습 쪽 함의: 순환이 생기지 않게 하려면 경계 충돌의 대칭을 깨는 정보(예: 충돌 칸의 대안 지지 = 유닛 안 "그 숫자가 갈 다른 곳" 의 부재)가 값 표현에 들어가야 하고, 그것은 굳힘과 분리된 반응 항으로 학습돼야 한다.

### 6.13 8일차 — 자기교정의 구조 규명: 무엇이 안 되고 무엇이 되는가 (2026-08-30, stdp1 @228,501, w 세그먼트 초기화, 512 held-out)
목표: 추가 학습 없이 은닉 상태 안에서 오답을 스스로 고치게 하기. 결론부터: **이 모델 = 강한 전파기 + 퍼즐당 1~3개의 눈 감은 추측(블록 1)**, 추측을 되돌리지 못한다. 자기 검증(정지)은 있고 방향은 없다. 아래 순서로 확인.

**(1) 값을 미는 개입은 전부 실패, 이유 확인** (`2026-08-30/analysis/ctds.py`, `integrator.py`, `residual_descent.py`, `2026-08-30/results/json/ctds/`, `2026-08-30/results/`)
| 개입 | 결과 | 이유 |
|---|---|---|
| 위반 간선 반발 지수성장 (CTDS 형, ρ·cap·감쇠 스윕) | 236 대비 ≤244, 충돌 6→9~20 | 경계 충돌은 국소적으로 대칭: 틀린 칸(확신 1.0, 덩어리가 지지)이 아니라 맞는 상대(.96)가 밀림. 512 에서 맞는 쪽이 약한 비율 46:54 |
| 칸 굳힘 해제 u (경계 항 ×1/(1+u)) ± 이웃 확산 | 232 / 214, 붕괴 | 경계 항이 전파 중계를 겸함 |
| 전달장 f 적분 / 블록 Δh 모멘텀 / 세그먼트 Δh 모멘텀 | 8 / 0 / 215~234 (기준 236) | 고정점에서 f≠0 → 고정점 이동; 블록 과도 패턴 증폭; 세그 변위는 하강 성분이 소수 |
| 잔차 ½‖F(h)−h‖² 경사 하강 / 뉴턴-GMRES (퍼즐 57) | 잔차 .245→.096 인데 오답 6→5 / 발산 | 잔차 지형에 0 아닌 오답 최소가 널림; 비수축 사상(|λ|=1.3)에서 뉴턴 선형화 무효 |
| 야코비안 상위 부분공간 차기 (퍼즐 57, seg40) | 40세그 안 풀림 스펙트럼 11/48 vs 무작위 4/48 | 탈출 변위의 45% 가 상위 24 모드 안 — 방향은 있으나 확률적·소폭 |
탐지기 자체는 됨: 읽기 "같은 숫자" 확률 × 피어에서 음수인 헤드(0,2,4) 반발, 대각 제외, θ0=.216 → 정밀도 .71 재현율 .75 (`edge_signal.py`). 값 코사인은 공통 성분 탓에 비선택적(1%).

**(2) 퍼즐 57 해부 — 갇힘과 탈출의 실체** (`one_puzzle_long.py`, `dyn_macro.py`, `decline_anatomy.py`, `jacobian_spectrum.py`, `msg_decompose.py`, 페이지 `2026-08-30/results/puzzle57_escape.html`)
- 규칙 없이 115 세그먼트에 풀림. 오답 4칸 고리 {2:9, 4:7, 20:7, 21:3}(정답 7,9,9,7): 안쪽 충돌 없음, 충돌은 경계(4↔76, 21↔12·26)뿐.
- 고리 여유의 "느린 하강" 은 저역통과 착시: 조용한 세그먼트의 고정점(21: 3.4, 나머지 14~17)은 20~112 내내 불변, 힘의 항들도 불변, ‖h‖ 77~81 고정. 가장자리(76·12·26) 튐은 고리 여유를 **올리고**(+0.2~0.8, 보호막), 튐 빈도(창별 10,4,14,12,2)가 저역 여유를 흔든 것.
- 탈출 = 장벽 3.4 로짓의 요동 탈출: 21 은 모든 창에서 0 을 스치지만 튐이 되돌림; 100~112 의 조용한 창에서 3 세그먼트 버티자 넷이 동시에 넘어감(집단 모드). DMD: 갇힘 창 모든 |λ|<1, 진동은 가장자리에; 탈출 창 고리 위 모드 |λ|=1.022.
- 야코비안(정확 미분+Arnoldi): 갇힘 seg40/100 은 고정점이 아님 — ‖F(h)−h‖/‖h‖ .19~.25, |λ| −1.39/−1.33(주기-2 불안정, 가장자리에 퍼짐), 복소쌍 1.09/.993. 풀림 seg120: 잔차 .006, 스펙트럼 반경 **0.42**. 결합 a_tn 은 얼어 있음(σ 2~5%, 표류 없음).
- 흐름 코사인 cos(F(h)−h, h*−h): 전파 구간 +.14, 갇힘 +.05(±.2, 정답 거리 1.02→1.02 불변), 탈출 114→118 에서 .63→.97. **장은 덫에서 정답을 가리키지 않는다**; 갇힌 상태와 풀린 상태는 67k 차원에서 거의 직교(거리 ≈ ‖h*‖).
- 메시지 분해(seg16): 칸 2 는 7/9 동점(+7.4/+7.5; 비피어 주어진 7 이 +14 로 정답 쪽, 틀린 피어 20·4 가 −18 로 상쇄), 칸 4 는 틀린 7 에 +29 지지 대 76 의 "7 아님" −3.1.

**(3) 오답의 구조 (모집단)** (`failure_structure.py`, `root_rules.py`, `root_causal.py`, `phase_order.py`, `root_blame.py`)
- 미해결 294: 오답 중앙값 28.4칸(1~4칸은 2개). 첫 오답 결정 = 블록 1(90% 가 ≤3), 지지 .86(정상 2.4); 그 뒤 오답은 맞는 전제 위의 정확한 전파(지지의 72% 가 맞는 칸에서).
- 오답 칸의 순 메시지(정답−현재 로짓): 틀린 피어 −11.5, 틀린 비피어 −4.6, **맞는 칸 +0.5(중립)**, 주어진 비피어 −2.1; 합 −17.5, 정답 쪽인 오답 칸 2%. 충돌 쌍 오답–정답 79%, 오답–오답 21%, 오답 칸의 68% 는 충돌 무관. 난이도(탐색 칸 비율·주어진 수·오답 수)로 갈라도 구조 동일 — 난이도는 빈도(해결률 .88→.19~.27)와 덩어리 크기(21→30)만 바꿈.
- 뿌리 찾기(라벨 없음) 전부 실패: 이른 칸 지지 최소 .19, 위상 pre-ness .05, 인과 DAG 충돌 후손 .22, 결정 시 실제 지지 기여로 책임 역류 .28 (이른 칸 무작위 .20). 단, 뿌리는 81% 가 블록 ≤1 결정 칸(중앙값 8개) 안에 있음.
- 위상 순서는 결정 순서를 기록한다: a_tn−a_nt 가 "n 이 먼저" 예측 AUC 헤드 0 .76/.69, 헤드 5 .83/.71(부호 반대), 나머지 .5. STDP 비대칭 창이 인과 순서를 담는 자리(이른 칸은 주지만 뿌리는 못 줌).
- 응답 진폭(회전에 대한 흔들림): 퍼즐 수준 AUC .85(풀림 .006 vs 미해결 .2~.3), 같은 퍼즐 안 오답/정답 .63 — 고리 안쪽이 가장자리보다 뻣뻣.

**(4) 자기 검증 = 정지.** 64세그 뒤 float32 1세그 상대 변위: 풀림 235/235 전부 <1e-4(정확한 고정점), 미해결 277 중 <.01 은 4개(중앙값 .25). 재현율 100%, 정밀도 98.5%, 라벨·손 규칙 없음. 단 개입(억제 편향)을 넣은 재전개에서는 89% 로 떨어짐 — 편향이 틀린 배치를 조용하게 만듦. "정답이면 정지" 는 참, "정지면 정답" 은 동역학이 스스로 도달한 경우에만.

**(5) 되는 것**
| 방법 | 512 완답 | 성격 |
|---|---|---|
| 기준 (16 세그) | 218 | |
| 순수 반복 128 세그 | 241 | 요동 탈출(작은 고리만) |
| **블록 1 추측 되돌리기** 깊이 1 ≤8회 + 정지 채택 (`guess_retract.py`) | **297** | 라벨 없음, 후보에 뿌리 78%; 라벨로 뿌리 1회 억제 상한 308(미해결의 30%) → 깊이 1 의 한계 |
| 깊이 2 탐욕(잔차 최소 부모 2 × 후보 8, 억제 누적) + clean 채택(억제 제거 후 재정지) (`guess_retract2.py`) | **327** | 미해결당 재전개 ~12회(총 3,712). clean 채택 373 중 오답 51(14%): 억제로 유도된 재전개는 자연 궤적(1.5%)과 달리 정지하는 오답 배정을 자주 만듦 — 검증은 개입 아래서 약해진다. 깊이 2 의 증분(+30) < 깊이 1(+79) |
| **제시 간 일관성 결합 기억 C** (`consistency_memory.py`) `(1−λ)w+λC`, λ=.3, η EMA .3, 전체 대칭군 | 8제시 313 · 32제시 358 · **64제시 372** | 라벨·채택 없음, 마지막 은닉 상태가 답. η 평균은 −30~40(초반 나쁜 제시가 영구히), λ .3→.6 은 손실 20→9 로 안정화(365). 남은 ~140 은 변환 불변 오답 |
| 오라클: 자기 예측 중 맞은 것만 되받기 (`selfpin_oracle.py`) | 485 (1회) · **511** (4회) | 상한. K 정답 힌트: 1→41%, 3→78%, 7→99% 의 미해결 해결 |

**(6) 이론 정리.** 국소 합의(sheaf Dirichlet, 쌍별 에너지)는 쌍별 균형만 보므로 자기 지지 덩어리(안쪽 쌍 전부 만족)를 볼 수 없다 — 장애물은 전역(코호몰로지: 국소 단면이 이어 붙지 않음). 전역을 보는 항은 대안 시험(탐색)·좌표계 불변성(제시)·순환 전송(홀로노미)뿐. 귀류법의 연속화: 쌍대 증인(Farkas; CTDS 승수), BP 조건화, 스냅샷+섭동+정지검사+복원(미분 가능 스택). 우리에게 없는 부품은 가설 표시와 의존 기록 — 그 자리는 STDP 시간 비대칭 증분 `Δv_t·v_n`(t 가 바뀔 때 n 이 서 있었다) 으로 w 를 의존 그래프로 만드는 것. 학습 표적 후보: 오라클이 준 (갇힌 상태 → 풀린 고정점) 쌍(flow-matching 식).

**(7) 다음.** (a) 되돌리기 깊이 2(후보 8 × 2차 후보) + 편향 제거 후 재정지 확인; (b) C 에 풀기 규칙; (c) 학습: 시간 비대칭 STDP 로 의존 기록 + 정지 실패 시 철회 항, 표적은 (갇힘→풀림) 쌍; (d) 첫 세그먼트 전체 지도 완화("모름" 허용) — 추측을 미루게. 스크립트 전부 `analysis/`, 로그 `2026-08-30/results/`.

### 6.14 8일차 밤 — 반증들, 그리고 퍼즐 57 의 인과 추적 (2026-08-30)
§6.13 이후에 한 것. 스크립트·산출물은 `2026-08-30/`.

**(A) 굳힘 게이트 — 만들고, 학습시키고, 반증했다.**
설계: 경계(단정) 항의 이득을 메시지의 척도 불변 판별력 `d = (top1−top2)/std` 의 칼만 이득 형 `K=(sd)²/(1+(sd)²)` 으로 조절. 근거로 "뿌리는 블록 1 에 지지 0.86 으로 결정된다" 를 썼다.
반증: 그 통계는 퍼즐마다 오답 28칸 중 **최솟값**을 고른 것이었다. 결정 블록별 오답률을 제대로 세면 **일찍 굳는 칸이 가장 정확하다** — 블록 0-1 결정 칸 오답률 **.248**, 2-3 .347, 8-15 .463, 16-31 .543, 32-63 **.662**, 64-127 .610 (기저 .505). 게이트는 전파의 씨앗을 죽이고 있었다. 게이트 특징 d 자체도 무정보(결정 시점 AUC **.431**, 궤적 평균 .533)이고 학습된 모델에서 중앙값 ≈0 이라 켜면 경계가 통째로 꺼진다. 학습 곡선도 기준 이하(1,953 .479 / 3,906 .545 vs minimal .494/.583)에서 중단.
그리고 같은 출력 안에 반증이 이미 있었다(결정 시 지지 AUC .473, 오답 칸은 중앙값 블록 44 에 굳고 정답 칸은 16). **측정을 해 놓고 읽지 않은 것이 이 설계의 원인이다.**

**(B) 인과 STDP 구현 결함 3개 — 시험 자체가 무효였다.** `Δv̂` 를 boundary+주입의 **반 블록 차분**으로 잡아 고정점에서 0 이 되지 않았고(정착한 정답 칸에서 ‖Δv̂‖ 0.911, 미해결 0.559 — 오히려 정답에서 최대), 대각 `⟨Δv̂_t,v̂_t⟩=1−cos ≥ 0` 이 항상 양수라 움직이는 칸에 자기 굳힘을 더했으며, 항 크기가 기존 `a·⟨v̂,v̂⟩` 의 **3.17배**였다. 즉 섭동이 아니라 전달 규칙 교체였다. 런 중단.

**(C) 단일 궤적 동역학 수정은 전부 minimal 보다 느리다** (held-out 셀, 1,953/3,906/5,859/7,812): minimal .494/.583/.633/.668 · stdp1 .478/.543/.593/.624 · causal .453/.537/.582/.616 · gate .479/.545/—/—. 이긴 것은 둘뿐이고 둘 다 동역학을 안 건드린다(제시 일관성 372, 되돌리기 297→327).

**(D) 기억 시평 스윕은 결론을 못 낸다** (`memory_horizon.py`, carry, 128 세그, 512 퍼즐, 완답): η=학습값 **240** · η=1(기억 제거) **20** · η=0.2 균일 129 · η=0.05 균일 117. **결합 기억은 필수**(빼면 붕괴)이지만, 세 개입이 전부 균일 치환이라 학습된 다중 시간 척도(η .07~.77)를 뭉갠다. 모델이 자기 η 에 공적응돼 있어 **추론 시점 η 변경으로는 시평 가설을 시험할 수 없다.**

**(E) "영원히 못 푼다" 는 증명 불가** (`orbit_period.py`). 공리(유한 집합 위 결정론 사상, 비트 재현성 확인) + 정리(비둘기집)로 정확한 주기를 찾으면 그 퍼즐은 반복으로 영원히 못 푼다는 **증명**이 된다. 128 퍼즐 × 1,024 세그먼트 Brent 검출 결과 **주기 0개**. 상태 공간이 커서 이 방법은 원리적으로 이 계에 안 통한다. 부수적으로 **"정답은 정확한 고정점" 은 틀렸다** — 상대 잔차가 1e-4 아래일 뿐 비트 고정점이 아니다. 그리고 128 이후에도 계속 풀린다(136·220·331 세그먼트에서 하나씩).

**(F) 퍼즐 57 인과 추적 — 정확한 항등식만 사용** (`flip_anatomy.py`, `why_112.py`, `msg_decompose.py`). 읽기가 선형이라 `m_out = c·(m_in + Δ경계 + Δ주입 + Σ_n Δm_n)` 이 근사 없이 성립한다.
- **경계는 힘이 아니라 여유에 대한 이득이다.** 여유를 −8→+16 으로 훑으면 Δm_경계 가 −12.2→+10.2 로 단조 증가하고 여유 0 근처에서 부호가 바뀐다. 출력 크기 ‖δ‖ 는 거의 불변(52→76). 이차형식의 `a²c₃ − b²c₇` 구조라 승자독식. "seg 111 에 경계가 무너졌다" 는 원인이 아니라 결과.
- **자기 항은 래치가 아니라 감쇠다.** 경계(+1.29 m) + 전달의 자기 항(−1.59 m) = **−0.30 m**, Φ 0.62. 이웃이 없으면 여유가 블록당 0.27배로 준다. **틀린 배치를 붙드는 것은 전적으로 오답끼리의 상호 지지다.** (오전에 "쌍선형 굳힘이 우물을 만든다" 고 한 설명은 이로써 부정된다.)
- **잠금의 정체는 점거이지 지지가 아니다.** 1행에서 칸 2·4 가 7↔9 를 맞바꿔 들고, 3열에서 칸 2·20 이 또 맞바꿔 든다. 맞바꿈은 "같은 줄에 중복 없음" 과 "1~9 모두 있음" 을 **둘 다 만족**하므로 칸 2·20 은 충돌 상대가 아예 없다. 실제 위반은 맞바꿈에 끼지 못하고 밀려난 꼬리 칸 21 하나뿐(3 을 집어 칸 12·26 과 충돌).
- **정답 신호는 처음부터 상수로 와 있었다.** 칸 21 의 피어에는 7 이 주어진 칸이 없고, 판의 다른 곳 주어진 7 세 개(2행8열·4행1열·6행6열)가 **−19.1** 을 90 세그먼트 내내 상수로 보낸다. 반대편도 상수: 학습된 비피어 동일숫자 끌림 **+24.6**(논리적으로 거꾸로), 막는 피어 +10.4. 즉 **모델은 답을 알고 있었고 같은 크기의 반대 힘에 눌려 있었다.**
- **움직인 항은 하나뿐** — 칸 12·26(맞게 3 을 든 칸)이 미는 힘 −10~−16. 그리고 두 칸이 동시에 강했던 적이 90 세그먼트 동안 **18번**, 매번 1~9 블록 만에 흩어졌다. 19번째만 9 블록 넘게 이어졌고 그것으로 끝났다. 칸 12 의 확신은 111.3 에 14.85 로 이전 최대 11.60 을 넘었고, 두 칸 동시 강도도 13.44 로 이전 최대 11.11 을 넘었다.
- **미확립**: 칸 21 뒤집힘과 칸 20·2 붕괴의 선후. 칸 21 이 칸 20 에 준 힘은 −1.90 인데 칸 20 의 여유는 11.8 이었고, 칸 20 을 실제로 떨어뜨린 항은 칸 2 의 그립 약화(+9.89→+4.55)였다. 세 칸의 하락은 110~115 에 **겹쳐서** 일어난다. "칸 21 이 오래 붙들어 20 을 밀어냈다" 는 제 유추였고 숫자가 뒷받침하지 않는다.

**(G) 거시 구조 — 오전의 주장 하나를 정정한다.**
"틀린 배치를 어트랙터로 만들지 말라는 요구는 손실에 없다" 고 썼는데 **틀렸다.** 깊은 지도는 매 세그먼트 "정답을 출력하라" 이므로 양방향으로 작동한다 — 정답이면 머물러라, 아니면 움직여라. 측정이 그 결과를 보인다: 풀린 상태는 235/235 가 상대 변위 <1e-4 이고 야코비안 스펙트럼 반경 **0.42**(진짜 흡인 영역), 안 풀린 상태는 변위 .19~.47 에 **|λ|=1.33** 인 불안정 모드. 128 세그먼트를 돌려도 푼 퍼즐은 하나도 안 잃는다.
그러니 이 모델은 **"정답 쪽으로 가는 흐름" 이 아니라 "정답 말고는 멈출 수 없는 흐름"** 이다. 목적성은 방향이 아니라 정지 조건에 있다. 덫 안에서 흐름과 정답 방향의 코사인은 **+0.05**, 100 세그먼트 동안 정답까지 거리 1.02→1.02 로 불변. **부족한 것은 국소 방향이고, 어트랙터 구조는 이미 있다.** 남은 문제는 "탐색을 만들어라" 가 아니라 "이미 있는 탐색을 빠르게 하라" 이며, 오늘 이긴 두 방법은 둘 다 방향을 준 게 아니라 다양체 위의 이동을 넓힌 것이다.

**(H) 과정에 대한 기록.** 오늘 실패한 설계들의 공통 원인은 하나다 — **그럴듯한 기제를 먼저 만들고 근거를 나중에 붙였고, 반증이 이미 내 출력 안에 있을 때도 읽지 않았다.** 게이트(최솟값 통계 오독), 결정 시각(상관을 원리로 승격), 세 번째 인자(궤적 범함수는 단일 궤적에서 입력 이상의 정보를 담지 않으므로 정의상 휴리스틱), 무계 성장(구동자가 해에서 0 이 되어야 안전한데 우리 Γ 는 아님) 전부 같은 형태다. 다음 세션에서는 설계 전에 해당 신호의 판별력을 먼저 재는 것을 규칙으로 둔다.

### 6.16 문헌 대조 — 내 이식이 어디서 틀렸나 (2026-08-30, `papers/`)
하루 종일 논문 없이 기제를 지어내고 반증하기를 반복한 뒤에야 읽었다. 읽고 나니 **오늘 막혔던 지점에 이미 답이 있었고, 내 이식은 세 군데가 구조적으로 틀렸다.**

**논문의 식** (Miconi et al., ICML 2018 / ICLR 2019):
```
x_j(t) = σ{ Σ_i ( w_ij + α_ij·Hebb_ij(t) ) x_i(t−1) }        (1)  연결 = 고정 기저 + 가소성 성분
Hebb_ij(t+1) = Clip( Hebb_ij(t) + M(t)·E_ij(t) )             (4)  신경조절 M 이 쓰기를 결정
E_ij(t+1)    = (1−η)E_ij(t) + η·x_i(t−1)x_j(t)               (5)  적격 흔적 = 순수 pre·post
```
- **M(t) 는 네트워크 자신의 스칼라 출력**(tanh, 부호 자유)이고 바깥 경사가 학습시킨다. 세 번째 인자는 **고르는 게 아니라 배우는 것**이다. 내가 정상성·결정 시각·대칭 불일치를 손으로 고르려다 매번 휴리스틱이라고 자백한 문제가 여기서 사라진다.
- **적격 흔적**이 기록 시각과 판단 시각을 분리한다. E 는 판단 없이 계속 쌓이고, 나중에 M 이 부호와 함께 쓴다. 내가 "기록에 파라미터를 두면 안 산다" 고 헤맨 신용 할당 구조가 이것으로 풀린다.
- 클리핑은 RL 산물이 아니라 헤브 학습의 내재적 불안정성 상쇄용이라고 본문에 명시. 우리 EMA 는 |Γ|≤1 이라 이미 그 역할을 한다.

**내 이식의 오류 셋.**
1. **기저의 정체가 다르다.** 논문의 `w_ij` 는 **학습된 파라미터**(사전 지식), `Hebb` 은 에피소드 내 획득. 우리 `a_tn` 은 매 블록 상태에서 **다시 계산되는 양**이다. 그러니 `(1−λ)a + λw` 는 "사전 대 에피소드" 가 아니라 **"순간 대 지연"** 이다. λ 스윕은 논문 구조가 아니라 지연 감소를 시험한 것이다(결과는 아래 — 실제로 이득이 있었지만 근거가 다르다).
2. **흔적에 그래프를 접어 넣었다.** 논문의 흔적은 **순수 pre·post** 이고, 부호와 위상(=위상학)은 전적으로 기저가 담당한다. 우리 `Γ = a_tn·⟨v̂,v̂⟩` 는 그래프를 흔적에 곱해 넣은 하이브리드이고, 게다가 `λ=1` 로 그 하이브리드를 **결합 전체로** 썼다. 두 이탈이 겹친 상태다.
3. **그래서 예전 기각이 무효다.** `stdp_target="value"`(Γ = D(Δ)·⟨v̂,v̂⟩)를 "부호가 없어서" 기각했는데(§ config 주석), 논문 구조에서 **흔적은 결코 단독으로 결합이 되지 않는다.** 부호는 기저가 준다. 그 기각은 논문이 쓰지 않는 형태에 대한 기각이었다.

**λ 스윕 (추론 시점, reset, 512 퍼즐, 완답 8/16/32)** — `2026-08-30/analysis/lam_sweep.py`
| | 8 | 16 | 32 | 충돌/퍼즐 |
|---|---|---|---|---|
| λ=1 (학습된 그대로) | 159 | 218 | 234 | 5.7 |
| λ=0.75 그냥 혼합 | 167 | 200 | 214 | 7.6 |
| λ=0.5 / 0.25 그냥 혼합 | 7 / 0 | 4 / 0 | 4 / 0 | 52.8 / 89.1 |
| λ=0.9 **척도 보정** | 174 | 219 | 230 | 5.5 |
| **λ=0.75 척도 보정** | **184** | **222** | **235** | **5.3** |
| λ=0.5 척도 보정 | 108 | 137 | 153 | 14.3 |
그냥 섞으면 붕괴하는데 `a` 를 `w` 의 RMS 에 맞추면 λ=0.75 가 세그먼트 8 에서 **+25(+16%)**, 충돌도 감소. 붕괴는 척도 불일치였다 — 논문의 학습되는 `α_ij` 가 바로 그 척도를 맞추는 계수이고, 우리는 그것 없이 1:1 로 섞었다.

**충실한 이식에서 따라 나오는 것 (땜빵이 아니라 유도).**
`결합 = a_tn + α·E_tn`, `E ← (1−η)E + η·⟨v̂_t,v̂_n⟩` 로 쓰면:
- 일관되게 **같은 숫자**를 든 피어 쌍은 E → +1. **학습된 α<0** 이면 결합이 더 음수 = **더 세게 밀어낸다.**
- 일관되게 **다른 숫자**를 든 피어 쌍은 E < 0 이라 α·E > 0 = 반발이 약해진다.
- 즉 스칼라 하나로 **"오래 같은 값을 들고 있을수록 더 떼어 놓는다"** 는 위반 구동 압력이 나오고, **위반이 해소되면 저절로 꺼진다**(해에서 구동자가 0 이 되는 CTDS 의 안전 조건을 자동으로 만족).
- 이건 내가 오늘 세 번 손으로 만들려다 세 번 다 틀렸던 그 항이다(부호를 잘못 겨냥, 상한·감쇠로 정리의 전제 파괴, 구동자가 해에서 0 이 아님). **여기서는 부호가 학습 파라미터이고 나머지는 구조가 보장한다.** 그리고 이 역할에는 세 번째 인자 M 이 필요 없다 — M 은 그보다 어려운 역할(무엇이 좋은 배치인가) 몫이다.

**유보.**
- 모델은 `결합 = w` 로 학습됐으므로 `a + α·E` 는 분포 밖이다. 추론에서 안 나와도 구조의 반증이 아니라 "학습이 필요하다" 일 뿐이다.
- 논문은 **연결별** α 를 강조하는데, 자유 81×81 α 는 위치 일반성을 깨서 ARC 이식에 해롭다. 헤드별 또는 (헤드, |Δ|)별이 타협점.
- M 은 여전히 학습이 필요하고 내용도 미해결.
- 그리고 오늘 경험적으로 가장 크게 이긴 것은 가소성이 아니라 제시 간 일관성(+154)이었다. 기대값으로는 그쪽이 먼저일 수 있다.

**과정에 대한 규칙 하나.** 오늘 실패한 네 설계는 전부 "만들고 나서 근거를 붙였다". 이 절의 유도는 **학습 없이 추론에서 바로 시험 가능**하다는 점이 다르다(`agree_trace.py`). 앞으로 설계는 그 전제를 먼저 잴 수 있는 형태로만 낸다.

### 6.17 충실 이식 사양 — 다음에 학습시킬 구조 (구현 전 사전 등록)

**추론 시험 결과 먼저**: `agree_trace.py` 로 `결합 = a + α·EMA(⟨v̂,v̂⟩)` 를 학습된 stdp1 에 얹으면 α = −1/−0.5/−0.25/+0.25/+0.5 **전부 완답 0**(충돌 269~420). 결합의 정체가 통째로 다른 물건이라 완전한 분포 밖이다. **구조의 반증이 아니라 "추론으로는 확인 불가, 학습해야 한다"** 는 뜻이다.

**바꿀 식** (현재 → 충실형). 현재는 버퍼 하나(`w`)가 흔적·가소성·결합을 모두 겸한다. 충실형은 논문대로 셋을 분리한다.
```
현재:   Γ = a·⟨v̂,v̂⟩ ;  w ← w + η(Γ−w) ;  결합 = w                       (λ=1, 기저 없음)
충실형: E ← (1−η_h)E + η_h·⟨v̂_t,v̂_n⟩                                     (5) 적격 흔적 = 순수 pre·post, [−1,1]
        H ← Clip(H + M_t·E)                                              (4) 가소성 성분, [−1,1], Clip = hard clip
        결합 = a_tn + α·H_tn                                             (1) 기저(위상 커널: 부호·위상학) + α·가소성
```
- `a_tn` 은 지금 그대로. **부호와 위상학은 전적으로 기저가 담당하고 흔적은 크기만 더한다** — 이것이 §6.16 의 오류 2·3 을 고치는 지점.
- `M_t = tanh(u·h_t + b)` — 칸별 스칼라, `u ∈ R^832` 학습. 논문의 M(t) 는 전역 스칼라이거나 연결별 메타학습 벡터인데, 우리는 칸(post)별이 중간 지점. **손으로 고르지 않는다.**
- `α` 는 **(헤드, ‖Δ‖) 별 테이블**(8 × 17 = 136 파라미터). 자유 81×81 은 위치 일반성을 깨서 ARC 이식에 해로우므로 상대 거리로만 색인한다.
- `E`, `H` 는 퍼즐 시작에 0 (논문: "initialized to zero at the beginning of each episode"). 기존 `fresh` 경로 재사용.

**절제 사다리** (논문 자신의 진행과 일치 — 어느 부품이 일하는지 가른다)
| 단계 | 구조 | 대응 |
|---|---|---|
| P0 | 가소성 없음 (`plastic=false`) | minimal — 기준선 |
| P1 | 결합 = a + α·E (H·M 없음) | Differentiable plasticity (ICML18) |
| P2 | + H, M≡1 (항상 씀) | 두 단계 기억, 조절 없음 |
| P3 | + 학습되는 M_t | Backpropamine (ICLR19) |
| (참고) | 현재 stdp1 (하이브리드 Γ, λ=1) | 어느 쪽도 아님 |

**하이퍼·초기화**
- `α` 초기값은 `α·RMS(H) ≈ 0.3·RMS(a)` 가 되도록. 가소성이 소수 기여로 출발해 학습이 키우도록.
- `b` 는 `tanh(b) ≈ 0.5` 로 초기화 — M 을 0 에서 시작하면 쓰기가 없어 `u` 에 경사가 안 간다(게이트에서 겪은 죽은 출발).
- `α, u, b, eta_raw` 는 weight decay 제외 목록에 추가.
- 나머지(lr 1e-4, bs 128, 16 세그, 8 블록)는 그대로 — 비교 가능해야 한다.

**시평 문제 (실질 난점)**. 논문은 에피소드 전체로 역전파하지만 우리 하네스는 세그먼트마다 detach 한다. M 의 이득이 8 블록 밖에 있으면 학습되지 않는다. 선택지: (a) 그대로 두고 세그먼트 안 이득만으로 학습되는지 본다(가장 싸다), (b) `E`·`H` 채널에 대해서만 k 세그먼트 truncated BPTT 를 연다(메모리 k배; E,H 각 [B,8,81,81] = 27MB/세그, k=2 는 가능할 것). **(a) 를 먼저 하고, P3 만 이득이 없으면 (b) 를 시험한다.**

**판정선** (held-out 셀, 같은 스텝에서). minimal `.494 / .583 / .633 / .668` @ 1,953 / 3,906 / 5,859 / 7,812. stdp1 `.478 / .543 / .593 / .624`. 70% 돌파 스텝도 함께(stdp1 은 train 기준 10,400).

**사전 등록한 예측 (맞으면 구조가 맞는 것, 틀리면 이 절이 틀린 것)**
1. 학습된 `α` 가 **피어 거리대(‖Δ‖ 가 같은 행·열·박스에 해당하는 값)에서 음수**로 간다. → "오래 같은 값을 든 쌍일수록 더 밀어냄" 의 직접 증거. 체크포인트에서 바로 확인 가능.
2. 16→128 반복 외삽 이득이 stdp1(+123)보다 **크다**. 위반이 지속되는 쌍에서 압력이 계속 자라므로.
3. **reset 대 carry 의 차이가 줄어든다**. 기저 `a` 가 복원되어 결합 전체가 지연에 걸리지 않으므로. (stdp1 은 reset 이 +56 이었다.)
4. P1 → P2 → P3 로 갈수록 좋아진다. 안 그러면 그 부품은 이 과제에 불필요.

**하지 말 것 (오늘의 교훈)**: 세 번째 인자를 손으로 고르지 않는다. 흔적을 단독으로 결합으로 쓰지 않는다. 척도를 맞추지 않고 섞지 않는다. 그리고 초반 8k 스텝 곡선으로 판정하지 않는다 — stdp1 이 초반에 느리고도 228k 에서 원판(14.1M)의 959 를 절반 파라미터로 넘어 974 를 냈다.

### 6.15 남은 질문 (내일)
1. 원판 재학습을 7,812 까지 돌려 7.4 의 조성 비교를 같은 스텝에서 (`CKPT_PATH=checkpoints/R1B8_bilin_same/step_1953.pt ... resume.sh`).
2. ψ 자유 + 분할 + `Γ=a·⟨v̂,v̂⟩` 4단계 재정의(대칭 포기, 이류 유지) — 또는 순간 결합 대칭 + 방향은 기억 창 β 에.
3. 자기교정 루프의 다음 지렛대는 탐지가 아니라 **재형성 억제**: 기억 강화(λ 스케줄), 되돌린 칸 tabu, 쌍 단위 되돌리기.
4. 증강 합집합(6.8)을 기본 추론으로 채택하고, 학습에 등변성 정칙화를 넣어 카오스 기저율(14~19%)을 줄이기.
