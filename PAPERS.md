# 참고문헌 보관 — ARC 전환 (2026-08-27)

PDF 실물은 `papers/` (tracked, 3.1MB). 원본 취득 명령(참고):

```bash
mkdir -p refs/papers && cd refs/papers
for id in 2510.04871 2512.14693 2307.00337 2106.04537 2506.15746; do curl -sL -o $id.pdf https://arxiv.org/pdf/$id; done
cd .. && git clone --depth 1 https://github.com/UbiquantAI/URM.git      # ARC 데이터 동봉 (kaggle/combined/*.json)
git clone --depth 1 https://github.com/SamsungSAILMontreal/TinyRecursiveModels.git   # TRM 원본 (미취득)
```

| # | arXiv | 논문 | 왜 |
|---|---|---|---|
| 1 | 2510.04871 | **TRM** — Less is More: Recursive Reasoning with Tiny Networks (Jolicoeur-Martineau, Samsung SAIL) | 7M 파라미터로 ARC-AGI-1 45% / 2 8%. 우리 하네스 계보의 직계 조상. 4×H100·3일의 출처 |
| 2 | 2512.14693 | **URM** — Universal Reasoning Model (Ubiquant) | 우리가 이식한 하네스. ARC-AGI-1 **53.8%** / ARC-AGI-2 **16.0%** pass@1. 주장: 이득은 재귀 귀납편향 + **트랜스포머의 강한 비선형 성분**에서 오지 정교한 구조 설계가 아니다 |
| 3 | 2307.00337 | Recursive Algorithmic Reasoning (Jürß·Jayalath·Veličković) | 신경 알고리즘 추론 계열 — 재귀 호출 스택을 명시 구조로 |
| 4 | 2106.04537 | Can You Learn an Algorithm? Easy-to-Hard 일반화 (Schwarzschild et al.) | 재귀 반복 수 외삽(우리 rand_R·시간 외삽 축의 문헌 대응) |
| 5 | 2506.15746 | Neural Cellular Automata for ARC-AGI (Xu·Miikkulainen, UT Austin) | ARC를 NCA로 — 우리 RD/튜링 프레이밍의 최근접 선행 연구. 차별점 설계에 필수 |

## URM 저장소 (refs/URM, c14e55f 계열)

ARC 데이터가 저장소에 동봉돼 있다 — 별도 다운로드 불필요:
`kaggle/combined/arc-agi_{training,evaluation,training2,evaluation2,concept}-{challenges,solutions}.json`

```bash
python -m data.build_arc_dataset --input-file-prefix kaggle/combined/arc-agi \
  --output-dir data/arc2concept-aug-1000 --subsets training2 evaluation2 concept --test-set-name evaluation2
```
발사 스크립트 `scripts/URM_arcagi{1,2}.sh` (8 GPU torchrun, epochs=200000, loops16·H2·L6·layers4, puzzle_emb_lr=1e-2, wd=0.1, ARC-2는 `use_muon=True`).

---

# 정독 요약 (2026-08-27) — 우리 설계에 걸리는 지점만

> TRM·URM 은 기 숙지. 아래 세 편은 "우리가 이미 측정한 실패 양상에 문헌이 붙인 이름"을 찾는 관점으로 읽었다.
> 각 절 끝의 **[우리]** 가 대응 지점. 인용 수치는 원문 기준.

## A. Neural Cellular Automata for ARC-AGI (Xu·Miikkulainen, ALIFE 2025 / arXiv 2506.15746v3)

**프로토콜 — 과제당 독립 학습, 사전학습 없음.** ARC-AGI-1 공개 *training* 400과제, 과제마다 NCA 하나를 처음부터.
~1만 파라미터(은닉 20채널; 상당수 과제는 2,500 이하로 충분), **10 스텝**, 예제당 128 롤아웃/에폭, 800 에폭, AdamW 2e-3→1e-4. **과제당 수 분, 소비자 GPU 1대.**

**구조.** 10색 one-hot 이 첫 10채널(softmax), 나머지는 자유 은닉. 갱신은 잔차가 아니라 **`s_{t+1} = f(s_t)` 전체 출력** —
잔차형 `s + f(s)` 는 "포화·기울기 소실"로 더 나빴다. Sobel 고정 필터 대신 학습 conv, 채널 LayerNorm. 생존 마스크는 무익.

**핵심 기법 둘.**
1. **비동기 확률 갱신 + 마스크 확률을 롤아웃마다 [0, 0.75]에서 추출** (고정 0.5 는 수렴 불안정). 마스크된 셀은 0 이 아니라 옛/새 상태를 랜덤 강도로 보간.
   효과 = 정규화: "훈련 예제에 과적합한 약한 해를 걸러내고, **갱신 스케줄에 무관한 안정 끌개**를 만든다." 실패 과제에서도 더 일관된 출력.
   테스트는 동기(마스크 없음)로 돌려도 안정.
2. **매 스텝 손실** (마지막만 감독하면 ① 1스텝 과제가 조기 수렴을 안 배워 중간 상태가 오염, ② 긴 스텝에서 기울기 흐름 붕괴, ③ 오차 누적).

**결과.** 400 중 138 은 격자 크기 변화(범위 밖), 90 은 색 일반화(훈련에 없는 색 → 구조적 실패) → **가능 172 중 23 해결 (13.4%)**.
loss<0.01 인 과제 95개 중 성공은 절반뿐(훈련 손실 ≠ 성공), 추가 48개는 픽셀 90%+.
- 성공 사례: 나선(28e73c20) — 작은 예제로 학습해 **100×100 까지 크기 불변** 확장 (30 스텝 학습이 10 보다 낫다).
- 실패 3분류: ① 시드·설정에 따라 들쭉날쭉 ② **훈련은 맞추고 테스트 실패(최다, 과적합)** ③ 암기조차 실패.
- 구조적 한계 (저자 명시): **전역 조정·장거리 전파 과제에서 셀이 전역 문맥 확립 전에 조기 갱신** → 구조적 과적합.
- 근본 문제: 예제 2~3개에 맞는 해가 무수히 많고 경사하강은 그중 아무거나 잡는다 — 프로그램 합성의 Kolmogorov 사전분포에 해당하는 것이 없음. 제안: 최소 망 탐색, 변분/베이즈 NCA (LPN, CompressARC 인용).
- 미래 과제 목록: 적응 계산량, 변분 NCA, 해석가능성, **"attention·gating 등 주류 기법의 국소 갱신 보존 하 도입"**, 더 넓은 벤치마크.
- 동시 연구: Guichard et al. 2025 *ARC-NCA* (ALIFE 2025, 은닉 메모리 상태 추가) — **미취득, 확보 필요**. CAX(Faldor·Cully 2024)는 1D-ARC 에서 GPT-4 상회, 카운팅은 NCA 에 어려움.

**[우리]**
- 위치: 우리 코어 = "부호 있는 비국소 커널 + 학습된 수용장(α)"의 NCA. NCA(3×3 국소)와 트랜스포머(전역 softmax) 사이의 빈 칸. 저자가 실패 원인으로 꼽은 **장거리 전파**가 정확히 α→0 전역 채널이 하는 일 (1일차 parity: 수용장 128~3698 ≫ T 인 집계 채널 자발 형성). 저자의 미래 과제 4번이 문자 그대로 우리 구조 — Novelty 서술의 앵커.
- **마스크 확률 무작위화 ≡ rand_R** (REPORT §4.4: dt 무작위화로 R=96 정확도 .286→.947). 둘 다 "다양한 갱신 스케줄에 노출 → 스케줄 무관 끌개". 명제 24(학습된 흐름은 시간 경계값 문제, 고정점 아님)를 NCA 가 독립 재발견한 셈. 논문에서 이 대응을 한 줄로 못 박을 것.
- 매 스텝 손실 ≡ URM 하네스 deep supervision (세그 16회).
- "잔차형이 더 나빴다" — 방향이 같다: 우리도 흐름판 M1(작은 dt 잔차)이 벽, dt=1 완보 R1B8 이 돌파(3일차). 단 이유는 다름(경계 밀도). NCA 는 LayerNorm 으로, 우리는 Φ 포화로 야코비안을 잡는다(5일차 §7.5).
- 해 다중성 = 우리 다중안정성 진단(5일차 §7.6: 오답의 56% 가 제약 무위반 자기일관 격자)의 과제 수준 판. 같은 현상.
- 범위 제한이 같다: 크기 변화 과제(138/400)·색 일반화(90/400). 색은 TRM/URM 식 색치환 증강이 부분 해결, 크기는 EOS 예측(URM 형식) 또는 포기 중 택일.
- **비용 산술 (ARC 전략의 핵심)**: NCA 과제당 수 분 → 240과제 ≈ 수 시간, **Kaggle 예산(4×L4·12h) 안**. TRM/URM 3일×4×H100 ≈ 12 H100-일 vs 예산 ≈ 0.4 H100-일 (L4≈1/5) — **약 30배 초과**. 과제당 학습 프로토콜에 우리 코어(d=64, 25K)를 얹는 것이 예산상 유일하게 닫히는 경로 후보. 단 NCA 천장(가능 과제의 13%, 사전분포 부재)을 어떻게 넘느냐가 문제 = 공개 과제로 공유 코어 사전학습(사전분포) + 테스트 시 과제별 적응.
- 비교 시 주의: NCA 수치는 training set·가능 과제 172 기준. 같은 부분집합에서 재야 한다.

## B. Can You Learn an Algorithm? Easy-to-Hard (Schwarzschild et al., NeurIPS 2021 / arXiv 2106.04537)

**설정.** 가중치 공유 잔차 블록(4 conv 층, BN·bias 없음)을 반복하는 순환망 vs 동일 유효깊이 피드포워드.
쉬운 문제로 학습 → 어려운 문제를 **테스트 시 반복 횟수를 늘려** 풀게 한다. 유효깊이 = 1 + 4·반복 + 3.
- prefix sum mod 2: 32비트 학습 → 40/44/48비트. maze: 9×9 (5만) → 13×13 (1만). chess(Lichess): 레이팅 <1385 → >1385.

**결과.**
- prefix: 10회 반복으로 학습한 순환망이 **16회 반복 시 40비트 90%+**. 같은 깊이(68층) FF ≈ 22~25%, 528층 FF 도 ≤53%/27%. **외삽은 반복을 늘리기 전엔 나타나지 않는다.**
- 수용장 통제: 같은 수용장의 FF 는 실패 → 이득은 수용장이 아니라 **학습된 과정**. dilated 필터(깊이 없이 수용장)는 소폭 보조.
- maze: 20회 학습(84층) → 큰 미로 ~50%, +5회에 70%+. chess: 소폭이지만 단조.
- **반복 선택 = 신뢰도**: 출력 신뢰도가 정확도의 대리 → 가장 자신 있는 반복의 출력을 답으로.
- 중간 출력 해부: prefix 는 앞 비트부터 순서대로 확정(순진한 순차 알고리즘과 동형), maze 는 후보 경로 두 갈래 → 좁혀감, chess 는 "어느 말"부터 "어디로"로.
- **과사고(overthinking)**: maze 에서 정답 도달 후 2회 유지하다 픽셀 몇 개가 뒤집힘(#9). 저자 미해결 질문: "정답에 도달한 뒤 갑자기 틀리지 않게, 멈출 때를 알 수 있나."
- 학습 불안정: prefix 는 훈련 100% 모델만 저장, clip 1.0. 큰 문제로 학습한 모델은 작은 문제를 더 적은 반복에 푼다(44비트/10회 → 16비트/4회).
- 후속 (미취득): Bansal·Schwarzschild et al. 2022 *"End-to-end Algorithm Synthesis with Recurrent Networks: Logical Extrapolation Without Overthinking"* — 과사고 처방 = **recall(매 반복 입력 재주입) + progressive loss(중간 상태에서 기울기 끊고 재시작)**. **확보 필요.**

**[우리]**
- 테스트 시 반복 연장 = `depth_use.py` 의 loops 32 실험. 46.9k 판에서 163→170(+7): 약하지만 같은 방향. ARC 프로토콜로 채택: loops=16 학습 → 테스트 loops 16~64 쓸기 + 셀 엔트로피로 반복 선택(우리는 세그별 logit lens 가 이미 있다).
- 과사고 = 5일차 §2.5 "미해결 칸이 매 블록 7~10% argmax 뒤집힘". 문헌의 처방 둘이 **URM 하네스에 이미 들어 있다**: recall = block_inj(끄면 0.565→0.104 붕괴, 4일차), progressive loss = detach 세그먼트 + carry. 즉 하네스는 임의 선택이 아니라 과사고의 알려진 치료제. 논문 서술에 쓸 것.
- "같은 수용장 FF 실패 → 과정이 관건" ↔ 우리 A 고정 대조(폭×16·깊이×4 무반응, REPORT §8.7). 같은 논증 구조.
- 크기 일반화(작은 미로→큰 미로)는 conv 의 병진불변 덕. 우리 커널은 상대위치 `θ·Δ` 라 같은 성질 — **TRM/URM(고정 900 캔버스 + 과제 임베딩)은 없는 성질**. Universality 축.
- 명제 24 와의 긴장: 우리 흐름판은 r>R 붕괴(시간 경계값 해). E2H 의 외삽은 목표가 고정점(prefix sum 은 안정 상태)이라 가능. R1B8 이산판이 외삽을 조금 보이는 것과 정합 — ARC 에서 "고정점까지 적용"(`--fixpoint`) 과제 정의가 이 축을 살린다.

## C. Recursive Algorithmic Reasoning (Jürß·Jayalath·Veličković, LoG 2023 / arXiv 2307.00337)

**설정.** 신경 알고리즘 추론(NAR), CLRS-30 의 DFS. GNN 프로세서가 매 스텝 알고리즘 변수(**힌트**)를 예측하도록 감독.
문제: 재귀 알고리즘은 호출 스택만큼의 메모리가 필요한데 망은 고정 크기 → 큰 그래프로 OOD 실패
(Ibarz et al. 일반 학습기: 32노드 99.8% / **96노드 53.9%**). Delétang et al. 2023(촘스키 계층): 구조화 메모리 없는 망은 문맥의존 과제에서 일반화 못 함.

**제안.**
1. **스택 증강 GNN**: push/pop/noop 을 그래프 힌트로 감독(알고리즘의 재귀 진입/복귀에서 추출). 노드별 스택 / 그래프 스택 두 종.
   "스택 최상단을 입력으로 쓰는 것 = **시간을 가로지르는 동적 skip connection** → 중간 스텝을 역전파 안 해도 됨(기울기 소실 완화)."
2. **궤적 샘플링 정정**: CLRS-30 의 노드별 힌트(모든 노드의 predecessor 등)는 **전역 상태를 통째로 주는 것**이라 DFS 가 재귀 없이 DP 로 풀림(Xu et al. 2020 GNN–DP 정렬). → 현재 노드 정보만 주는 그래프 힌트로 바꿔 상태 저장/복원을 강제. 순환 은닉상태도 제거(안 그러면 노드별 힌트를 은닉에 재학습). 출력은 한 번에 예측하지 않고 **스텝마다 표에 기록(output collection)**.

**결과 (32노드 / 96노드).** 노드별 스택 + 그래프 힌트 + 출력 수집 + teacher forcing 50%: **100 / 100**. 그래프 스택: 98 / 73. 스택 제거: 65 / 73 (OOD 이득의 주원은 힌트 정렬, 스택은 in-dist 복원). 출력 수집 제거: 50 / 25. teacher forcing 제거: 72 / 44. 어텐션 풀링: 92 / 49 (합 풀링 크기가 OOD).
한계: **정답 힌트가 있어야 스택을 감독** — NAR 밖에선 없음(RL 로 대체 제안).

**[우리]**
- 5일차 §4-3 "미해결 칸 순환 = 탐색 종료·백트래킹 기제 부재, URM 도 같은 천장"에 문헌이 붙인 이름: **호출 스택 부재**. 스도쿠 탐색 필요 칸 52.4% 는 되돌림이 필요하고, 제약 전파는 "노드별 힌트 체제"(전역 상태 병렬 갱신 = DP) 라 재귀 없이 가는 데까지만 간다. 다중안정성 = DP 가 탐색 없이 자기일관 오답 고정점에 앉는 것.
- 스도쿠·ARC 에는 중간 힌트가 없다(입력→출력뿐). NCA 의 "해 다중성"과 RAR 의 "정렬 필요"는 같은 말: **중간 감독 없이는 망이 아무 해나 잡는다.** 우리 deep supervision 은 중간 *알고리즘 상태* 가 아니라 *최종 답* 을 매 세그마다 감독 — 힌트와 다르다. 이 차이를 논문에서 명시.
- "스택 최상단 = 시간 가로지르는 skip" 의 동역학 번역 = 감쇠하지 않는 느린 변수 (SUDOKU.md 감쇠 시계 B, DAMPING). 스택은 아니지만 같은 자리.
- ARC 우선순위: 대부분의 ARC 과제는 재귀가 아니라 국소/DP 형 변환이라 9주 안에 스택을 쫓지 않는다. **정직한 한계 서술**로만 쓴다 ("이 구조(와 URM)가 못 푸는 것 = 탐색형").
- 합 풀링 크기 OOD 실패 ↔ 우리는 `|a|≤1`·0차 동차(명제 1·2)라 어텐션 크기가 h 스케일에 불변. 단 `d_t=Σa` 는 토큰 수에 비례할 수 있음 — α>0 이 상한(κ₀)을 준다. 크기 OOD 주장 시 이 조건을 붙일 것.

## D. 세 편이 삼각측량하는 것 — 우리가 이미 잰 실패 3종

| 실패 양상 | 문헌의 이름 | 우리 실측 | 우리의 대응 |
|---|---|---|---|
| 스케줄 특정 해·과사고 | E2H overthinking / NCA "갱신 순서 암기" | 명제 24 (r>R 붕괴), 5일차 §2.5 순환 | rand_R · block_inj · detach 세그 (= DeepThinking 처방 2종) |
| 해 다중성 | NCA Kolmogorov 부재 / RAR 정렬 필요 | 5일차 §7.6 자기일관 오답 56% | 미해결 — 사전분포(공유 코어 사전학습) 또는 변분 |
| 구조화 메모리 부재 | RAR 호출 스택 / Delétang 촘스키 | 탐색 칸 52.4% 천장 (URM 동일) | 없음 — 한계로 명시 |

**ARC 전략 함의.** (i) 과제당 학습(NCA)은 예산 안, 공유 사전학습(TRM/URM)은 30배 초과 → 하이브리드(공개 과제로 코어 사전학습 + 테스트 시 과제별 적응)가 닫히는 유일한 형태. (ii) NCA 의 한계(국소·13%)와 미래 과제(attention/gating)가 우리 구조의 자리를 문헌이 비워둔 형태. (iii) 테스트 시 반복 연장 + 신뢰도 선택(E2H)은 무료 이득 후보. (iv) 크기 일반화는 상대위치 커널의 구조적 성질 — TRM/URM 에 없는 Universality 축.

**추가 확보 권장**: Guichard et al. 2025 *ARC-NCA* (ALIFE) · Bansal et al. 2022 *Logical Extrapolation Without Overthinking* (NeurIPS).
