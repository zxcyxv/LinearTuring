# 연구 색인 — 무엇을 알아냈고, 무엇을 채택했나

> 이 파일이 진입점이다. 상세는 각 문서로.
> **변종 동결 중**: W_O 절제 4종 완료 후 신규 변종 없음. 다음 단계는 스도쿠(SUDOKU.md).

## 문서 지도

| 파일 | 역할 |
|---|---|
| `INDEX.md` | 이 파일 — 결론·채택 원장 |
| `REPORT.md` | 실험 서사 전체 (사양 검증 → 카오스 → 폭/깊이 → 경계 사영) |
| `THEORY.md` | 명제 20개, [정리]/[실측]/[미결] 구분 |
| `SUDOKU.md` | 다음 단계 설계 사양 (검증된 선택 + 열린 결정) |
| `RESULTS.md` | 학습 101건 자동 원장 |

## 한 문단 요약

활성화함수 없는 위상-정규화 선형 어텐션(Model 1)은 MNIST 97.6%를 내지만, 그 비선형성은
전부 "입력→어텐션 그래프" 1회 사상에 있다(아핀 재귀 대조 97.06%). 재귀의 상태 의존
게이팅 `A(h)`는 이상한 끌개(λ≈+1.7)를 만들며, 순차 비선형 과제(CA)에서만 필수가 되고
— 폭×16·깊이×4로도 대체 불가 — 그 용량은 폭이 정한다(날카로운 문턱). γ는 스케일
게이지이고, 반응-확산 해석은 부호 그래프 균형 흐름으로 교체됐다. 유일하게 검증된 구조
개선은 τ 블록 경계 사영 W_O(3.2배, 시너지형)다.

## 핵심 수치 6개

| | 값 | 의미 |
|---|---|---|
| MNIST (kern) | 97.62% | 활성화 0개, 26.5K 파라미터 |
| 아핀 재귀 대조 (frozenA_g0) | 97.06% | MNIST 비선형성은 x→A 1회가 전부 |
| λ (randR) | +1.74 | 이상한 끌개 — BPTT 안정성 상수이기도 함 |
| CA 상전이 | A자유 d256: 0.997 / A고정: 0.000 | 게이팅은 폭·깊이로 대체 불가 |
| 폭 문턱 | d=192: 0.000 → d=256: 1.000 | 8항 parity, 날카로운 전이 |
| 경계 사영 | 0.131 → 0.414 (2k step) → **0.879 (4k step, 미포화)** | 폭 9배(0.940)의 실질 대체재, 파라미터 1/7.6 |

## 변종 원장 — 채택 / 기각 / 계류

### 채택 (스도쿠 레시피에 포함)
| 변종 | 근거 |
|---|---|
| **A 자유** (상태 의존 게이팅) | 순차 과제의 유일한 계산원. 2×2 + 폭/깊이 대조로 삼중 확정 |
| **rand_R** (dt 무작위화) | 정밀화 저항 .286→.947, 비용 0.3%p. 진짜 연속 흐름을 만듦 |
| **커널 lr ×30** | 커널이 학습되는 유일한 설정, MNIST 최고 97.62% |
| **경계 사영 W_O (수축 init)** | 3.2배→**5.1배**(수축 init, 0.666), 모드 절제 완료. ρ=e^{−λ̂} — FTLE 가 설계 상수 |
| **value bias b 유지** | 게이지를 깨는 유일한 항이자 동차성 파괴자. 단 γ 큰 영역에선 유해 (√γ 증폭) |

### 기각
| 변종/가설 | 기각 근거 |
|---|---|
| A 고정 (계산용으로) | 폭×16, 깊이×4 에 무반응. 단 **분석 도구로는 유용** (사양 이론이 성립하는 극한) |
| γ 조절 (소산 손잡이로) | 스케일 게이지 (γ×1600 에 λ 불변). 흡수구 반경은 h 의 단위 |
| ψ 감시 (§2.4 처방) | ψ≡0 고정해도 진동 유지. 감시 대상은 Λ 스펙트럼 |
| 1/t 정규화 (Model 0) | 위상 정규화가 대체, |a|≤1 구조 보장 |
| τ 연장 (A 고정 구제용) | 완전 평평 (0.000→0.001) |
| 잡음 커리큘럼 (커널 학습 유도) | 커널이 더 안 움직임 (0.097) — 원인은 난이도가 아니라 그래디언트 스케일 |
| noov 혼합 전무 + 깊이 | 0.0002 — 혼합 없으면 깊이가 라운드를 못 삼 |

### 계류 (실험 중이거나 다음 단계)
| 변종 | 상태 |
|---|---|
| ~~W_O 모드~~ | **확정: 수축 init (ρ=e^{−λ̂}≈0.18·I) 채택** — 0.666 > 직교 0.492 > 잔차 0.453 > plain 0.414 > 헤드별 0.201. λ 측정값이 설계 상수로 환류된 첫 사례. 직교는 이론 보장 필요시 대안. 수축 init × 다른 축 조합은 스도쿠에서 |
| 감쇠 시계 (S 의 재귀축 취급: 리셋/누적/블록리셋) | 설계 축 정의됨, 미실험. fast-slow(FHN) 후보 |
| sheaf 수선 W_OVᵀ(·)W_OV | 이론상 소산 보장. 미실험 |
| 커리큘럼/국소화 (폭 문턱 인하) | 예측 등록됨, 미실험 |
| 중간상태 프로브 (축차 시뮬레이션 검증) | 체크포인트 있음, 미실행 — 해석가능성 최우선 후보 |

## 스도쿠 확정 레시피 (현재 시점)

```
Model 1 + A 자유 + rand_R + 커널 lr ×30 + 경계 W_O(모드 미정) + b 유지
폭: 과제 결합 항수 기준 문턱 여유 확보 · λ(FTLE) 학습 중 로깅 (BPTT 안정성 데이터)
```

## 자기정정 기록 (교훈 순)

1. 에너지 증가 ≠ 확산 반증 (튜링 패턴의 정의) → 항별 수지로 교체
2. 비정규화 라플라시안의 국소화 인공물 → 정규화 + 참여비
3. `3^R` 차수 논증은 Model 0 전용 (정규화가 0차 동차로 만듦)
4. "용량 가설 기각되는 중" (d≤192 만 보고) → d=256 에서 반전. **날카로운 문턱 아래에서는
   모든 폭이 같아 보인다** — 이 연구에서 가장 반복된 함정
5. 명제 9 증명 결함 (전역 vs 토큰별 감쇠) → Stuart–Landau 재식별
6. k*=1.12 "튜링 파장 선택" → θ 초기화 유산 (미학습 모델 동일값)
7. SQ-경도 "구조적 불능" → 폭 요구량으로 완화 (d=256 이 풂)
