# 학습 계획 — 2026-09-01 (λ 되돌리기)

작성 2026-08-31. 구조 사양은 `SPEC.md`, 미해결 목록은 `UNSUP.md`.
`[검증]` 이번 세션 재현/측정 · `[코드]` 파일 확인.

---

## 한 문장

**`stdp_lam_fixed=1.0` 을 빼고 λ 를 헤드별 학습(init 0.25)으로 되돌린다. 그 하나만 바꾼다.**

---

## 왜 이것인가 — 근거

### 근거 1. 이식 실험은 λ 가 작아야 한다고 말한다 `[검증: 재현 완료]`
`analysis/stdp_infer.py`, 체크포인트 123k(구 아키텍처, `w` 를 **모르는** 가중치), held-out 2048, loops 16,
**학습 없이 추론에만** 얹음. `STDP.md §2.3` 기록과 소수점까지 일치.

| η \ λ | 0.25 | 0.5 | 0.75 |
|---|---|---|---|
| 0.1 | 969 (+22) | 991 (+44) | **31** |
| 0.05 | **999 (+52)** 셀 0.7351→0.7447 | 687 | 533 |

기준 947. 얹은 규칙은 가장 단순한 형태다 — `Γ = a` (agree 곱 없음), **이득 1**, `w ← w + η(Γ−w)`,
`결합 = (1−λ)a + λw`. 읽을 것 셋:
- **느린 기억(η=0.05, τ=20스텝)이 실제로 돕는다.**
- **기억은 소수 성분이어야 한다.** λ=0.75 에서 붕괴.
- **이득 1 로 +52.** `κ/δ > 1` 없이 나온 결과다.

### 근거 2. 7일차 학습은 그 붕괴 지점 너머로 갔다 `[코드][검증]`
`sudoku/minimal.yaml` 기본값은 이미 옳았다 — `stdp_lam_init: 0.25`, `stdp_lam_fixed: -1.0` (λ 학습).
그런데 `R1B8_min_stdp1` 은 명령줄에서 `arch.stdp_lam_fixed=1.0` 으로 덮어썼다.
결과: 체크포인트의 `lam_raw` 가 8헤드 전부 정확히 0.25(=init) 에 얼어 있다 — 그래디언트가 안 갔다.

### 근거 3. λ=1 로 학습된 모델은 섭동 불가다 `[검증]`
`2026-08-31/analysis/lam_eta_probe.py`, stdp1 @228,501, held-out 2048, loops 16.

| λ \ η | learned | 0.1 | 0.05 |
|---|---|---|---|
| **1.0** | **966** (기준) | 501 | 459 |
| 0.75 | 894 | 885 | 773 |
| 0.5 | 22 | 58 | 72 |
| 0.25 | 0 | 0 | 0 |

λ=1 로 학습하면 신선한 커널 `a` 가 전달에 **한 번도 쓰이지 않는다.** 그래서 추론에서 `a` 를 조금만
섞어도 모델이 본 적 없는 신호가 되어 무너진다. **이 축은 추론으로 물을 수 없고 학습으로만 물을 수 있다.**

### 근거 4. 나란히 놓으면
| | 완답/2048 |
|---|---|
| 123k 원판 | 947 |
| **123k + 이식 기억 (λ=0.25, η=0.05, 학습 없음)** | **999** |
| stdp1 학습판 (λ=1 강제, 228,501) | 974 (곡선 최종 980) |

아키텍처가 다르므로(14.1M 구판 vs 7.16M minimal) 깨끗한 비교는 아니다.
**가중치가 `w` 를 모르는 채 얹은 쪽이 `w` 를 전담시켜 학습한 쪽보다 높다** 는 사실만 기록한다.

---

## 런 정의

`arch=minimal`, 나머지는 stdp1 과 **완전히 동일**. 데이터 `sudoku-extreme-1k-aug-1000`,
`global_batch_size=128`, `lr=1e-4`, `wd=1.0`, `ema=True`, `loops=16`, `blocks_per_seg=8`, `d=832`, `H=8`.

### Run A (주력) — λ 만 바꾼다
```bash
LT_ROOT=/root/LinearTuring ARCH=minimal \
bash sudoku/launch.sh R1B8_min_lam arch.stdp=true arch.stdp_target=product
```
stdp1 과의 차이는 **`arch.stdp_lam_fixed=1.0` 을 안 주는 것 하나**. λ 는 `lam_raw` 로 헤드별 학습, init 0.25.
**변수 하나. 귀속이 깨끗하다.**

### Run B (부차, 여력 있으면) — Γ 형태도 되돌린다
```bash
LT_ROOT=/root/LinearTuring ARCH=minimal \
bash sudoku/launch.sh R1B8_min_addr arch.stdp=true arch.stdp_target=addr
```
근거 1 의 이식이 쓴 것은 `Γ = a` 다 (agree 없음). `stdp_target=addr` 가 그 형태이고,
이 경로에서는 `beta`(가소성 창 비대칭)가 **살아난다** — `product` 에서는 `kcb` 가 계산만 되고 안 쓰여
체크포인트의 β 가 정확히 0.0000 이었다 `[검증]`.
변수 둘(λ + Γ)이라 귀속이 덜 깨끗하다. **A 를 먼저 본다.**

### 비교 기준 — 재학습 불필요
`results/curves/R1B8_min_stdp1_eval.csv` 에 stdp1 전 곡선이 있다. 같은 step 에서 비교한다.

---

## 사전 등록 판정

`R1B8_min_stdp1` 기준선 (held-out 2048, loops 16). 속도 **8.00 it/s**, eval 포함 실효 **7.07 it/s** `[검증: 로그]`.

| step | 셀 acc | 완답 | 경과 |
|---|---|---|---|
| 19,530 | 0.6755 | 55 | 0.77h |
| 39,060 | 0.6863 | 59 | 1.53h |
| 48,825 | 0.6899 | 93 | 1.92h |
| 58,590 | 0.6963 | 160 | 2.30h |
| **78,120** | **0.7046** | **317** | **3.07h** |
| 117,180 | 0.7402 | 374 | 4.60h |
| 156,240 | 0.7875 | 692 | 6.14h |
| 236,313 | 0.8195 | 980 | 9.28h |

**완답은 39k 까지 55~59 로 평평하다(계단형).** 초기 판정에는 **셀 acc** 를 쓰고, 완답은 78k 이후에 본다.

- **1차 게이트 @39,060 (~1.5h)**: 셀 acc 가 0.686 을 **밑돌면** 중단하고 원인 확인.
  (완답으로 판정하지 말 것 — 이 구간은 둘 다 ~59 라 안 갈린다.)
- **2차 게이트 @78,120 (~3.1h)**: 셀 acc 0.7046 / 완답 317 과 비교. **여기가 주 판정 지점.**
- **최종 @236,313 (~9.3h)**: 셀 0.8195 / 완답 980 과 비교.

**추가로 반드시 확인할 것 — λ 가 어디로 가는가.** 체크포인트의 `sigmoid(lam_raw)` 를 헤드별로 찍는다.
- 0.25 근처에 머물면 → 근거 1 과 일치. 소수 성분이 맞다.
- 1 쪽으로 올라가면 → 학습은 λ=1 을 원한다. 7일차 선택이 우연히 옳았던 것이고, **이 계획의 전제가 틀린 것.**
- 0 으로 내려가면 → 기억을 아예 안 쓴다. `w` 기제 자체를 재검토.

이 셋은 성능과 **독립적으로** 정보를 준다. 성능이 같아도 λ 의 행선지는 봐야 한다.

---

## 코드 변경 — 최소

**Run A 는 코드 변경이 필요 없다.** yaml 기본값이 이미 λ 학습이고, 명령줄 덮어쓰기를 안 주면 된다.

선택 사항 하나 (안전, 순수 속도 이득):
`core/minimal.py` 의 `step()` 에서 값 사영 einsum 이 **두 번** 돈다 — `agree` 용 `vv` 와 수송용 `v` 가
같은 계산이다 (`addr_dim=0` 이면 `hv0 == hv`, 분할이어도 같은 슬라이스) `[코드]`.
한 번만 계산해 raw 는 수송에, 정규화본은 `agree` 에 쓰면 **비트 동일**하다.
이 einsum 은 `B·T·H·C·d ≈ 7.2e9 MAC` 으로 어텐션 einsum(3.5e8 / 7.0e8)보다 **한 자릿수 크다** — 실측 이득이 있을 것.
비트 동일 확인 후에만 적용할 것. 급하면 건너뛰어도 무방하다.

---

## 하지 않는 것 (이번 런에서)

| | 왜 |
|---|---|
| **권위 가중 (`SPEC.md` 변경 A)** | 2단계. 판별자가 지시할 때만. 지금 넣으면 λ 와 섞여 귀속 불가 |
| **이득 `G > 1` (변경 B)** | 근거 1 이 **이득 1 로** +52 를 냈다. 필요조건이 아니다. 변수 추가일 뿐 |
| **`beta`·`lam_raw` 제거** | `beta` 는 Run B(`addr`)에서 살아난다. `lam_raw` 는 이번 계획의 **주인공**이다 (초판 SPEC 의 "죽은 파라미터 → 제거" 진단은 원인을 거꾸로 봤다 — 덮어쓰기 때문에 죽은 것) |
| **detach 구조 변경** | 사용자 결정: 그대로 둔다 (`UNSUP.md` 5번 보류) |
| **ARC 전환** | `UNSUP.md` 7번. 스도쿠에서 λ 결론이 서야 옮길 게 생긴다 |

---

## 자원

RTX A4500 20GB. stdp1 활성값(`[B,H,81,81]` 계열 약 48개 × 4.81 MB/샘플 × B=128 ≈ **0.6 GB**) + 모델·옵티마이저
(7.16M 파라미터, Adam fp32 상태 ≈ 0.12 GB) → 여유 충분.

- **직렬**: A 를 78k(3.1h) 까지 → 게이트 통과 시 계속, 아니면 중단하고 B.
- **병렬**: A·B 동시. GPU 를 나눠 쓰므로 각 ~3.5 it/s → 78k 를 6.2h. 두 런이 **같은 시점에 판정 지점 도달**.
  총 시간은 직렬과 비슷하고 비교가 쉬워진다.

---

## 판정 후 분기

- **A 가 기준선을 넘는다** → λ 가 원인. `SPEC.md` 를 확정하고 다음은 이득 `G` 또는 ARC(7번).
- **A 가 기준선과 같다** → λ 는 중립. λ 의 행선지를 보고, `w` 기제 자체(`UNSUP.md` 2번 잔여)로 이동.
- **A 가 기준선을 밑돈다** → λ=1 이 옳았다. 근거 1 의 +52 는 **구 아키텍처 특유**이거나
  "관성에 의한 진동 감쇠"(`STDP.md §2.3`: 불일치량은 정착도를 재지 오답을 짚지 않는다)이지
  minimal 에 옮겨지는 것이 아니다. 이 경우 `SPEC.md` 전체를 다시 연다.

---

## 실행 기록 — 2026-08-31 발사

**돌고 있는 런**: `R1B8_min_faith` (pid 는 재확인할 것, PPID=1 로 터미널과 분리됨)

```bash
LT_ROOT=/root/LinearTuring ARCH=minimal bash sudoku/launch.sh R1B8_min_faith \
  arch.stdp=true arch.stdp_target=faithful arch.stdp_eta_init=0.05 arch.stdp_gain_init=1.0
```

| | |
|---|---|
| Γ | `a^β · ⟨v̂_t, v̂_n⟩` — 가소성 창(β) × 동시활동. **`faithful` 은 2026-08-31 신규 구현** |
| 누적 | `w ← (1−δ)w + δ·G·Γ`, 고정점 `G·⟨Γ⟩`. **이득 G 도 신규 구현** |
| 전달 | `(1−λ)a + λw`, λ 헤드별 학습 (init 0.25) — `stdp_lam_fixed` 안 줌 |
| init | δ=0.05 (τ=20), λ=0.25, G=1.0, β~N(0,0.5) |
| 속도 | 7.36~7.46 it/s, ETA ~14.5h (390,625 step 완주 기준) |
| 데이터 | train 1,001,000 (증강 검증됨), test 2,048 (이전과 비트 동일) |

**상태 확인**: `bash 2026-08-31/analysis/status.sh`

### 이 발사 전에 고친 것

1. **데이터 사고** — 분석용으로 `NAUG=0` (증강 없음, 퍼즐 1,000개) 빌드한 데이터로 학습을 두 번 돌렸다.
   train acc 가 3,600 step 에 0.90 까지 갔는데 1,000개를 ~460바퀴 돈 결과였다. 두 런 모두 폐기.
   재발 방지로 `sudoku/launch.sh` 에 발사 전 게이트 추가 — train 예제 10만 미만이면 발사 거부.
2. **중복 연산 제거** (비트 동일 확인: 4타깃 × amp on/off 8조합 전부 `max|Δlogit|=0`)
   - `attn` 을 `addr`(사영) + `attn_xy`(회전·내적) 로 분리 → `faithful` 이 커널 두 개를 쓸 때 사영 공유
   - `v = W h` 를 블록당 1회로 (agree 용 정규화본과 수송용 원본이 공유)
   - 학습 속도 6.74 → 7.46 it/s (+10.7%). **동역학 불변** — 수송은 여전히 정규화 안 된 `v`, agree 는 정규화본.

### 판정 (기준선 `results/curves/R1B8_min_stdp1_eval.csv`)

| step | stdp1 셀 acc | stdp1 완답 |
|---|---|---|
| 9,765 | 0.6404 | 32 |
| **39,060** | **0.6863** | 59 |
| 78,120 | 0.7046 | 317 |
| 236,313 | 0.8195 | 980 |

39,060 이전에는 완답이 계단 이전이라 평평하다 — **셀 acc 로만 판정.**
초반 held-out 은 EMA(`ema_rate=0.999`, τ=1000 step) 지연 때문에 빨리 좋아지는 쪽이 손해다.

**성능과 독립적으로 볼 것**: λ·η(δ)·G 의 행선지.
η 가 0.5 로 붕괴하면 (stdp1 이 그랬다) 긴 기억이 사라져 누적 가설을 못 본다.
G 가 1 위로 올라가면 모델이 실제로 축적을 원하는 것이다.

### 정정 — 넘어야 할 기준선은 `stdp1` 이 아니라 `R1B8_min` (2026-08-31)

초판은 게이트를 `R1B8_min_stdp1` 로만 잡았다. **느슨한 기준이었다.**
기억 없는 `R1B8_min` 과 기억 있는 `stdp1` 을 겹치는 구간에서 대조하면:

| step | minimal (기억 없음) | stdp1 (λ=1) | Δ |
|---|---|---|---|
| 1,953 | **0.4939** / 0 | 0.4776 / 0 | +0.016 |
| 3,906 | **0.5829** / 0 | 0.5430 / 0 | +0.040 |
| 5,859 | **0.6331** / 27 | 0.5930 / 0 | +0.040 |
| 7,812 | **0.6676** / **52** | 0.6240 / 8 | +0.044 |

**겹치는 구간 내내 기억 없는 쪽이 이긴다.** 완답은 7,812 에서 52 vs 8.

그런데 `R1B8_min` 은 **7,812 에서 중단**됐고 그 뒤로 안 돌았다.
→ **이 저장소에 기억 없는 긴 런이 없다. "STDP 기억이 도움이 되는가" 는 한 번도 확인된 적이 없고,
확인 가능한 구간에서는 오히려 손해였다.**

**따라서 판정은 두 단계다.**
1. **@7,812 — `minimal` 을 넘는가** (셀 0.6676, 완답 52). 못 넘으면 기억이 초기 비용을 못 갚는 것.
2. **@39,060 이후 — `stdp1` 을 넘는가** (셀 0.6863 @39k, 0.7046 @78k, 0.8195 @236k).

1번을 못 넘어도 2번은 볼 값어치가 있다 — 기억의 효용이 긴 지평에서 나온다면 초기 손해는 예상된 것이다.
다만 그 경우 **`R1B8_min` 을 길게 돌린 대조군이 반드시 필요하다.** 지금은 없다.

### 참고 — train 배치 곡선 (초반)

| step | B(faithful) | minimal | stdp1 |
|---|---|---|---|
| 800 | 0.4775 | **0.5039** | 0.4610 |
| 1,600 | 0.5485 | **0.5806** | 0.5133 |
| 2,000 | 0.5739 | **0.6038** | 0.5198 |

기억을 넣으면 초반 학습이 느려진다(그래디언트가 EMA 를 거침). 다만 λ 를 풀어
`minimal` 과의 격차가 stdp1 −0.084 → B −0.030 으로 좁혀졌다.

### 체크포인트 정리

`2026-08-31/analysis/prune_ckpt.sh` 가 5분마다 최신 10개만 남긴다 (최대 ~1.4GB).
`setsid nohup` 으로 분리 실행, 학습 종료를 감지하면 자동으로 끝난다. 로그: `results/logs/prune.log`.

### 발견 — `gain_raw` 가 weight decay 제외 목록에 없었다 (2026-08-31, 런 도중)

하네스는 동역학 상수를 wd 에서 제외한다 (`pretrain.py`, LT 패치):
`psi, theta, alpha_raw, gamma_raw, inj_gate, st_gain, eta_raw, lam_raw, beta, mu` + `ndim<=1`.

**내가 추가한 `gain_raw` 가 목록에 없다.** shape 이 `[H,1,1]` 이라 `ndim<=1` 에도 안 걸린다 → `wd=1.0` 을 그대로 맞는다.

| 파라미터 | wd 제외 | @9,765 실측 | wd 만으로 예측 |
|---|---|---|---|
| λ | O | 0.237 | — (실제 학습, 0.25 에서 **하강**) |
| δ | O | 0.054 | — (실제 학습, 0.05 **유지**) |
| **G** | **X** | **0.808** | **0.800** ← 거의 일치 |

→ **G 의 하락은 학습이 아니라 감쇠다. 이 런에서 변경 B(이득)는 테스트되고 있지 않다.**
`(1−1e−4)^t` 로 끌려 softplus(0)=0.693 에 수렴한다.

**조치**: `refs/URM/pretrain.py` 와 `sudoku/urm_patches.diff` 의 제외 목록에 `gain_raw` 추가함 (코드만, 재시작 안 함).
돌던 프로세스는 이미 로드된 코드로 도므로 영향 없다.

**깨어나서 결정할 것**: 이 런을 그대로 둘 것인가(Γ 충실형 + λ 만 유효한 시험), 아니면 재시작할 것인가.
λ·δ 는 유효하게 학습 중이므로 런 자체가 무의미하지는 않다.

**부수 정정**: 앞서 "stdp1 의 η=0.49 는 모델이 빠른 기억을 고른 것" 이라고 적은 것은,
`eta_raw` 가 제외 목록에 있으므로 실제 학습된 값이 맞다. 다만 그 패치가 7일차에도 있었는지는 확인 못 했다.
