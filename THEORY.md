# Model 1 — 이론적 분석

이 문서는 **증명 가능한 것**, **경험적으로만 확인된 것**, **미결인 것**을 분리한다.
각 명제에 `[정리]` / `[실측]` / `[미결]` 표시를 달았다.
`REPORT.md` 가 실험 기록이라면 이 문서는 그 아래에 깔린 구조다.

---

## 0. 기호와 모델의 정확한 정의

상태 `H ∈ R^{T×d}`, 행 `h_t ∈ R^d`. 헤드 `m = 1..M`, `d_h = d/M`, `p = d_h/2`.
위치 `pos_t ∈ Z²`, `Δ_tn = pos_t − pos_n`.

**복소 주소 투영**
```
z_t^(m) = W_C^(m) h_t ∈ C^p,        [A_m; B_m] ∈ R^{d_h×d} 행-직교
ẑ_t^(m) = z_t^(m) / (‖z_t^(m)‖₂ + ε)
q̂_t = e^{+iψ/2} ⊙ ẑ_t,   k̂_t = e^{−iψ/2} ⊙ ẑ_t
```

**커널**
```
U_jj(t←n) = exp(−α_j‖Δ_tn‖₁ − i(θ_j·Δ_tn)),    α_j = softplus(·) > 0
```

**어텐션 계수**
```
a_tn^(m) = Σ_j Re[ conj(q̂_{t,j}) · U_jj(t←n) · k̂_{n,j} ]
         = Σ_j D_j(Δ_tn) · Re[ conj(ẑ_{t,j}) ẑ_{n,j} e^{−i(ψ_j + θ_j·Δ_tn)} ]
```
여기서 `D_j(Δ) = e^{−α_j‖Δ‖₁} ∈ (0,1]`.

**벡터장**
```
f_t = Λ h_t + Σ_m W_OV^(m) Σ_n a_tn^(m) P_m h_n + ( Σ_m d_t^(m) ) b − γ‖h_t‖² h_t
d_t^(m) = Σ_n a_tn^(m)
```
(마지막 4차항은 구현상 Strang 분할의 `Φ` 로 따로 처리되지만 벡터장의 일부다.)

**적분**
```
Φ_τ(h) = h / √(1 + 2γ‖h‖²τ)
h^(r+1) = Φ_{dt/2} ( Φ_{dt/2}(h^(r)) + dt·f(Φ_{dt/2}(h^(r))) )
```

---

## 1. 유계성과 정규화

### 명제 1 `[정리]` — `|a_tn| ≤ 1`

**증명.** `D_j ≤ 1` 이고 `|e^{−iΘ}| = 1` 이므로
```
|a_tn| ≤ Σ_j |ẑ_{t,j}| |ẑ_{n,j}| ≤ ‖ẑ_t‖₂ ‖ẑ_n‖₂ ≤ 1
```
(중간은 Cauchy–Schwarz, 마지막은 `‖ẑ‖ = ‖z‖/(‖z‖+ε) ≤ 1`.) ∎

**의미.** 사양 §1.3 의 주장이 정확히 성립한다. `1/t` 정규화도 `Z_t` 도 필요 없다.
등호는 `ε=0`, `Δ=0`, 두 `ẑ` 의 위상이 정렬될 때만.

### 명제 2 `[정리]` — `a` 는 `h` 에 대해 0차 동차 (ε=0 일 때)

`ẑ(c·h) = ẑ(h)` (`c > 0`) 이므로 `a_tn(c·H) = a_tn(H)`. ∎

**따름정리.** 벡터장의 항별 동차 차수는
```
Λh : 1차     Σ a(h)W_OV P h : 1차     (Σd)b : 0차     −γ‖h‖²h : 3차
```
**어텐션 항은 3차가 아니라 1차 동차의 "방향 의존 게이팅"이다.**
Model 0 에서 `a_tt ∝ ‖z‖²` 였던 자기 증폭 루프가 정규화로 제거됐다.

> **주의.** 사양 §2 의 "재귀 깊이 R 에서 최대 `3^R` 차 다항식" 은 **Model 0 의 성질**이며
> Model 1 에는 적용되지 않는다. 차수가 곱해지지 않으므로 표현력을 그 논거로 변호할 수 없다.

### 명제 3 `[정리]` — 흡수구

`M(τ) = max_t ‖h_t(τ)‖`, `κ₀ = max_t Σ_n e^{−α_min‖Δ_tn‖₁}`, `κ = max_m ‖W_OV^(m)‖₂ · κ₀`.
최댓값 달성 토큰에서 Dini 미분을 취하면 명제 1 로부터
```
Ṁ ≤ ( λ_max(Λ_sym) + κ ) M + κ‖b‖ − γ M³
```
따라서 `M(τ) ≤ max( M(0), R* )`, `R*` 는 `γM³ = (λ_max+κ)M + κ‖b‖` 의 양의 근. ∎

**성립 조건 없음.** 4차 소산이 반드시 이긴다. 사양 §1.7 은 옳다.

**단, 상한은 tight 하지 않다** `[실측]`. 예측 78.4 vs 실측 15.5 (5배).
`κ` 가 `|a_tn| ≤ 1` 을 모든 간선에 동시에 적용한 최악 상한인데, 실제 `a` 는 부호가 섞여 상쇄되기 때문.

---

## 2. γ 는 자유도가 아니다

### 명제 4 `[정리]` — γ 는 스케일 게이지 (b=0, ε=0)

`h = u/√γ` 를 대입하면, 명제 2 에 의해 `a(u/√γ) = a(u)` 이므로
```
(1/√γ) u̇ = Λ(u/√γ) + Σ a(u) W_OV P (u/√γ) − γ‖u/√γ‖²(u/√γ)
          = (1/√γ)[ Λu + Σ a(u) W_OV P u − ‖u‖²u ]
```
양변에 `√γ` 를 곱하면 **γ 가 사라진다**:
```
u̇ = Λu + Σ_m a^(m)(u) W_OV^(m) P_m u − ‖u‖²u
```
∎

**따름정리 4.1.** 끌개 반경 `∝ γ^{−1/2}`.
**따름정리 4.2.** 리아푸노프 스펙트럼은 γ 에 **불변**(선형 변수변환이므로).
**따름정리 4.3.** 흡수구 반경 `√((λ_max+2κ)/γ)` 는 안정성 여유가 아니라 **`h` 의 단위**다.

`[실측]` γ×1600 에서 λ 변화 4%, `‖h‖` 비 40.19 vs √1600=40.0,
궤적 상대오차 0.004 (λ≈1.7 인 계에서 τ=6 이면 실제 차이는 2.7만 배로 증폭됐어야 함).
독립 학습된 7개 모델에서 `‖h‖ ∝ γ^{−1/2}` 가 5% 이내.

### 명제 5 `[정리]` — 게이지를 깨는 항은 정확히 둘

`b ≠ 0` 이면 `(Σd)b` 는 0차 동차이므로 위 대입에서 `√γ(Σd)b` 가 남는다 —
**value bias 의 유효 강도가 `√γ` 로 커진다.**
`ε ≠ 0` 이면 `ẑ` 가 완전한 0차 동차가 아니므로 명제 2 가 근사가 된다.

`[실측]` γ=128 에서 `b` 를 제거하면 정확도 .4610 → .7835.

### 명제 6 `[실측]` — 학습에는 게이지가 아니다

`b` 를 제거해도 γ 만 바꿔 학습하면 λ 가 +1.71 vs +0.60 으로 갈린다.
AdamW 의 파라미터별 스케일 적응, weight decay, 고정된 임베딩 초기화 스케일이
모두 스케일 등변이 아니기 때문. **γ 는 동역학을 그대로 두고 최적화 문제만 바꾼다.**

> **따라서 γ 는 카오스의 인과성을 묻는 도구로 부적절하다.**

---

## 3. 대칭성과 gradient flow

### 명제 7 `[정리]` — `ψ = 0` ⟺ `A` 대칭

`X_tn = Δφ_tn − θ·Δ_tn` 로 두면 `a_tn ∝ cos(X_tn − ψ)`, `a_nt ∝ cos(−X_tn − ψ)`.
따라서 `a_tn = a_nt ⟺ sin X_tn · sin ψ = 0`. `ψ = 0` 이면 모든 입력에 대해 성립. ∎

`[실측]` `psi0` 모델에서 `max|A − Aᵀ| = 5.96e−08` (부동소수점 오차 수준).

### 명제 8 `[정리]` — gradient field 판정

단연결 영역에서 `C¹` 벡터장 `f` 가 gradient field ⟺ 야코비안 `Df` 가 모든 점에서 대칭.

`f(h) = A h` (`A` 상수 대칭) 이면 `Df = A` 대칭 → `f = −∇E`, `E = −½hᵀAh`.
`f(h) = A(h) h` 이면
```
Df = A + (∂A/∂h)·h
```
이고 **둘째 항은 `A` 가 대칭이어도 대칭일 이유가 없다.** ∎

`[실측]` `gradflow` 모델(ψ=0, W_OV=I, Λ=0, b=0)에서
`A` 를 얼린 벡터장의 비대칭도 **0.0000 ± 0.0000**, 실제 벡터장 **0.575 ± 0.337**.

> **사양 §2.4 의 논증은 틀린 게 아니라 다른 대상에 대해 참이다.**
> 고정된 대칭 `A` 에 대해서는 정확히 옳고, `A(h)` 에는 적용되지 않는다.
> 끊어지는 원인은 세 겹: Λ 의 비정규성, `W_OV` 의 공간 불일치, 그리고 **`A` 의 상태 의존성**.
> 앞의 둘을 제거해도 셋째가 남는다.

### 명제 9 `[정정: 실측]` — `A` 고정 계의 비카오스성 (증명에 결함 발견)

**원래 증명의 결함.** 초판은 `ḣ = Mh − γ‖h‖²h` 로 놓고 전체 상태의 극분해
(`ω̇ = (I−ωωᵀ)Mω` 가 자율)를 썼다. 그러나 **실제 4차 감쇠는 전역 노름이 아니라 토큰별**
(`Φ` 가 `‖h_t‖²` 로 토큰마다 포화)이므로 감쇠 벡터가 전체 상태와 평행하지 않고,
각도 방정식이 자율계가 되지 않는다. **증명은 감쇠가 전역 노름일 때만 유효하다.**

**A 고정 계의 올바른 식별.** 토큰별로 (선형부 Λ + 등방 3차 포화)를 갖고
부호 있는 고정 그래프로 선형 결합된 계 — 이것은 문자 그대로
**결합 Stuart–Landau(진폭 방정식) 네트워크**다. Stuart–Landau 네트워크는 일반적으로
카오스가 **가능**하다(진폭 카오스는 CGL 계열의 표준 현상). 따라서:

- A 고정 ⟹ 비카오스는 **정리가 아니라 실측**이다. `frozenA` λ = −0.16, `frozenA8` λ = −0.02.
- **A 고정 모델은 "선형"이 아니다** — 선형 혼합 + 토큰별 반경 포화(소프트 정규화)다.
  `Φ_τ(h) → h/(√(2γτ)‖h‖)` (큰 ‖h‖) 는 LayerNorm 류 정규화 비선형성과 같은 부류다.
  진짜 선형 대조군은 `A 고정 + γ→0` 이며 이는 별도 실험이 필요하다(§6.7).

**얻은 것.** 대화의 맨 첫 결론 — "Ginzburg-Landau / 결합 비선형 진동자의 텐서 일반화" — 이
정확히 **A 고정 극한의 이름**이었다.
- A 고정 = 고정 그래프 위의 Stuart–Landau 네트워크
- A 자유 = **결합 그래프가 매 순간 진동자 상태로부터 다시 그려지는** Stuart–Landau 네트워크
  (문헌의 adaptive / co-evolving oscillator network 부류 — 상태 의존 결합이
  카오스·키메라를 만드는 것으로 알려진 영역)

---

## 4. `noov` 구조의 정확한 성질

`noov` = `W_OV^(m)` 를 블록 배치 항등으로, `Λ` 를 대각으로 고정한 변종.

### 명제 10 `[정리]` — 확산/반응 분해가 정확히 성립

임의의 행렬에 대해 다음은 **대수 항등식**이다:
```
Σ_n a_tn h_n = Σ_n a_tn (h_n − h_t) + ( Σ_n a_tn ) h_t
               └──── "확산" ────┘      └── "반응" ──┘
```
이것이 **상태 공간에서** 성립하려면 `Σ_m W_OV^(m) P_m = I` 가 필요하다.
`noov` 는 `W_OV^(m)` 가 블록 `m` 을 제자리에 되돌리는 항등이므로 정확히 성립한다(블록 단위).
`full` 은 `W_OV^(m) ∈ R^{d×d_h}` 가 학습되므로 성립하지 않는다. ∎

> **분해 자체는 물리가 아니라 대수다.** 어떤 선형 연산자든 "비대각 차분 + 행합×자기" 로 쪼개진다.
> 확산이라는 **내용**은 `a ≥ 0` 일 때만 붙는다.

### 명제 11 `[정리]` — 디리클레 에너지 항등식

`E = Σ_t ‖h_t − h̄‖²`. `Σ_t (h_t − h̄) = 0` 이므로 `h̄` 의 시간미분 항이 소거되어
```
dE/dt = 2 Σ_t ⟨ h_t − h̄, ḣ_t ⟩
```
`a` 가 대칭일 때 확산항의 기여는
```
2 Σ_t ⟨ h_t − h̄, Σ_n a_tn(h_n − h_t) ⟩ = − Σ_{t,n} a_tn ‖h_t − h_n‖²
```
**증명.** `Σ_{t,n} a_tn(⟨h_t,h_n⟩ − ‖h_t‖²)` 를 전개하고 `a` 의 대칭성으로
`−½Σ a_tn‖h_t−h_n‖²` 와 같음을 확인. `h̄` 항은 `Σ_t Σ_n a_tn(h_n − h_t) = 0`(대칭성)에서 소거. ∎

**따름정리 11.1.** `a_tn ≥ 0` 이면 `dE/dt ≤ 0` — **음이 아닌 확산은 절대 에너지를 늘릴 수 없다.**
**따름정리 11.2.** `a_tn < 0` 인 간선은 `+|a_tn|‖h_t−h_n‖²` 를 주입한다.
주입량이 **이미 벌어진 거리의 제곱에 비례**하므로 양성 되먹임이다.

`[실측]` `noov` 끌개에서 확산항 기여 **+15,269**(최대 주입원, 39%).
간선 부호별: 양성 −25,214 / 음성 **+40,731**.
음성 간선의 평균 `‖h_t−h_n‖² = 40.5`, 양성 26.6 —
**학습이 척력을 이미 가장 다른 패치 쌍 위에 배치한다**(측면 억제).

> **튜링 메커니즘과의 차이**: 튜링계에서 확산은 언제나 소산항이고, 패턴은 두 종의
> **확산 속도 차이**가 반응의 균일 정상상태를 불안정하게 만들어 생긴다.
> 여기서는 확산 자리의 항이 스스로 주입원이다. **부호가 아니라 역할이 뒤바뀐 구조.**

### 명제 12 `[정리]` — `noov` 최소형의 닫힌 해 (`A` 고정 시)

단일 헤드, `W_OV = I`, `Λ` 대각, `b = 0`, 4차항 제외, `A` 고정이면
```
Ḣ = A H + H Λ    (Sylvester 형)  ⟹  H(τ) = exp(τA) · H(0) · exp(τΛ)
```
`A ⊗ I` 와 `I ⊗ Λ` 가 교환하므로 지수가 분리된다.
**토큰 축과 채널 축이 완전히 분리된다.** ∎

다중 헤드나 일반 `W_OV` 면 항들이 교환하지 않아 이 분해가 깨진다.

> 이것이 "재귀를 행렬 거듭제곱으로 접기"(피보나치식)가 가능한 유일한 경우다.
> `A` 가 `h` 에 의존하면 원리적으로 불가능하다.

### 명제 13 `[정리 + 주의]` — 부호 그래프와 균형

부호가 섞인 대칭 `A` 에 대해 `L̄ = D̄ − A`, `D̄ = diag(Σ_n|a_tn|)` 는 **PSD** 이고,
`λ_min(L̄) = 0 ⟺ 그래프가 균형(balanced)` — 즉 노드를 두 집단으로 나눠
양성 간선은 집단 내부, 음성 간선은 집단 사이에 오게 할 수 있다 (구조적 균형 이론, Harary).
그때 `ḣ = −L̄h` 의 정지 상태는 균일장이 아니라 **±1 이분 분할**이다.

**주의 (중요).** 우리 모델의 확산 연산자는 `A − D`, `D = diag(Σ_n a_tn)` 이며
이 `D` 는 **부호합**이라 음수일 수 있고 `L̄` 와 다르다. 따라서
**균형 측정은 그래프의 성질을 특성화하는 것이지 우리 연산자의 정지 상태를 증명하는 것이 아니다.**
"에너지 증가 = 분할로의 수렴" 이라는 서술은 유비이며, 엄밀한 대응은 미결(§6.4).

`[실측]` 정규화 부호 라플라시안 기준 `noov` 좌절도 0.132 (무작위 부호 0.636, 4.8배 낮음),
참여비 34.8/49(전역 분할), 스펙트럼 간극 11배. `full` 은 0.723 으로 무작위보다 높고 간극 없음.
분할의 최대 파수 1.00 이 상태장 스펙트럼 `k* = 1.12` 와 같은 척도.

---

## 4.5 함수 클래스와 정칙성

### 명제 17 `[정리]` — 정칙성이 깨지는 곳은 정확히 하나

`Φ_τ(h) = h/√(1+2γ‖h‖²τ)` 는 **모든 곳에서 실해석적**이다. 근호의 인자가 `≥ 1 > 0` 이고
`‖h‖²` 이 다항식이므로. (사양 §4 가 "근호를 넣는 순간 국소 해석성으로 약화된다" 고 걱정한 것은
**이 항이 아니다** — 여기는 문제가 없다.)

깨지는 것은 `ẑ` 정규화다. `‖z‖₂ = √(Σ_j|z_j|²)` 는 `z = 0` 에서 해석적이지 않다.
`ε > 0` 이라 분모는 0 이 되지 않지만 `‖z‖` 자체의 비평활성이 남는다:
```
ẑ(z) = z/ε · (1 − ‖z‖/ε + …)
```
`z‖z‖` 항은 1차원의 `|x|x` 와 같아 **C¹ 이지만 C² 는 아니다.**

`W_C^(m)` 가 행-직교이므로 `ker(W_C^(m))` 은 `d − d_h = 48` 차원, 즉 예외집합
`{h_t : W_C^(m) h_t = 0}` 은 **여차원 `d_h = 16`**. 토큰·헤드에 대한 합집합도 측도 0.

> **모델은 조밀 열린집합(전측도)에서 `C^ω`, 여차원 16 의 예외집합에서 `C¹` 이되 `C²` 는 아니다.**
> 카오스계라 궤적이 그 집합에 임의로 접근할 수 있으나 도달 확률은 0.

### 명제 18 `[정리]` — 어텐션은 대수함수이지 유리함수가 아니다

```
a_tn = P₂(h_t, h_n) / [ (√Q(h_t) + ε)(√Q(h_n) + ε) ],    Q(h) = ‖W_C h‖²
```
`P₂` 는 쌍선형(각 인자에 1차), `Q` 는 양정부호 2차형식.
따라서 `a` 는 **각 토큰에 `√Q(h_t)` 를 첨가한 대수함수체의 원소**다. ∎

**따름정리.** 벡터장의 항별 형태:

| 항 | 형태 | 동차 차수 |
|---|---|---|
| `Λh` | 다항식 | 1 |
| `Σ a(h) W_OV P h` | 3차 분자 / 근호 분모 | **1** |
| `(Σd) b` | 2차 분자 / 근호 분모 | **0** |
| `−γ‖h‖²h` | 다항식 | 3 |

### 명제 19 `[정리]` — 복잡도는 대수함수 차수로 지수 성장

대수함수 `f`(차수 `d₁`), `g`(차수 `d₂`) 의 합성은 차수 `≤ d₁d₂`.
따라서 R회 합성한 전체 사상은 차수 `≤ c^R` 의 대수함수다.

> **다항식 차수 `3^R` 은 없지만 대수적 복잡도는 여전히 R 에 지수적이다.**
> 단 이것은 **상한**이며, 특정 목표 함수에 대한 하한을 주지 않는다(§6.2).

### 명제 20 `[정리]` — `b` 와 4차항을 빼면 양의 동차 1차

명제 18 의 표에서 `b` 항(0차)과 4차항을 제거하면 `f(c·h) = c·f(h)` (`c > 0`).

> **이것은 편향 없는 ReLU 망의 성질과 정확히 같다.**
> 사양이 대비시킨 구도 — "ReLU 는 공간을 다면체로 쪼개고 이 모델은 대수 다양체로 쪼갠다" — 는
> 진폭 정규화를 넣은 순간 절반이 무너진다. **두 계열 모두 원뿔(cone) 구조**이고,
> 결정 경계가 원점에서 뻗는 방향으로 결정된다. 차이는 그 분할이
> **조각별 선형이냐 매끄러운 대수 곡면이냐** 뿐이다.
>
> 그리고 `b` 는 **동차성을 깨는 유일한 항**이다. 사양은 이를 "기함수 대칭성의 1줄 해법" 으로
> 도입했으나, 더 근본적으로는 **ReLU 망에서 bias 가 하는 역할과 동일**하다.

### 요약

> **함수 클래스**: R회 합성된, 여차원 16 집합 밖에서 실해석적인,
> 양의 동차 1차 게이팅 벡터장의 흐름 + 반경 포화 + 동차성을 깨는 편향.

---

## 5. 이산화

### 명제 14 `[정리]` — 구현된 스킴은 Strang 2차가 아니다

`S(dt) = Φ_{dt/2} ∘ Ψ_dt ∘ Φ_{dt/2}` 가 2차가 되려면 중간 `Ψ` 가 정확한 흐름이거나
대칭(자기수반) 방법이어야 한다. 구현의 중간 스텝은 **전진 오일러**로 대칭이 아니므로
전체 스킴은 **1차**다. 사양의 "Strang 분할" 은 구조는 맞으나 차수 주장은 성립하지 않는다.

**따름정리.** 반응항(4차)의 해석적 해가 주는 이득은 **가짜 폭발 차단**이지 정확도 차수 향상이 아니다.

### 명제 15 `[정리]` — 오일러 안정 조건

중간 스텝 `h + dt·f` 가 안정하려면 대략 `dt · ‖M‖₂ < 2`, 즉 `R > κ` 정도가 필요하다.

`[실측]` `κ ≈ 129 ≫ R = 8` 로 16배 위반인데도 발산하지 않는다.
`κ` 가 명제 3 과 같은 최악 상한이고 실제 `a` 의 부호 상쇄로 유효 노름이 훨씬 작기 때문.
**즉 `R > κ` 는 충분조건이지 필요조건이 아니다.**

### 명제 16 `[실측]` — 정성적 동역학은 `dt` 에 무관, 학습된 함수는 종속

`dt` 를 1/8 → 1/128 로 16배 줄여도 `‖h‖`(15.5 로 수렴)와 상대속도(~3)와 λ 가 유지된다 —
**연속 흐름이 잘 정의돼 있다.**
반면 τ=1 을 고정한 채 `dt` 만 줄이면 정확도가 .972 → .286 으로 무너진다.
**그러나 이것은 구조가 아니라 학습의 문제다** — 매 배치 `dt` 를 무작위화하면
R=96 정확도가 .286 → .947 이 되고 정확도 비용은 0.3%p.

---

## 6. 미결 명제

명시적으로 증명되지 않은 것들. 각각에 **무엇으로 해결되는지**를 적었다.

### 6.1 `[미결]` 자유-`A` 계의 카오스

`λ > 0` 은 유한시간 리아푸노프 지수의 실측(τ_span=12, 재정규화 0.5, `dt` 16배 변화에 강건)이지
**증명이 아니다.** 진짜 카오스임을 보이려면 Smale 말굽이나 그에 준하는 구조를 찾아야 한다.

- **반증 가능성**: `τ_span` 을 더 늘렸을 때 λ 가 0 으로 수렴하면 준주기였던 것.
- **해결 방법**: (i) 훨씬 긴 적분에서 λ 안정성 확인, (ii) 푸앵카레 단면에서 말굽 구조 탐색,
  (iii) 저차원 축소(단일 헤드, 작은 `T`, 작은 `d`)에서 분기 해석.

### 6.2 `[미결]` `A` 고정 계의 표현력 상한

`A` 고정이면 출력은 `y = W_cls · P_R(M(x)) · h₀(x)` 이고 `M` 은 입력의 유계 차수 유리함수다.
따라서 `x ↦ y` 는 **유계 복잡도**이지만, 특정 함수족(예: `k` 라운드 CA)에 대한
**하한은 증명하지 않았다.** 지금 있는 것은 상한 비교와 실측뿐이다.

- **해결 방법**: `A` 고정 클래스가 `k`-라운드 국소 갱신을 표현하려면 필요한 폭의 하한을
  회로복잡도(예: 유계 깊이 회로에서 parity 의 게이트 수 하한)로 유도.

### 6.3 `[미결]` `b ≠ 0` 인 `A` 고정 계의 비카오스성

명제 9 는 `b = 0` 에서만 증명된다. `ḣ = Mh + c − γ‖h‖²h` 는 4차항이 평행이동 불변이 아니라
극분해가 깨진다.

- **해결 방법**: `c` 를 섭동으로 두고 `‖c‖` 가 작을 때 구조 안정성 논증, 또는 수치 연속법.
- 실측은 `b ≠ 0` 에서도 λ ≈ −0.16 으로 비카오스.

### 6.4 `[미결]` 부호 연산자와 균형의 엄밀한 대응

명제 13 의 주의 참조. 우리 연산자는 `A − diag(Σa)` 이고 균형 이론은 `diag(Σ|a|) − A` 를 쓴다.

- **해결 방법**: `A − diag(Σa)` 의 스펙트럼을 직접 분석하거나,
  두 연산자의 정지 상태가 실제로 가까운지 수치로 확인.

### 6.5 `[미결]` `ε` 의 정량적 영향

명제 2·4 는 `ε = 0` 에서 정확하다. `ε = 10⁻⁴`, `‖z‖ ≈ 10` 이면 상대 섭동 `10⁻⁵` 이지만
카오스계에서는 증폭될 수 있다.

- **해결 방법**: `ε` 를 쓸면서 λ 와 정확도를 재는 것. (사양도 §3 경고 (b) 에서 `ε` 가 너무 크면
  `ẑ` 가 과도하게 눌린다고 지적했다.)

### 6.6 `[미결]` 상태 의존성이 사주는 순차 라운드 수

`[실측]` CA 에서 `A` 고정은 `k ≤ 2~3` 까지, 자유는 `k ≤ 4` 까지.
그러나 **용량 통제가 끝나지 않았다** — 우리 모델은 25K 파라미터이고 기준선은 150~345K 다.
전이가 날카로우므로 격차의 크기가 문턱과의 거리를 말해주지 않는다.

- **해결됨**: 폭 문턱 확인 (Rule 90 k=7: d≤192 전부 0.0000 → d=256 에서 1.0000).
  그리고 **같은 폭에서 A 고정은 두 과제 모두 0.000** (Rule 90 k=7, Rule 110 k=8) —
  깊이 ×4 에도 무반응. 라운드 분리는 용량·깊이 통제 하에서 생존했다.
  §6.2 의 표현력 하한은 여전히 미증명이나, 실측상 A 고정 클래스는
  폭 395K·깊이 τ=4 범위에서 8라운드 과제를 풀지 못한다.
- **두 번째 교란**: CNN/GRU 는 CA 와 일치하는 국소·순차 귀납 편향을 갖는다.
  폭을 맞춰도 "활성화 없음"과 "귀납 편향 불일치" 는 분리되지 않는다.
  커널을 국소로 제약한 변종이 필요하다.

### 6.7 `[미결]` 진짜 선형 대조군

A 고정 모델도 토큰별 반경 포화라는 정규화 비선형성을 갖는다.
`A 고정 + γ→0` 이면 재귀가 `h₀` 에 정확히 아핀이 된다.
- MNIST 에서 여전히 ~97% → 반경 포화 불필요, 비선형성은 전부 `x↦A` 사상에.
- ~92%(로지스틱 회귀 수준)로 하락 → 정규화 비선형성이 실질 기여.

### 6.8 `[미결]` k* 의 기원 — 초기화 유산으로 판명

미학습 모델(시드 3개)이 학습된 모델과 동일한 `k* = 1.12` 를 내고, θ×2 에서 1.88 로 이동한다.
**"튜링 특징 파장이 선택돼 있다"는 §6.3 계열 주장은
"θ 초기분포가 정한 파장이 학습 후에도 유지된다" 로 하향한다.**
남는 질문: 학습이 파장을 옮길 유인이 없어서인가(kern 처럼 커널 lr 을 올리면 옮기는가).

---

## 7. 요약표

| # | 명제 | 상태 | 근거 |
|---|---|---|---|
| 1 | `\|a_tn\| ≤ 1` | **정리** | Cauchy–Schwarz |
| 2 | `a` 는 0차 동차 (ε=0) | **정리** | `ẑ` 의 정의 |
| 2′ | `3^R` 차수 주장은 Model 1 에 무효 | **정리** | 명제 2 |
| 3 | 흡수구 존재 (조건 없음) | **정리** | Dini 미분 + 명제 1 |
| 3′ | 상한이 5배 헐거움 | 실측 | 78.4 vs 15.5 |
| 4 | γ 는 스케일 게이지 (b=0,ε=0) | **정리** | 명제 2 + 대입 |
| 5 | 게이지를 깨는 항은 `b`, `ε` 뿐 | **정리** | 동차 차수 |
| 6 | 학습에는 게이지가 아님 | 실측 | λ +1.71 vs +0.60 |
| 7 | `ψ=0` ⟺ `A` 대칭 | **정리** | `cos(X∓ψ)` |
| 8 | gradient flow ⟺ `Df` 대칭; `A(h)` 가 깸 | **정리** | 야코비안 |
| 9 | `A` 고정 ⟹ 비카오스 | **실측** (증명 결함: 감쇠가 토큰별) | Stuart–Landau 네트워크로 식별 |
| 10 | 분해는 `ΣW_OV P = I` 일 때만 상태 공간에서 성립 | **정리** | 대수 |
| 11 | 음이 아닌 확산은 `E` 를 못 늘림 | **정리** | 에너지 항등식 |
| 12 | `noov` 최소형 닫힌 해 `exp(τA)H exp(τΛ)` | **정리** | 교환성 |
| 13 | 부호 그래프 균형 ⟹ 이분 정지 상태 | 정리(단, 연산자 대응은 미결) | Harary |
| 14 | 구현 스킴은 1차 (Strang 2차 아님) | **정리** | 전진 오일러의 비대칭성 |
| 15 | `R > κ` 는 충분조건일 뿐 | 정리 + 실측 | κ=129, R=8 안정 |
| 16 | 정성적 동역학은 `dt` 무관 | 실측 | dt 16배 쓸기 |
| 17 | `C^ω` on 전측도 집합, 여차원 16 에서 `C¹`\`C²` | **정리** | `‖z‖` 의 비평활성 |
| 18 | 어텐션은 대수함수 (유리함수 아님) | **정리** | `√Q` 첨가 |
| 19 | 대수적 복잡도가 `c^R` 로 지수 성장 (상한) | **정리** | 합성의 차수 곱 |
| 20 | `b`·4차항 제외 시 양의 동차 1차 (= bias 없는 ReLU 망) | **정리** | 명제 18 의 차수표 |
| — | 자유-`A` 계가 진짜 카오스 | **미결** | FTLE 실측만 |
| — | `A` 고정 클래스의 표현력 하한 | **미결** | 상한 비교만 |
| — | 순차 라운드 분리의 용량 통제 | 해결 | d=256 문턱 (§6.6) |
| 21 | noov 채널 비혼합 ⟹ d종 RD·조건부 선형 | **정리(검사)** | 재생 오차 정확히 0 |
| 22 | 균일 상태 닫힌 심볼; ψ=0 ⟺ Im Â≡0 | **정리** | psi0 실측 0.0e+0 |
| 23 | 진동형 튜링 불안정 — RD 재분류 | **실측** | 균일 안정/띠 불안정, 6측정 |
| 24 | 시간 경계값 문제 (고정점 아님) | **실측** | 전 설정 ρ>1·장 미정착·commitment |
| 25 | 계산은 라우팅(장)에 있다 | **실측(개입)** | 장 이식 0.60/0.85 vs 0.06/0.52 |
| 26 | sheaf (W,Wᵀ): 학습된 종·에너지 귀속 보존 | **실측** | 0.073→0.503, W_O 와 보완재 |

---

## 8. 구조에 대한 결론

**이 모델은 무엇인가.** 부호형·비국소·준선형 반응-확산계다 — 균일모드 안정·유한 파수
진동 띠가 실측된다(명제 23). 유한격자 서술로는 **부호 그래프 위의 흐름 + 4차 포화**.

- "확산" 항은 계수의 절반이 음수라 **최대 에너지 주입원**이고, 유일한 소산원은 4차항이다.
- `W_OV` 는 sheaf 복원 사상 자리인데 `δᵀδ` 가 아니라 **`δ` 만** 있어 PSD 가 아니다.
  그래서 `W_OV` 통과 전후로 에너지 역할의 **부호가 뒤집힌다**(`full`: −7,049 → +32,633).
  `(W,Wᵀ)` 로 바꾸면 간선별 에너지 부호가 sign(a) 로 고정된다 — 단 소산 *보장*은 여전히 없다
  (a 가 부호형; "보장" 서술은 a≥0 암묵 가정의 오류였다). **2일차 실측 완료 → 채택 (명제 26).**
- `noov` 는 복원 사상이 항등이라 표준 그래프 확산에 해당하고, 분해가 정확하며,
  부호 그래프로서 균형에 가깝다(좌절도 0.132). **이론적으로 다룰 수 있는 유일한 변종.**

**비선형성은 어디에 있는가.** 두 곳이고 성질이 다르다.
1. `x ↦ A` — 임베딩의 유리식. **1회** 계산. 이것만으로 MNIST 97% 가 나온다.
2. `A(h_r)` 의 재계산 — **R회** 합성. 명제 8·9 가 말하듯 이것이 gradient 구조를 깨고 카오스를 만든다.
   MNIST 비용 0.16%p, CA 에서는 순차 라운드 1~2 개 값어치(용량 통제 미완).

**사양의 이론이 참인 영역.** 명제 8·9 로 정확히 규정된다 — **`A` 를 상수로 볼 때.**
사양은 선형 어텐션을 "주어진 그래프 위의 확산" 으로 읽는 관습을 가져왔고,
그 관습은 `A` 가 매 스텝 자기 상태로부터 다시 그려지는 순간 무효가 된다.

---

## 9. 2일차 명제 (08-22) — 장 · 스펙트럼 · sheaf

### 명제 21 `[정리(검사)]` — noov 채널 비혼합과 조건부 선형성
noov(W_OV=블록항등, Λ 대각)에서 채널 c 는 이웃의 채널 c 만 받고(슬라이스 항등 수송),
Λ·Φ·b 는 채널별 스칼라/고정벡터. 채널 간 상호작용은 공유 스칼라 장 `{a^(m), d_t, ‖h‖}`
경유가 전부다. 따름: (i) noov = 공유 부호 전도도의 d종 반응-확산계;
(ii) 장을 고정하면 동역학은 채널별 선형·아핀 — 실증: 기록-재생 상대오차 정확히 0 (`fields.py`).

### 명제 22 `[정리]` — 균일 상태의 닫힌 심볼
균일 상태에서 `a(Δ) = Σ_j w_j e^{−α_j‖Δ‖₁} cos(ψ_j+θ_j·Δ)`, `w_j=|ẑ_j|²`.
무한격자 심볼(`S_α(q)=sinh α/(cosh α−cos q)`):
`Re Â(k) = Σ w_j·½cos ψ_j[S(θ−k)+S(θ+k)]`, `Im Â(k) = Σ w_j·½sin ψ_j[S(θ−k)−S(θ+k)]`.
**ψ=0 ⟺ Im Â≡0** (명제 7 의 스펙트럼 형태; psi0 실측 0.0e+0). 띠 위치 |k*|≈|θ|.
유효조건 `1/α ≪ 격자` — 아니면 유한격자 직접 대각화.

### 명제 23 `[실측]` — 진동형 튜링 불안정, RD 재분류
균일 고정점 기준: 균일모드 안정/한계, 유한 k 띠 불안정(진동형, |Im|≈7.5) — 6개 측정 전부.
학습의 역할: 띠 창조가 아니라(미학습도 보유) 진동화(|Im| 0.12→7.5)·협대역화(339→70)·
(full)균일 안정화(+1.24→−0.72). §8 의 이전 판정을 정정: **부호형·비국소·준선형(A=A(h)) RD**
— Swift–Hohenberg/신경장 지파. 적용 한계: 궤적이 균일 근방을 지날 때만 (CA 무효, 0/2048).

### 명제 24 `[실측]` — 학습은 시간 경계값 문제를 푼다
고정점 의미론이 없다: 학습 후 전 설정 블록 ρ>1 (유일 예외 frozenA8 0.75 — 순차계산 불능 판),
장 미정착(Δa/‖a‖≈0.4 유지), r>R 붕괴, commitment r≈R−1..R 집중.
학습이 만드는 것은 "r=R 에 읽히는 시간 맞춤 궤적"이다.
따름: DEQ/IFT 불가(현 좌표); randR 이 시간 외삽을 사는 유일한 방법인 이유.
주의: λ(FTLE)와 ρ 는 순위가 다르다(full 이 λ 최저·ρ 최고) — DEQ 판정은 ρ 로.

### 명제 25 `[실측(개입)]` — 계산은 라우팅에 있다
장 이식(샘플 i 의 장 × 샘플 j 의 초기상태): 예측이 장 공여자를 따른다
(MNIST noov 0.60 vs 0.06 · full 0.59 vs 0.07 · CA 0.85 vs 0.52).
§10 의 "비선형성은 x→A" 가 개입 수준 인과로 승급. 정확한 분해 `logits = W·L_장[h₀] + c_장`
에서 CA 의 c 항(노름 9%)만으로 셀 0.766 — `d_t·b` 경로가 실질 운반자의 하나.

### 명제 26 `[실측]` — sheaf 값 경로 (W,Wᵀ)
수송 `WᵀW`(대칭 PSD) = "학습된 종": 헤드별 고유기저에서 종별 확산(μ_i≥0),
혼합 = 헤드 간 기저 불일치. noov ⊂ sheaf (W=P 가 초기화). 간선별 에너지 부호 = sign(a_tn)
이 혼합 하에 보존; 조건부(장 고정) 동역학은 자기수반 — 채널 회전 없음.
CA: 0.073→0.392(Λdiag)→0.503(Λfull) vs full 0.666. 경계 W_O 와 **보완재**(2×2: 3.0×/5.1×).
W_O 없이는 sheaf ≥ full (0.166 vs 0.131) — 비대칭 수송의 우위는 경계 수축 하에서만 실현.
