# ARC 계획 — v0 (과제 임베딩 gradient) → v1 (문맥 내 델타 규칙)   (2026-08-29, 설계)

> 전제: 스도쿠에서 확인된 것 = 코어가 입력·상태로부터 제약 그래프를 조립한다(step-8 `a` 가 손 그래프와 동등), 결합 기억 `w` 가 라벨 없이 작동한다(원판 +52).
> ARC 가 스도쿠와 다른 점 = (i) 과제마다 규칙이 다르다 (ii) 규칙은 완성이 아니라 **변환**(입력 격자 → 다른 출력 격자)이다 (iii) 규칙은 데모 쌍 몇 개에서 와야 한다.
> 이 문서는 (i)(ii)를 URM 프로토콜 위에서 먼저 처리하고(v0), (iii)를 문맥 내 빠른 가중치로 바꾸는(v1) 순서다.

## 0. 데이터·하네스 사실 (refs/URM, 2026-08-29 확인)

- `data/build_arc_dataset.py`: 격자 하나 = 30×30 = **900 토큰**, vocab 12 (0 PAD, 1 EOS, 2..11 = 색 0..9). 출력 크기는 격자 밖 첫 행·열을 EOS 로 채워 **시퀀스 안에 인코딩** → 크기 예측이 따로 필요 없다(같은 T 안에서 EOS 위치를 맞추면 됨).
- 증강: 이면체 8 × 색 치환(0 고정) — `aug(name)`; 학습 split 은 무작위 평행이동 패딩 추가. **과제 id = (과제, 증강) 쌍마다 하나** → `num_puzzle_identifiers` 수천.
- 평가: 예측을 역증강·crop 해 해시, 같은 시험 입력에 대한 예측들을 q 로 가중 투표 → pass@2. (`evaluators/arc.py`)
- 데이터 원본: `kaggle/combined/arc-agi_{training,evaluation,evaluation2}-{challenges,solutions}.json`. HRM/URM 프로토콜은 **평가 과제의 데모 쌍을 학습에 포함**(시험 출력은 제외) → 과제 임베딩이 데모에서 학습된다 = gradient 로 하는 few-shot.
- 코어 속도(HANDOVER §5): d256·H8·8스텝·배치 8·fwd+bwd 에서 T=900 이 141 ms / 4.05 GB. 128 스텝이면 배치 8 당 ≈ 2.3 s. **d=1248 판은 ARC 에 못 쓴다** → v0 는 d 256~512.
- ARC-2 eval 실측(HANDOVER §5): 120 과제, 시험 입력 172, 입출력 같은 크기 69%, 시험에 새 색 등장 71/172.

## 1. v0 — 과제 임베딩을 gradient 로, 단 **수송과 커널에** 넣는다

URM 그대로: 과제 id 별 벡터 `e ∈ R^k` (k=128, `CastedSparseEmbedding` + SignSGD 별도 옵티마이저, 데모로 학습). 바뀌는 것은 `e` 가 들어가는 자리다. URM 은 `e` 를 입력에 더한다. 우리 코어에서 규칙이 사는 자리는 셋이고, 셋에 `e` 를 조건으로 건다.

```
(A) 수송 — 과제 조건부 제한 사상
    v_t = W_h h_t^{val}                                   (헤드별 stalk, c 차원)
    메시지 = Σ_n a_tn · W_hᵀ diag(1 + tanh(U_h e)) v_n     U_h ∈ R^{c×k}
    → 과제가 "어느 채널을 얼마나 실어 나르는가" 를 정한다 (색 치환·복사/삭제 류)

(B) 커널 — 과제 조건부 변위와 방향
    θ_j ← θ_j + V_j e            (채널별 2D 파수 이동: "오른쪽으로 1", "대각선" 류의 위치 규칙)
    ψ_j ← P_j e                  (채널별 위상차 = 비대칭 = **방향**)
    → ψ=0(2단계) 은 스도쿠의 대칭 제약용 결정이었다. ARC 의 이동·복사는 방향이 있으므로 ψ 를 과제 조건부로 되살린다.
      a_tn 은 ψ=0 이면 Δ 에 짝함수라 "왼쪽에서 오른쪽으로" 를 표현할 수 없다 (명제 7).

(C) 경계 — 선택
    쌍선형 앞에 FiLM(e): h ← h + W_d[(W_g h ⊙ (1+tanh(Q e)))/2 ⊙ (W_u h)]. v0 에서는 끈다(변수 최소화).
```
그 외는 스도쿠 3단계 그대로: minimal 구조, 주소/값 분할, 활성화 0개, 블록마다 주입(입력 격자), 세그먼트별 CE(출력 격자 900 토큰, EOS 포함), detach. `w`(STDP) 는 v0 에서 끈다 — 변수 하나씩.

**왜 입력 덧셈이 아니라 수송·커널인가.** 입력에 더한 `e` 는 값 블록의 초기 조건일 뿐이고 그래프·수송은 그대로다. 규칙이 "무엇을 어디로" 라면 그것은 정확히 `WᵀW`(무엇을) 와 `θ, ψ`(어디로) 의 자리다. 스도쿠에서 배운 사실 — 그래프는 첫 세그먼트에 조립되고 그 뒤 고정 — 은 ARC 에서 "과제가 그래프를 정한다" 로 읽히고, 그 조립을 `e` 가 직접 하게 한다. v1 에서 `e` 를 문맥으로 쓸 때 같은 자리에 꽂을 수 있다는 것도 이유다.

**규모.** d=384, H=8 (p=24), d_a=128 / d_v=256, T=900, 배치 16, 세그 8 × 블록 8 (= 64 스텝; 스도쿠 128 의 절반, 속도 때문). 파라미터 ≈ 경계 2.5M + 코어 0.5M + 임베딩(과제 수 × 128).

## 2. v0 판정 절차

| 단계 | 내용 | 통과 기준 |
|---|---|---|
| M0 데이터 | `build_arc_dataset.py` 로 ARC-1 training(400) + evaluation 데모 빌드(URM 기본, num_aug 1000 → 시간 보고 300 으로) | dataset.json: seq 900, vocab 12, id 수 |
| M1 학습 가능성 | v0 를 training 과제로 학습. **train exact(증강 포함)** 곡선 | 스도쿠 때 기준과 같은 형태: 정체 후 재가속이 있는가. 없으면 d·블록 수 조정 후 1회 재시도 |
| M2 few-shot(gradient) | 평가 과제 데모 포함 학습 → 시험 입력 pass@2 (역증강 투표). **투표는 데모 검증 가중**: 각 증강 변형에서 같은 모델로 데모 입력을 풀어 데모 출력 재현률로 시험 예측을 가중(스도쿠의 '유효성=정답' 에 가장 가까운 ARC 검증기; STDP.md §6.8 — 논리 동일 변형에 완답 14~19% 가 뒤바뀌므로 증강 = 준독립 재시작). 출력 크기(EOS)·색 집합 일관성으로 후보 탈락. | 0 이 아닌가. 참고: HRM/TRM ARC-1 eval 40%대 (수백 GPU-h). 우리 예산 ~10 GPU-h 에서 5% 이상이면 구조 검증으로 충분 |
| M3 절제 | `e` 자리: 입력 덧셈만 / (A)만 / (A)+(B) | (B) 가 이동·복사 류 과제를 여는지 — 과제 유형별 정답률로 확인 |
| M4 해석 | 학습된 `e` 가 커널을 어떻게 바꾸는지(θ 이동·ψ 부호)를 과제 유형과 대조. step-8 `a` 를 뽑아 "데모가 정한 그래프" 를 그림으로 | 스도쿠의 그래프 판독과 같은 절차 |

M1 이 실패하면 M2 이하는 하지 않는다. M1 의 첫 시험은 **입출력 같은 크기 과제만**(69%)으로 좁혀 빠르게 본다.

## 3. v1 — 규칙을 문맥에서 쓰기 (진짜 테스트타임 적응)

v0 의 `e` 는 gradient 로 쓴 벡터다. v1 은 같은 자리를 **순전파 한 번**으로 채운다.

```
시퀀스 = [데모 입력 1 | 데모 출력 1 | … | 데모 입력 K | 데모 출력 K | 시험 입력]      (격자당 900, K ≤ 4 → T ≤ 8,100)
위치 = (격자 번호, r, c).  커널 Δ 는 (r,c) 차 + 격자 관계(같은 격자 / 입력↔출력 짝 / 데모↔시험).
```
두 개의 빠른 변수:
1. **그래프 기억 `w_tn`** (스도쿠 4단계 그대로): 시험 칸 ↔ 데모 칸의 대응에 관성. 격자 간 간선에 걸린다.
2. **수송 기억 `S_h`** — 문맥 내 제한 사상 (델타 규칙, DeltaNet 꼴):
   ```
   데모 쌍의 대응 칸 (t_in, t_out) 마다:   S_h ← S_h + η ( v_{t_out} − S_h v_{t_in} ) v_{t_in}ᵀ        S_h ∈ R^{c×c}, 과제마다 0 에서 시작
   시험 칸의 수송:                          메시지 = Σ_n a_tn · W_hᵀ S_h v_n
   ```
   v0 의 `diag(1+tanh(U e))` 자리에 `S_h` 가 들어간다. 델타 규칙이라 같은 키(입력 값)에 대한 옛 연상을 지우고 새로 쓴다 — 데모 K 개가 서로 간섭하지 않는다. sheaf 로는 "제한 사상을 데모에서 쓴다"(neural sheaf diffusion 의 순전파 판).
   대응 칸 짝은 같은 크기 과제에서는 (r,c) 동일 위치로 주어지고, 크기가 다르면 그래프 `a`(격자 간 간선)가 짝을 정한다 — 후자는 v1.5.

v1 의 학습: 느린 가중치(`W, W_C, θ, U, 경계`)와 규칙 파라미터(η)는 여전히 지도학습(BPTT, 세그먼트 안). `S, w` 는 순전파. 즉 "메타학습된 빠른 가중치"(differentiable plasticity)이고, 시험 시 과제당 gradient 가 **없다**. Kaggle 12h 예산의 대부분을 추론이 아니라 사전 학습에 쓸 수 있다는 뜻.

v1 판정: v0 와 같은 과제에서 (a) 과제당 gradient 0 회로 M2 와 같은 수준이 나오는가 (b) 데모를 바꿨을 때 `S_h` 와 시험 출력이 바뀌는가(문맥 의존성의 직접 증거).

## 4. 그 사이의 한 걸음 — 입력 조건부 그래프 (지그소 스도쿠)

ARC 전에 "그래프가 입력마다 달라지는 과제" 에서 코어가 도는지 봐야 한다. 스도쿠는 그래프가 늘 같아 그 능력을 약하게만 시험했다.
지그소 스도쿠: 박스 모양이 퍼즐마다 다르고 영역 id 가 칸마다 토큰으로 주어짐(입력 vocab 에 영역 채널 추가). 같은 코어·같은 하네스. 판정은 step-8 `a` 를 뽑아 영역 그래프와 대조(스도쿠에서 한 방법 그대로). 비용 작음. v0 M1 과 병행 가능.

## 5. 위험·미지

- **T=900 의 계산량.** 어텐션 [B,H,900,900] 이 블록마다. d 를 줄이면 스도쿠에서 본 1248 판의 거동과 달라진다. M1 이 안 되면 첫 의심은 여기.
- **출력 크기.** EOS 인코딩이 있지만 크기 변환 과제(31%)를 코어가 표현할 수 있는지 미지. v0 는 같은 크기부터.
- **새 색(71/172).** 색 치환 증강이 있으므로 원리상 가능하나, 값 블록이 색을 어떻게 코딩하는지에 달림.
- **ψ 부활.** 방향 결합은 2단계에서 버린 대칭성을 깬다 — 에너지·해석 도구 일부를 ARC 에서는 포기하는 결정. 과제 조건부(`ψ(e)`)라 스도쿠 류 과제에서는 0 으로 돌아갈 수 있다.
- **v1 의 대응 칸 짝** — 크기 다른 과제에서 미해결.

## 6. 순서

1. 지그소 스도쿠 데이터 빌더 + 3단계 코어 발사 (그래프 조립 확인)  ─ 병행
2. M0 → M1 (같은 크기 과제, d=384, 64 스텝)
3. M2 → M3 (임베딩 자리 절제)
4. v1: `S_h` 델타 규칙 + 격자 간 커널 → 과제당 gradient 없는 few-shot
