# 단계별 구조 변경의 필요성 — 무엇을 얻으려고 무엇을 지불했나 (2026-08-29)

> 7일차에 원판(R1B8_bilin)에서 네 단계를 차례로 학습했다: minimal → ψ=0 → 주소/값 분할 → STDP.
> 같은 스텝에서 held-out 은 단계마다 내려갔다 (9,765: 원판 .690/119 → .652/55 → .620/26 → .617/21).
> 이 문서는 그 각각이 **왜 필요했는지**를 이론으로 적고, 대가가 무엇인지를 같이 적는다. 판정 기준은 성능이 아니라 자기교정 가능성이다(§6).

## 0. 공통 목표 — 해석이 근사가 아니라 항등식이어야 한다

이 프로젝트의 목표는 정확도 자체가 아니라, 하네스 수준에서 모델의 추론을 점검하고 "여기서 잘못 생각했으니 이 값을 바꾸면 정답으로 간다" 를 **판단할 수 있는 규칙성**을 찾는 것이다. 그 판단이 서려면 두 가지가 있어야 한다:
- (i) 상태를 항으로 **정확히** 분해할 수 있을 것 — 근사(선형화·귀속 추정)가 아니라 항등식.
- (ii) 동역학이 **어디로 가는지** 를 말해 주는 양 — 에너지·고정점·위반량 — 이 존재할 것.

활성화 함수 0개는 (i) 를 위한 것이었다(스칼라 장 `{a, ‖h‖, g}` 를 고정하면 블록이 정확히 아핀, 명제 6). 그러나 원판은 (ii) 가 없었다: `a=a(h)` 가 스텝마다 다시 그려지고 결합이 비대칭이라 어떤 에너지의 경사도 아니며(명제 8), 그래서 오답의 정체를 알아도("초기 추측의 상호 정당화 순환", 안정 X 는 궤적으로 V 와 구별 불가) 교정 방향을 유도할 수 없었다. 네 단계는 전부 (ii) 를 만들기 위한 것이다.

## 1. minimal — 근거 없는 부품 제거

**무엇**: `exp(skew)`(5.5M, 게이지 자유도) → QR 행직교; `w_ov·w_bo`(미사용) 제거; `Λ`·`d_t·b` 제거; Strang 반스텝 제거.
**왜 필요한가**: 해석의 항등식은 항이 적을수록 읽힌다. `Λ`(비대칭 자유 선형)는 튜링/FHN 그림의 부품이었고 그 그림은 첫날 이론(명제 8·9)이 이미 기각했다. `d_t·b` 의 근거(기함수 대칭 파괴)는 쌍선형 경계가 이미 짝함수라 공허하다. 남는 것은 전부 이름을 가진 연산(주소 커널·sheaf 수송·Φ·쌍선형·주입)이다.
**대가**: 없음 — 같은 스텝에서 원판과 동급(7,812: .668/52 vs .677/53). 파라미터 14.1M → 7.15M.
**얻은 것**: 이후 단계의 판독(step-8 `a` 를 뽑아 외부 동역학에 꽂기)이 가능해진 기반.

## 2. ψ = 0 — 결합의 대칭

**무엇**: 위상차 ψ 를 0 으로 고정 → `a_tn = a_nt` (명제 7).
**왜 필요한가**: 값 수송 `WᵀW` 는 이미 대칭 PSD 다. 결합 `a` 까지 대칭이면, 스텝의 값 갱신이 정확히
```
E_adj(h; a) = −½ Σ a_tn ⟨Wh_t, Wh_n⟩
```
의 경사 하강이 된다(인접 꼴이 이미 경사 — 라플라시안 개조 불필요). 비대칭이면 `a` 의 반대칭 부분 `Σ a^a WᵀW(·)` 이 남고 이는 어떤 계량에서도 경사가 아니다(`cos(X−ψ)/cos(X+ψ)` 는 대칭화 불가). 즉 ψ=0 은 (ii) 의 **필요조건**이다 — 에너지·고정점·"최소 = 답" 이 성립하려면 다른 길이 없다.
**대가**: 방향 간선. 대칭 결합은 이완만 하고 순환(방향 전파)이 없다 — 이론이 예고했고, 실측은 4천 스텝 지연 + 완답 절반. **2×2 로 확정(STDP.md §6.1): 지연·완답 손실은 전부 여기서 온다** (ψ자유+분할 9,765: .680/71 vs ψ0+분할 .620/26). 방향의 정체 = 위치 고정 진행파 방향장(이류), 학습된 ψ≈π/2 (§6.3).
**얻은 것**: 스텝 = 에너지 하강의 후보. 단, 스텝 안에서 `a` 가 변하면 이 에너지는 조건부(명제 6 수준)에 그친다 → 3단계.

## 3. 주소/값 분할 — 규칙 쓰기와 규칙 따르기의 분리

**무엇**: 상태 `h = [주소 d_a | 값 d_v]`. `W_C` 는 주소만 읽고, 값 수송·(Λ·b) 는 값 블록에만 쓴다 → `row(W_C) ⟂ row(W)`. 경계·주입만 주소를 쓴다.
**왜 필요한가**: 원판에서는 주소와 값이 같은 좌표를 써서 값 메시지가 들어올 때마다 주소가 **저절로** 바뀌었다. 그 결과 (a) 스텝 안에서 `a` 가 변해 2단계의 에너지가 스텝 단위로도 성립하지 않고, (b) 결합이 값에 즉시 반응하므로 "틀린 값이 그래프를 다시 써서 자신을 정당화" 하는 통로(어제 C3: 결합 재협상)가 열려 있으며, (c) 조건부 선형성이 "장을 고정하면" 이라는 단서를 항상 달고 다닌다. 분할은 세 가지를 한 번에 닫는다:
- 스텝 안 `a` 불변(실측 `|Δa| = 1e-3`) → 스텝은 **무조건** `E_adj` 의 정확한 경사 하강.
- 그래프 재작성은 경계에서만 → "블록 k 에서 간선 (t,n) 이 바뀌었다" 가 로그 가능한 **사건**이 된다.
- 결합 재협상 경로가 스텝 안에서 닫힌다.
델타 규칙(DeltaNet)이 키→값 간섭을 막는 것과 같은 요구이며, 상태 분할은 그것을 규칙이 아니라 구조로 만든 것이다.
**실측이 보여 준 것**: 분할 모델의 `a` 는 첫 세그먼트에서 조립된 뒤 고정된다(1→8 스텝 변화 2.1, 8→128 변화 0.44). 즉 "고정 위치 그래프 위의 sheaf 확산 + 쌍선형" 판이 실제로 학습되고 70% 를 넘는다 — 첫날 이론 C2 의 고정 그래프 판이 실재.
**대가**: 원판이 누출로 얻던 **내용 의존 그래프**를 그래프에서 표현할 수 없다 → 값 경로가 떠안는다. 주소 폭 절반(p 52→26). **2×2 실측: ψ 가 자유이면 분할의 비용 ≈ 0** (7,812: .664/53 vs minimal .668/52). 부작용: `a` 가 퍼즐 안 불변이라 `Γ=a` STDP 가 공허 → `Γ=a·⟨v̂,v̂⟩` 로 재정의.
**얻은 것**: (ii) 의 충분조건 — 스텝 단위 정확한 에너지, 사건 로그, 결합 재협상 차단. Equilibrium propagation 의 전제(대칭 결합 + 에너지 하강) 도 여기서 갖춰진다.

## 4. STDP — 결합의 기억

**무엇**: `w_tn ← w_tn + η(Γ_tn − w_tn)`, 결합 `(1−λ)a + λw`, 퍼즐마다 리셋, 세그먼트를 넘어 carry. 최종 `Γ = a_tn·⟨v̂_t, v̂_n⟩`.
**왜 필요한가**: 오답의 실물은 "초반 추측이 규칙을 거쳐 서로를 정당화하는 순환" 이고, 상태를 흔들어도 순환이 도로 채워지며(단일 칸 초기화 프로브), 남는 것은 입력 핀뿐이었다. 즉 모델에는 **결합 수준의 기억**이 없다 — 어떤 관계가 이미 확립됐는지를 상태가 아니라 결합이 들고 있어야 순환에 관성이 생긴다. 원판에 학습 없이 얹어 947→999(갱신 횟수에 단조)로 실증.
**정합한 형태의 유도**: 결합 에너지 `E(h,w) = −½Σ w_tn a_tn⟨Wh_t,Wh_n⟩ + ½Σ w²` 의 `∂/∂w` 가 곧 규칙이고, 그 목표가 `Γ = a·⟨Wh_t,Wh_n⟩` — 그래프(부호·국소성) × 값 일치. `Γ=a`(주소만) 는 분할 모델에서 상수라 공허했고, `Γ=⟨v̂,v̂⟩`(값만) 는 부호가 빠져 학습이 거부했다(λ 전 헤드 하락). 곱 형태만 비회귀.
**STDP 로 읽는 이유**: 커널 `cos(φ_t−φ_n−θΔ−β)` 는 위상 STDP 창의 1차 조화(Aoki–Aoyagi), 누적축은 재귀 스텝(정보를 담지 않는 적분 변수), 두 축이 별개라는 것은 생물 STDP 와 같다. 시간은 필요 없다.
**대가**: 지금까지 중립(3단계와 동일 곡선). 학습된 λ 가 헤드별로 갈림(한 헤드만 채택).
**얻은 것(예정)**: 순환에 대한 관성, 흔들어도 남는 그래프, 인과 기록으로서의 `w` — 단 이득은 충분히 학습된 뒤에만 잴 수 있다(9,765 시점의 시험은 무의미했음).

## 5. 이 순서가 왜 이 순서인가

각 단계는 앞 단계의 조건을 쓴다: 대칭(2) 없이는 에너지가 없고, 분할(3) 없이는 그 에너지가 스텝 안에서 성립하지 않으며, 기억(4)의 정합한 형태는 그 에너지의 `∂/∂w` 다. 그리고 전부가 ARC 로 가는 부품이다: 분할은 "그래프는 데모가 쓴다" 의 자리(ARC.md §1 B), 대칭 결합은 EP 의 전제, 결합 기억은 데모↔시험 대응의 관성, 값 수송은 문맥 내 제한 사상 `S_h` 의 자리.

## 6. 판정 기준 — 자기교정이 가능한가

성능이 원판보다 느린 것은 단계마다 능력을 지불했으니 당연하고, 이 문서의 판정 기준이 아니다. 네 단계가 만든 것은 (ii) — 에너지·사건·결합 기억 — 이고, 그것의 존재 이유는 하나다: **모델 자신의 동역학에서 교정 방향을 유도할 수 있는가.**

충분히 학습된 4단계 체크포인트에서 잴 것:
1. **탐지**: 라벨 없이 오답 칸을 짚는가. 후보는 결합 기억과 현재 그래프의 불일치 `|w − Γ|`, 스텝 에너지 `E_adj` 의 칸별 기여, 정착도. 원판에서는 정착도만 살아 있었고 안정 오답(초반 굳은 순환)은 못 짚었다 — 4단계에서 그 안정 오답이 짚히는지가 핵심.
2. **방향**: 짚은 칸을 모델이 정한 방향으로 흔들면(정착 목적 `J` 의 `∇_h`, 또는 `E_adj` 의 헤시안 무른 모드) 유효 격자로 가는 비율이 무작위 섭동보다 높은가. 원판: gradient 12/64 vs 무작위 4/64.
3. **관성**: 결합 기억이 있는 모델에서 오답 순환이 (a) 덜 생기는가(완답 대비 자기일관 오답 비율), (b) 흔들었을 때 도로 채워지는 비율이 낮은가(단일 칸 초기화 프로브의 복귀율, 원판은 안정 오답 14/18 복귀).
4. **루프**: 이 셋으로 만든 "탐지 → 방향 → 재실행 → 유효 격자 채택" 루프가 오답 채택 0 으로 완답을 얼마나 늘리는가. 원판에서 랑주뱅식 개입이 +100 안팎이었으니 그 이상이어야 의미가 있다.

이 넷이 되면 지불한 성능은 교정 루프가 되돌려 주는 것이고, 안 되면 단계들은 해석 도구를 남겼을 뿐 목표에는 못 미친 것이다. 측정 시점은 모델이 원판처럼 어려운 퍼즐을 절반 이상 풀 때(완답 수백)부터 — 그 전의 시험은 정보가 없다.
