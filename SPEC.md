# 다음 학습 사양 (v2) — 추론 시점 누적 학습

작성 2026-08-31. 기준 = `core/minimal.py`. 미해결 목록은 `UNSUP.md`.
근거 표시: `[코드]` 파일 확인 · `[유도]` 수식 · `[검증]` 수치 · `[측정]` 학습된 가중치를 열어본 결과.

> **사전 등록 문서다.** 학습 전에 무엇을 왜 바꾸는지, 무엇이 아직 안 정해졌는지를 박아둔다.
>
> **2026-08-31 2차 개정 — λ 가 핵심 축이다.** `analysis/stdp_infer.py` 재현 결과(아래 §0.5)가
> 초판·1차 개정의 전제를 뒤집었다. **`λ=1` 을 버린다.**
>
> **2026-08-31 1차 개정 — 변경을 단계로 나눴다.** 초판은 A(권위 가중)와 B(이득)를 한 사양에 묶었는데
> 잘못이다. ① A 의 정당화는 B 에 조건부다(지금 `w` 는 누적기가 아니라 짧은 평활이라
> 정규화가 무엇을 버리든 차이가 작다). ② B 는 산술(고정점이 `⟨Γ⟩` → 이득 1)이라 도메인 무관인데
> A 는 **스도쿠 측정 하나**에 걸려 있다(`UNSUP.md` 7번). ③ 묶으면 성공해도 실패해도 귀속이 안 된다.
> → **1단계 = B + C (Γ 는 방향만, 현행 그대로). A 는 §1 의 판별자가 지시할 때만 2단계.**

---

## 0.5 기준 사실 — 이식 기억은 이미 작동한다 (재현 완료)

`analysis/stdp_infer.py`, 체크포인트 `R1B8_bilin_r2 step123039` (구 아키텍처, `w` 를 **모르는** 가중치),
held-out 2048, loops 16, **학습 없이 추론에만 얹음**. 2026-08-31 재현 — `STDP.md §2.3` 기록과 완전 일치.

| η \ λ | 0.25 | 0.5 | 0.75 |
|---|---|---|---|
| 0.1 | 969 (+22) | 991 (+44) | **31** |
| 0.05 | **999 (+52)**, 셀 0.7351→0.7447 | 687 | 533 |

기준 947. 얹은 규칙은 **가장 단순한 형태**다:

    Γ = a          (순수 커널. agree 곱 없음)
    w ← w + η(Γ − w)   (이득 1)
    결합 = (1−λ)·a + λ·w

**여기서 읽어야 할 것 셋**
1. **느린 기억이 실제로 돕는다.** η=0.05 (τ=20스텝) 가 최적. "긴 기억은 지연이라 해롭다" 는 틀렸다 — λ=1 조건부였다.
2. **기억은 소수 성분이어야 한다.** λ=0.75 에서 붕괴(31). 신선한 커널 `a` 가 전달을 지배해야 한다.
3. **이득 1 로도 +52.** `κ/δ > 1` 없이 나온 결과다.

**함의 — `λ=1` 은 잘못이다.** `LTConfig` 의 `stdp_lam_init = 0.25` 는 위 최적과 일치하고 `lam_raw` 는
헤드별 학습 가능이었다. 그런데 7일차 학습이 `stdp_lam_fixed=1.0` 으로 덮어써 붕괴 지점 너머로 갔다.
`lam_raw` 가 죽어 있던 것은 **그 덮어쓰기 때문**이다 (초판에서 "죽은 파라미터 → 제거" 라고 한 진단은 원인을 거꾸로 봤다).

| | 완답/2048 |
|---|---|
| 123k 원판 | 947 |
| **123k + 이식 기억 (λ=0.25, η=0.05, 학습 없음)** | **999** |
| stdp1 학습판 (λ=1 강제, 228,501) | 974 |

아키텍처가 다르므로(14.1M 구판 vs 7.16M minimal) 깨끗한 비교는 아니다. 다만 λ=1 을 의심할 근거로 충분하다.

**단서** `STDP.md §2.3` — 불일치량 `Σ|w−a|` 은 **정착도**를 재지 오답을 짚지 않는다(정착·정답과 정착·오답이 같다).
그리고 loops 스윕에서 기준은 64 에서 포화, 결합 기억은 128 까지 계속 오른다.
→ +52 의 정체가 "오답을 찾아서" 가 아니라 **"관성이 붙어 진동이 덜해서"** 일 가능성이 크다.
진짜 이득이지만 목표(인스턴스 구조의 비지도 학습)와는 **다른 종류**다. 구별해 둘 것.

---

## 0. 무엇을 바꾸지 않는가

- **세그먼트 경계 detach 유지** (사용자 결정 2026-08-31). 그래디언트 창 = 8블록.
  → `UNSUP.md` 5번(학습 지평 8 vs 사용 128)은 보류 상태 그대로. **아래 §3-A 의 위험이 여기서 나온다.**
- 쌍선형 경계, 주입, 복소 주소 커널(RoPE 형), `Φ` 4차 소산, 블록 8·루프 16, 가중치 공유 — 전부 그대로.
- ψ 자유 학습 유지. **반대칭을 강제하지 않는다** — 학습된 모델이 피어 제약을
  *음의 대칭 결합*으로 풀었고(`w_tn = −0.0489`, `w_nt = −0.0488`, 반대칭 성분 평균 **−0.0000**) `[측정]`
  그게 옳은 인코딩이다. 피어 관계는 본질적으로 대칭이다.
- `addr_dim=0`, `psi_zero=False`, `gate=False`, `k_fields=1` — 현행 유지.

---

## 1. 1단계 — 변경 B + C 만. Γ 는 방향만 (현행 유지)

Γ 는 **건드리지 않는다**: `Γ_tn = a_tn · ⟨v̂_t, v̂_n⟩`, 크기 정보 없음.
누적기만 진짜 적분기로 바꾸고(변경 B), 죽은 코드를 치운다(변경 C).

**묻는 것**: *방향만 정렬해도 누적이 되는가.*
방향 누적은 실체 있는 대상이다 — `⟨v̂_t,v̂_n⟩` 의 시간 평균은 구면 위 두 궤적의
평균 정렬도(concentration)이고, 크기와 무관하게 관계의 안정성을 잰다. 크기는 교란변수일 수 있다.
그리고 크기는 **수송에서 이미 쓰인다**(`o = einsum(w, v)`, 정규화 안 된 `v`) —
Γ 에도 넣으면 이중 계산이다. 즉 현행 분업(`w` = 관계, `v` = 세기)이 옳을 가능성이 실재한다.

### 사전 등록 판별자 — 무엇이 관측되면 2단계로 가는가

δ 스윕만으로는 부족하다. 느린 δ 가 나빠도 **초기 오염**인지 **지연**인지 안 갈린다. 세 조건을 돌린다.

| 조건 | δ | 누적 시작 | 뜻 |
|---|---|---|---|
| (i) 기준 | 큼 (현행 수준 ≈0.5) | 0 | 짧은 평활 = 현행 |
| (ii) 긴 기억 | 작음 | 0 | 진짜 누적, 초기 포함 |
| (iii) 긴 기억·초기 배제 | 작음 | step 64 | 진짜 누적, 초기 제외 |

- **(ii) > (i)** → 방향만으로 누적이 된다. **A 불필요. 여기서 끝.**
- **(iii) > (ii)** → 초기 무지 구간이 오염원. **A 지시** — 권위 가중이 정확히 그것을 걸러낸다
  (초기에는 모든 칸이 조용하다: seg1 에서 정답 칸 41.5, 오답 칸 39.5 로 거의 구별 안 됨 `[측정]`).
- **(iii) ≈ (ii) < (i)** → 긴 기억 자체가 해롭다(지연). **A 로는 안 고쳐진다.** 설계를 다시 본다.

(iii) 이 궤적도 함께 바꾸는 것은 의도된 것이다 — 묻는 것이 "초기를 잊는 편이 나은가" 이므로.

### 학습 전 사전 점검 (선택, 싸다)

기존 체크포인트 `R1B8_min_stdp1_step228501` 로 **추론만** 돌려 위 세 조건을 볼 수 있다.
단 **`G=1` 로 고정**할 것 — `G>1` 은 전달 결합의 크기를 바꿔 학습된 가중치에 대해 분포 밖이 된다.
δ 와 시작 시점만 바꾸는 것은 전달의 *형태* 를 안 바꾸므로 상대적으로 안전하다.
결정적이지는 않다(체크포인트가 빠른 η 로 학습됨). 방향만 본다.

---

## 1단계 변경 B — 누적기 이득 (`UNSUP.md` 4번)

**문제** `[유도]` `w ← w + η(Γ−w)` 의 고정점은 `⟨Γ⟩`. **이득이 η 와 무관하게 정확히 1** 이라
결합이 순간 증거보다 강해질 수 없다. 역사는 주지만 강도는 못 준다 = 평활이지 학습이 아니다.

게다가 학습된 η 는 대부분 빠르다 `[측정]` — 8헤드 중 6개가 시상수 1.3~2.0 스텝
(최근 3스텝이 88~99%). 추론은 128스텝인데 `w` 는 사실상 **순간 커널의 짧은 평활**이다.

**변경**

    w ← (1−δ)·w + δ·G·Γ          # = w + δ(G·Γ − w),  고정점 = G·⟨Γ⟩
    δ = sigmoid(delta_raw)  [H,1,1]     G = softplus(gain_raw)  [H,1,1]

`G=1` 이면 현행과 정확히 동치. δ 는 "얼마나 빨리 잊나", G 는 "얼마나 쌓이나" — 분리됐다.

**부수 효과** `|a| ≤ 1` 정리 소멸 (`|w| < 2G`). `Φ` 의 흡수구가 상태를 여전히 절대적으로 묶으므로
(`‖Φ(h)‖ ≤ 1/√γ`, 입력 무관) 발산은 없다. 보장의 성격만 바뀐다.

**에너지 해석 유지** `E = −κ Σ wΓ + (δ/2) Σ w²` 의 경사 흐름. 이차 벌점을 약화한 것뿐이다.

---

## 1단계 변경 C — 죽은 코드 제거 (`[측정]` 로 확인)

| 대상 | 근거 | 조치 |
|---|---|---|
| `beta` `[H,p]` | 체크포인트에서 **정확히 0.0000**, 학습된 적 없음. `stdp_target="product"` 경로가 `kcb` 를 안 쓴다 (쓰는 건 `addr`·`causal` 분기뿐) | 제거 |
| `kernel(self.beta)` 호출 | 위 이유로 매 forward 계산되고 버려짐 | 제거 (세그먼트당 커널 계산 1회 절약) |
| `stdp_lam_fixed=1.0` 덮어쓰기 | 붕괴 지점(λ=0.75 → 31) 너머. `lam_raw` 가 죽은 **원인** `[측정]` | **제거. λ 를 헤드별 학습으로 되돌린다 (init 0.25)** |
| `v` 중복 einsum | `agree` 용 `vv` 와 수송용 `v` 가 같은 계산인데 두 번 돈다 (`addr_dim=0` 이면 `hv0 == hv`) `[코드]` | 한 번만 계산해 공용 |

**λ 는 학습으로 되돌린다** (`lam_raw`, init 0.25, 헤드별). §0.5 가 근거다 — 기억은 소수 성분이어야 하고,
init 0.25 는 이식 실험의 최적과 일치한다. `stdp_lam_fixed` 는 스윕용으로만 남긴다.

파라미터 순증감: −416 (`beta`) +8 (`gain_raw`) = **−408**. (`lam_raw` 는 **존치**)

---

## 2단계 (조건부) — 변경 A: Γ 에 권위 가중 (`UNSUP.md` 1번)

**§1 의 판별자가 (iii) > (ii) 를 낼 때만 착수한다.** 그 전에는 넣지 않는다.

**문제** `[측정]` 정착도를 나르는 양은 `‖W h_t‖` 다. seg16 에서 빈칸-정답 **93.2** vs 빈칸-오답 **31.2** (약 3배).
수송 `o = einsum(w, v)` 은 정규화 안 된 `v` 를 써서 이 신호를 쓴다. 그런데 누적기는

    Γ = a · ⟨v̂_t, v̂_n⟩        a: 칸 크기에 불변 `[검증: 100배 → 2e-5]`   v̂ = v/‖v‖: 정규화

두 인자 모두 크기를 버려 **Γ 가 칸 크기에 완전히 눈이 멀어 있다.** `w` 는 "헤매는 칸의 동의"와
"확신 있는 칸의 동의"를 같은 무게로 기록한다.

**변경** 보내는 쪽 권위만 곱한다. 받는 쪽에는 곱하지 않는다 — 크기는 수송에서 이미 한 번 쓰이므로
양쪽에 다 곱하면 이중 계산이 된다.

    mag = ‖v‖                          # [B,T,H]
    r   = mag / mean_cells(mag)        # 퍼즐 내 평균 대비 → O(1), γ 표류에 무관
    s   = 2r² / (1 + r²)               # 포화 이득. r=1 에서 s=1, 상한 2
    Γ_tn = a_tn · ⟨v̂_t, v̂_n⟩ · s_n     # n = 보내는 쪽

**성질**
- `Γ_tn / Γ_nt = s_n / s_t` → **기억이 비대칭이 되고 그 비대칭이 곧 권위비다.**
  부호가 아니라 **크기**로 "누가 양보하는가"가 `w` 에 기록된다. 위상에 순서를 심는 것보다 싸다.
- 음의 대칭 피어 결합은 보존된다. `w_tn ∝ s_n`, `w_nt ∝ s_t` — 둘 다 여전히 음수이고,
  약한 칸이 강한 칸에게서 더 세게 밀린다. 원하는 방향.
- 폭주 없음. `s < 2` 로 유계라 수송은 `s_n·‖v_n‖ ~ O(‖v_n‖)` 이지 `‖v_n‖²` 이 아니다.
- 관측값 대입: ‖v‖ 31 vs 93 → `r ≈ 0.5 / 1.55` → `s ≈ 0.40 / 1.41`, **약 3.5배 가중** `[측정]`
- 새 파라미터 0개. 새 `[B,H,T,T]` 텐서 0개 (`s` 는 `[B,H,1,T]`).

---

---

## 블록 정의

세그먼트 = 블록 8개, 전 블록 가중치 공유. 루프 16, 세그먼트 사이 detach.

```
블록:
  ① 경계     h ← h + W_d[(W_g h)/2 ⊙ (W_u h)]                    (활성화 0개)
  ② 주입     h ← h + β·E(x)
  ③ 주소     ẑ_t = W_C h_t / ‖·‖                                  (W_C = [A;B] 행직교, QR)
             a_tn = e^{−α‖Δ‖₁} Σ_j r_tj r_nj cos(u_tn,j − ψ_j)
             u_tn,j = (φ_nj − φ_tj) − θ_j·Δ_tn
  ④ 값       v_t = W h_t                                          ← 한 번만 계산
             mag_t = ‖v_t‖ ,  v̂ = v/(mag+ε)
  ⑤ 권위     r = mag / mean_cells(mag) ,  s = 2r²/(1+r²)          ☆ 2단계에서만
  ⑥ 가소성   Γ_tn = a_tn · ⟨v̂_t,v̂_n⟩ [· s_n]                      ☆ [·s_n] 은 2단계에서만
             w ← (1−δ)w + δ·G·Γ        (퍼즐 시작 시 w = G·Γ)     ★ 1단계
  ⑦ 수송     f_t = Wᵀ Σ_n w_tn (W h_n)                            (정규화 안 된 v)
  ⑧ 소산     h ← Φ(h + f) ,  Φ(h) = h/√(1+γ‖h‖²)

세그먼트 끝: h.detach(), w.detach()          로짓 = W_cls h
```

**역할 분리 (이 설계의 요지)**

| 채널 | 담당 | 근거 |
|---|---|---|
| 위상 `φ`, `θ·Δ` | **배선** — 누가 누구와 말을 거는가 | RoPE 구조. `a` 는 크기에 불변 `[검증]` |
| 진폭 `‖v‖` | **권위** — 누구 말이 센가 | seg16 에서 3배 분리 `[측정]` |
| 부호 (대칭) | **관계의 종류** — 배척(피어) / 인력(증거) | 피어 −0.049, 비피어·정답값 +0.0094 `[측정]` |
| `w` 의 비대칭 | **순서** — 누가 양보하나 | 변경 A 로 `s_n/s_t` 가 됨 |

생물의 STDP 는 `Δt` 하나가 이 넷을 다 했다. 그것밖에 없어서다. 여기서는 채널이 나뉘어 있고,
**계량 시간이 필요 없다** — 누적에 필요한 건 순서(스텝 128회)뿐이고 그건 이미 있다.

---

## 5. 아직 결정 안 된 것

### A. δ 를 학습시킬 것인가 — **가장 큰 위험**
detach 를 유지하므로 그래디언트 창은 8스텝이다. 누적의 효용은 128스텝에 걸쳐 나타난다.
δ=0.05 (시상수 20스텝) 면 8스텝 안에 효과의 **33%** 만 보이고, δ=0.008 이면 **6.2%** 만 보인다 `[검증]`.
학습에 맡기면 δ 가 다시 빠른 쪽으로 붕괴할 공산이 크다 — 현행 η 가 정확히 그렇게 됐다 `[측정]`.

- (권장) **δ 를 런당 상수로 고정하고 스윕**, G 만 학습. 가설이 검증 가능해진다.
- (대안) δ 도 학습. 빠른 쪽으로 가면 그 자체가 정보 — "8스텝 시야에서는 누적을 원하지 않는다".
- (대안) 5번 보류를 푼다. 사용자가 지금은 원하지 않음.

### B. G 의 init 과 상한
`G=1` 은 현행 동치라 안전한 출발점. 상한을 둘지(예: `1 + softplus`) 미정.

### C. 처음부터 학습 vs 체크포인트에서 이어서
변경 A 가 Γ 를 즉시 바꾸므로 `R1B8_min_stdp1_step228501` 과 동치가 아니다.
싸게 신호를 보려면 이어서, 귀속을 깨끗이 하려면 처음부터. 미정.

### D. `s` 를 학습 가능하게 할 것인가
현재 안은 파라미터 0개(`s = 2r²/(1+r²)` 고정). 헤드별 `p ∈ (0,1)` 로
`s = (1−p) + p·2r²/(1+r²)` 로 두면 모델이 가중을 끌 수 있다. p=0 init 이면 현행 동치 출발.
**첫 판은 파라미터 없이 p=1 고정** 을 권한다 — 가중이 도움이 되는지를 보는 게 목적이므로.

### E. `UNSUP.md` 의 미결 항목 그대로
- 2번 잔여: 양의 대칭 결합이 증거 채널이자 확증 채널. 오답 칸끼리 실제로 확증하는지 **미측정**.
  변경 A 가 이걸 같이 풀 후보다 — 둘 다 약할 때(`s_n` 작음)의 합의는 안 쌓인다.
- 6번: ARC 규모 `w` 메모리. 변경 A·B 는 `[B,H,T,T]` 텐서를 늘리지 않으므로 프로필 불변.
- 7번: **측정이 전부 스도쿠다.** 진폭이 정착도를 나른다는 그림이 ARC 에서도 서는지 모른다.
- ARC 데모를 추론 시점에 쓸 것인가 / `w` 를 어느 층위에 둘 것인가.

---

## 부록 — 구현 (`core/minimal.py` 변경분)

축 검증 완료 `[검증]`: `s` 가 **보내는 쪽에만** 걸리고(`Γ[:,:,t,n]` 의 `n` 축),
`Γ_tn/Γ_nt = s_n/s_t` 가 정확히 성립(오차 1.2e-07).

### `LTConfig`

```python
stdp: bool = True
stdp_delta_init:  float = 0.05    # 잊음률 δ. 시상수 20스텝
stdp_gain_init:   float = 1.0     # 이득 G. 1.0 = 현행과 동치 출발
stdp_delta_fixed: float = -1.0    # ≥0 이면 δ 고정(학습 안 함) — §5-A 권장 경로
# 제거: stdp_target, stdp_eta_init, stdp_lam_init, stdp_lam_fixed, stdp_mu_init
```

### `LT_Inner.__init__`

```python
if self.stdp:
    lg = lambda x: math.log(x / (1 - x))
    self.delta_raw = nn.Parameter(torch.full((H, 1, 1), lg(config.stdp_delta_init)))
    self.gain_raw  = nn.Parameter(torch.full((H, 1, 1), inv_softplus(config.stdp_gain_init)))
# 제거: eta_raw, lam_raw, beta, mu
```

### `LT_Inner.step`

```python
def step(self, h, AB, kc, w=None, fresh=None):
    """v2: 권위 가중 Γ + 이득 있는 누적. λ=1 고정."""
    a  = self.attn(h, AB, kc)
    hv = h[..., self.d_a:] if self.split else h
    v  = torch.einsum('btd,hcd->bthc', hv, self.w_sh)         # 수송·agree 공용 (중복 einsum 제거)
    if self.stdp:
        mag   = v.norm(dim=-1)                                 # [B,T,H]  정착도
        vhat  = v / (mag.unsqueeze(-1) + self.config.eps)
        agree = torch.einsum('bthc,bnhc->bhtn', vhat, vhat)    # [B,H,T,T]  (t=받는쪽, n=보내는쪽)
        r  = mag / (mag.mean(1, keepdim=True) + self.config.eps)
        s  = 2.0 * r * r / (1.0 + r * r)                       # [B,T,H]  r=1→1, 상한 2
        Gm = a * agree * s.permute(0, 2, 1).unsqueeze(2)       # [B,H,1,T] → n 축 = 보내는 쪽
        dlt = (torch.sigmoid(self.delta_raw) if self.config.stdp_delta_fixed < 0
               else torch.full_like(self.delta_raw, float(self.config.stdp_delta_fixed)))
        tgt = F.softplus(self.gain_raw) * Gm
        if w is None:
            w = tgt
        else:
            if fresh is not None:
                w = torch.where(fresh.view(-1, 1, 1, 1), tgt, w)
            w = (1 - dlt) * w + dlt * tgt                      # 고정점 = G·⟨Γ⟩
        a = w                                                   # λ = 1
    o = torch.einsum('bhtn,bnhc->bthc', a.to(v.dtype), v)      # 정규화 안 된 v
    f = torch.einsum('bthc,hcd->btd', o, self.w_sh)
    if self.split:
        f = F.pad(f, (self.d_a, 0))
    return self.phi(h + f), (w if self.stdp else a)
```

### `LT_Inner._forward`

```python
kcb / vprev / gate / causal 분기 전부 제거.  블록 루프는
    h = self.boundary(h); h = h + self.inj_gate * inj
    h, w = self.step(h, AB, kc, w, fresh); fresh = None
carry 는 current_hidden 과 coupling 만. (vprev, gate 필드 제거)
```

### 검증 항목 (학습 전)

1. `stdp_gain_init=1.0`, `s` 를 1 로 고정하면 기존 체크포인트와 로짓이 일치하는가 (동치성 확인)
2. `s` 가 보내는 쪽에만 걸리는가 — 한 칸의 `s` 를 0 으로 두고 그 칸이 *보낸* 항만 0 인지 `[검증 완료]`
3. `‖Φ(h)‖ ≤ 1/√γ` 가 G>1 에서도 유지되는가 (유도상 자명하나 수치 확인)
