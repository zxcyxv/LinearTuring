# K-순환 벡터장 — 물리 레이어의 흐름 번역 (설계 메모, 미실행)

> 상태: 등록만. 스도쿠 τ=2 실패(그래디언트 잡음화) 후 "τ 없이 깊이를 사는" 축으로 도출.
> 사용자 유보: 직접 선례가 없는 구조 — §5 선례 지형과 §7 저위험 진입로 참조.
> 정정(사용자): K↑ 에 유닛 반복을 줄이면 총 깊이 불변 — §3 회계. K 는 μ 와 결합해야 성립.

## 1. 동기

- URM 유효 깊이 768 (loops16 × H2 × L6 × 층4) vs 우리 128 (loops16 × 8스텝 × **단일 사상**)
- 깊이 확장 시도 τ=2 는 실패: 카오스 노출 e^{λτ} 배증 → 그래디언트 SNR 저하 (셀 0.29 @6k)
- 남은 깊이 축: 사상 자체를 여러 장으로 — **스텝마다 다른 벡터장을 순환 적용**

## 2. 구조

```
지금:   f → f → f → … (한 장을 8번)                      [자율 흐름]
K=4:    f₁ → f₂ → f₃ → f₄ → f₁ → …                       [시간 주기 (비자율) 흐름]
각 f_k = 자기만의 (ψ,θ,α / W_sh / Λ).  적분: h ← h + dt·f_{r mod K}(h)
```

어댑터 구현: core 를 K 개 두고 `micro(h, core[r % K])` — 30분 규모.

## 3. 총깊이 회계 (사용자 정정 반영 — 핵심 제약)

총 깊이 = K × 유닛 반복.  URM = 4×12 = 48/세그먼트, 우리 = 1×8 = 8 (6배 격차).
**스텝 수를 고정한 채 K 만 올리면(유닛 반복이 줄면) 총 깊이 불변 — 허수.**
진짜 깊이는 스텝 수 증가를 요구 (K=4 × 반복 8 = 32스텝/세그) → dt 회계 딜레마:

- dt 유지(1/8) → τ=4: 카오스 노출 4배 — **μ 마찰 없이는 실패 축** (τ=2 실측)
- dt 축소(1/32) → τ=1: **평균화 함정** — 주기장을 잔걸음으로 빠르게 순환하면
  dt→0 극한에서 평균장 f̄ 하나로 뭉개져 K-깊이가 소멸. 깊이는 이산 크기의 적용에서만 나옴

배경: URM 의 48층은 이산 블록(사실상 dt≈1 큰 스텝)이고 RMSNorm 이 야코비안을 O(1)로
못박아 지수 비용이 없다. 우리는 흐름이라 시간에 지수가 붙는다.

**따름: K-순환은 단독 축이 아니라 μ(DAMPING.md)와 한 몸** — 정합 구성은
K=4 × 32스텝 × dt=1/8(τ=4) × μ(λ−μ≈0 조준). 흐름 정체성을 유지하는 유일한 경로.

## 4. 이론 지위

- 시간 주기 ODE = Floquet 고전 이론. 블록 사상 ρ 측정 = 모노드로미 행렬 그대로
- 장 추적(기록·재생·이식)은 스텝별이라 무수정 성립. 간선 에너지 귀속도 f_k 별 성립
- 유닛(K장 묶음)은 전 재귀 공유 → "단일 유닛 재귀" 정체성 유지

## 5. 선례 지형 — "완전 무선례"는 아님 (정직한 배치)

| 요소 | 선례 |
|---|---|
| 유닛 안 비공유 K층 + 유닛 재사용 | **UT/URM/TRM 이 정확히 이 위상** (URM 유닛=비공유 4층) |
| 시간 의존 벡터장 f(h,t) | Neural ODE 표준형 (주기형은 특수 경우) |
| **무선례인 부분** | 위상 정규화 어텐션 + 카오스 흐름 + 경계 이벤트와의 **결합** — 최적화 거동 미지 |

## 6. 부분 비공유 사다리 (d=768, H=8 기준 추가 파라미터)

| 변형 | 비공유 대상 | +파라미터/(K−1) | 성격 |
|---|---|---|---|
| K-커널 | ψ,θ,α 만 | **~수천 개** | 그래프 문법만 순환 — 최저가 진입로 |
| K-종 | + W_sh | +0.5M | 종 기저 순환 |
| K-전체 | + W_C, Λ | +5.5M | URM 층과 등가 |

## 7. 리스크와 판정 설계

- 리스크: (i) 결합 무선례 — 최적화 거동 미지 (ii) Floquet 승수가 자율 λ 와 다를 수 있음(양방향)
  (iii) K-전체는 파라미터 급증
- **저위험 진입로**: K-커널 (비용 ~0) 을 먼저 — 그래프 문법 순환만으로 세그 곡선/전선이 움직이면
  상위 변형으로 상승. 안 움직이면 축 폐기가 싸게 끝남
- 비교 기준: SwiGLU τ=1 (셀 0.659 @4.8k) 대비 동일 step 전선·조기 평형 질
- 대안 축 (이 축 실패 시): μ 마찰 + τ=2 재도전 (DAMPING.md), 폭 d=1024

## 8. 관련

DAMPING.md (μ — 직교 축, 병용 가능) · SUDOKU.md · REPORT §11
