# LinearTuring — 장기 계획 (2026-08-29 작성, ARC Prize 2026 마감 11/2·논문 11/8)

> 이 문서 하나로 다음을 알 수 있어야 한다: 우리가 무엇을 만들고 있는가, 그것이 어떤 구조인가, 왜 그 구조인가(무엇이 장점인가),
> 지금까지 무엇이 확인됐는가, 무엇이 아직 가설인가, 그것을 어떤 실험으로 입증할 것인가, 그리고 두 달 동안 무엇을 어떤 순서로 하는가.
> 세부 정의는 `ARCHITECTURE.md`(코어 수식) · `THEORY.md`(증명된 성질) · `RESULTS.md`(6일차까지 실험) · `STDP.md`(7일차 해석·결합 기억) ·
> `STAGES.md`(단계별 필요성) · `ARC.md`(ARC 설계) · `HANDOVER.md`(재구축 절차) 에 있다.

---

## 1. 한 문장

**활성화 함수 없이 부호 있는 복소 어텐션의 재귀 동역학만으로 순차 제약 풀이를 학습하고, 그 덕에 추론을 근사 없이 항으로 분해해 오답의 기제를 읽으며, 결합에 라벨 없는 테스트타임 가소성(위상 STDP)을 두어 모델이 스스로 교정하게 한다.**

풀어 쓰면 세 주장이다.
1. **표현**: MLP·softmax·LayerNorm 없이, "누가 누구에게 어떤 부호로 말하는가"(어텐션)와 "무엇을 실어 나르는가"(sheaf 수송)와 "칸 안에서 굳히기"(2차 곱)만으로 스도쿠 급의 순차 계산이 학습된다. (실증됨: held-out 셀 .82, 완답 46%, 14M.)
2. **해석**: 비선형성이 전부 "이름 있는 스칼라 장 × 선형" 꼴이라, 장을 기록하면 블록이 정확히 아핀이다. 출처별 귀속·반사실이 항등식이고, 그것으로 오답의 실물(초기 추측의 상호 정당화 순환)을 읽었다. (실증됨.)
3. **교정**: 오답 순환에는 "결합의 기억" 이 빠져 있다. 커널이 위상 STDP 창의 꼴이므로 결합을 누적하는 변수 `w` 를 두면 라벨 없이 추론 중에 그래프가 관성을 얻는다. (원판에 학습 없이 얹어 +52 실증; 학습에 통합한 판은 검증 중; 자기교정 루프는 미입증.)

ARC 는 이 세 주장이 "고정 그래프 한 과제" 를 넘어 "입력마다 다른 그래프·과제마다 다른 규칙" 으로 확장되는지의 시험이다.

---

## 2. 지금의 구조 (4단계 = 학습 중인 최신판)

### 2.1 상태와 두 구역
칸 t 마다 `h_t ∈ R^1248 = [주소 416 | 값 832]`. 81칸(스도쿠), 2D 위치 `(행,열)`, 절대 위치 임베딩 없음(커널의 상대 위치만).

### 2.2 그래프 — 주소 위상 커널 (부호 있는 어텐션)
```
z_t = W_C h_t^{addr} ∈ C^{H×26}   (헤드 8, 행직교 W_C),   ẑ = z/‖z‖  (진폭 제거 → |a| ≤ 1 구조 보장, softmax 불필요)
a_tn = e^{−α_h‖Δ_tn‖₁} · Σ_j cos( φ_tj − φ_nj − θ_j·Δ_tn )        ψ = 0 → a_tn = a_nt (대칭)
```
- `cos(위상차)`: 주소가 맞으면 +, 반대면 −. 학습된 모델에서 피어(행·열·박스)는 음(억제), 같은 숫자를 든 비피어는 양(지지)으로 조직된다.
- `θ·Δ`: 위치 차에 따른 회전 = 행·열 같은 위치 관계. 박스처럼 Δ 로 안 되는 관계는 주소가 상태에서 조립한다.
- 이 커널은 위상 STDP 창 `Γ(Δφ − β)` 의 1차 조화와 같은 함수다(§4.3).

### 2.3 수송 — sheaf (W, Wᵀ)
```
v_t = W_h h_t^{val}  (stalk 104차원),   메시지_t = Σ_n a_tn W_hᵀ v_n,   수송 = W_hᵀW_h (대칭 PSD)
h^{val} ← Φ( h^{val} + 메시지 ),   Φ(h) = h / √(1 + γ‖h‖²)   (4차 소산의 닫힌 해; 진폭 통제, 노름 층 없음)
```
값 메시지는 값 블록에만 더해지므로 **스텝 안에서 주소(그래프)는 불변** — 스텝은 `E = −½ Σ a_tn⟨Wh_t, Wh_n⟩` 의 정확한 경사 하강이다.

### 2.4 경계 — 칸 안의 2차 곱 (결정, 그래프 재작성)
```
h ← h + W_d[ (W_g h)/2 ⊙ (W_u h) ]        활성화 0개. W_d 영 초기화 → 시작 시 항등
```
후보를 굳히고, 값을 읽어 주소 블록에 쓴다(그래프 재작성은 여기서만 — "블록 k 에서 간선이 바뀌었다" 가 사건이 됨). 실측: 주소는 첫 세그먼트에서 조립된 뒤 거의 고정.

### 2.5 결합 기억 — 위상 STDP (4단계)
```
Γ_tn = a_tn · ⟨v̂_t, v̂_n⟩                     그래프(부호·국소성) × 값 일치 = 결합 에너지 E(h,w) 의 ∂/∂w
w_tn ← w_tn + η_h (Γ_tn − w_tn)               블록마다 누적, 퍼즐마다 리셋, 세그먼트를 넘어 carry
결합 = (1−λ_h) a_tn + λ_h w_tn                 η, λ 는 헤드별 학습 (β: 창의 비대칭, 학습)
```
시간은 어디에도 인코딩되지 않는다 — 커널의 변수는 위상차, 누적축은 재귀 스텝(정보 없는 적분 변수).

### 2.6 블록과 하네스
```
블록 = 경계 → 주입(h ← h + β·E(x_t), 매 블록) → 스텝(그래프 1회 + 값 확산 + Φ)
세그먼트 = 블록 8개, 세그먼트 16개 = 128 스텝.  세그먼트마다 CE 손실, 세그먼트 사이 h.detach()  (URM 하네스, arXiv 2512.14693)
판독: 로짓 = W_cls h_t (9-way).  AdamATan2, lr 1e-4, wd 1.0 (동역학 상수·가소성 파라미터는 wd 0), 배치 128, EMA 0.999
```
데이터: Sudoku-Extreme 1k 퍼즐 × 증강 1000(숫자 치환·전치·밴드 셔플). 파라미터 13.35M(4단계; 원판 14.1M, minimal 7.15M).

### 2.7 원판과의 차이 (7일차에 하나씩 추가, 각각 학습 정상 확인)
| 단계 | 변경 | 얻은 것 | 지불한 것 (같은 스텝 held-out) |
|---|---|---|---|
| 1 minimal | 근거 없는 항 제거(Λ, d_t·b, exp(skew)→QR, 유령) | 항이 적어 판독 가능 | 없음 (7,812: .668/52 vs 원판 .677/53) |
| 2 ψ=0 | 결합 대칭 | 스텝 = 에너지 하강의 후보 | 방향 간선 (9,765: .652/55 vs .690/119) |
| 3 주소/값 분할 | row(W_C) ⟂ row(W) | 스텝 안 그래프 불변 → 에너지 무조건 성립, 그래프 재작성 = 사건, 결합 재협상 차단 | 내용 의존 그래프(누출) 상실, 주소 폭 절반 (9,765: .620/26) |
| 4 STDP | 결합 기억 w (`Γ=a·⟨v̂,v̂⟩`) | 순환에 관성(예정) | 중립 (46,872: .702/205; 46.9k 정지) |
| 2×2 | ψ 자유 + 분할 (`R1B8_min_split`) | 분할만 얻고 대칭은 안 삼 | **9,765: .680/71 — 손실은 ψ=0 하나에서 옴, 분할 ≈ 무료** |

성능이 원판보다 느린 것은 단계마다 능력을 지불했으니 당연하고, 판정 기준이 아니다(§5 C1). 대가를 되돌리는 길은 방향성·내용 의존을 **과제 조건부**로 되살리는 것이며 ARC 설계에 이미 들어 있다(ARC.md §1 B).

---

## 3. 왜 이 구조인가 — 장점을 정확히

1. **해석이 항등식이다.** 스칼라 장 `{a, ‖h‖, g(경계 게이트)}` 를 기록하면 세그먼트가 h 에 정확히 아핀 → `로짓 = W·L_장[h₀] + c_장`. 특정 간선·스텝을 지우고 재생하면 오차 0 의 반사실. softmax+MLP 망에서는 어떤 동결로도 불가능하다(THEORY 명제 6).
2. **부호가 있다.** 억제가 구조로 존재한다(softmax 는 양의 가중치뿐). 제약 풀이는 억제("내 숫자 아님")가 본질이다.
3. **에너지가 있다(2·3단계 이후).** 대칭 결합 + 대칭 PSD 수송 + 스텝 안 그래프 불변 → 스텝이 정확한 경사 하강. 고정점·최소·안장·장벽이 정의되고, 재시작·담금질·헤시안 무른 모드 같은 교정이 원리를 갖는다. Equilibrium propagation 의 전제도 여기서 갖춰진다.
4. **그래프를 입력·상태에서 조립한다.** 절대 위치 없이 8스텝 안에 제약 그래프가 `a` 에 생기고, 그것을 떼어 외부의 단순 배제 동역학에 꽂아도 손으로 짠 피어 그래프와 같은 성능(5/0.500 vs 6/0.493)이 난다. ARC 의 "입력마다 다른 그래프" 의 기제가 이것이다.
5. **라벨 없는 테스트타임 가소성이 실제로 작동한다.** 원판에 `w` 를 얹기만 해서 947→999, 갱신 횟수에 단조. 규칙 파라미터는 지도학습으로 메타학습되고(differentiable plasticity), 추론 중에는 순전파만.
6. **작다.** 7~14M. 12시간 예산에서 과제당 학습(TTT)을 여러 번 돌릴 수 있다.

---

## 4. 확인된 것 / 가설인 것

### 4.1 확인됨 (실측·정리)
- 원판 스도쿠: held-out 2048 에서 셀 .818, 완답 947(46%), 123k 스텝.
- 오답의 실물: 확신·오답은 (a) 진동 중 스냅샷(뒤집힘 많음) 또는 (b) 스텝 1~6 에 굳어 128 스텝 동안 안 바뀐 맞바꿈 순환(같은 행 안에서 값의 순환 치환). (b)는 궤적·여유·행합으로 정답 칸과 구별 불가, 내용(순환 구조)으로만 보임. 비확신·정답은 실체 없음(스냅샷).
- 순환은 국소 끌개: 정착 오답 칸을 초기화해도 14/18 이 오답으로 복귀. 남는 건 입력 핀뿐(정답 칸을 입력 토큰으로 핀하면 풀림, 상태로 두면 안 풀림).
- 모델은 주어진 칸 수에 단조가 아니다: 정답 힌트를 더 넣어도 어려운 영역에선 90 잃고 80 얻음. 조기 규칙은 hidden-single 의 연성 투표(정렬된 같은 숫자 주어진 칸 수로 확신, 유닛 배제 미검증).
- 경계는 결정만이 아니라 전파의 중계: 추론 시 경계를 끄면 우연 수준. 전파/결정은 추론 시 스위치로 분리되지 않는다.
- 정착-gradient 흔들기(모델 자신의 정착 목적 J 의 ∇_h)는 무작위보다 3배(12/64 vs 4/64) 유효 격자에 도달 — 정답 지향이 아니라 순환을 가장 싸게 깨는 방향.
- 주소 위상은 위치(행·열)를 담고 숫자는 값에 산다(숫자 치환 증강상 절대 숫자 코드는 존재할 수 없음).
- 위상 하나로 숫자를 들게 하면 1칸 비움도 못 푼다(국소 최소); 같은 에너지를 9-슬롯 분포에 걸면 국소 하강으로 단순 전파가 된다 → 내용은 중첩이어야 한다.
- 결합 기억 EMA: 원판 947→999(η .05, λ .25), loops 늘리면 기준은 포화·기억은 계속 상승. 분할 모델에서는 `a` 가 고정이라 `a` 의 기억은 공허; `Γ = a·⟨v̂,v̂⟩` 만 비회귀(값만은 부호가 없어 학습이 거부).
- 정리: |a|≤1, 0차 동차·γ 게이지, 조건부 선형성, ψ=0 ⟺ 대칭, 인접 꼴이 이미 E_adj 의 경사(라플라시안 개조 불필요), 비대칭이면 어떤 계량에서도 경사 아님.

- 2×2 (STDP.md §6.1): 지연·완답 손실은 ψ=0 하나. 분할 비용 ≈ 0. 학습된 ψ≈π/2, 결합의 62% 가 반대칭(위치 고정 진행파 방향장 = 이류). 대칭 모델은 진폭(주어진 칸 3.3배)으로 방향을 우회.
- 분할 모델의 오답은 97~100% 정착형(맞바꿈), 진동은 컬 유무와 무관하게 소멸 — 원판의 진동은 컬 + 그래프 재협상의 산물.
- 자기교정 루프(원판 123k, `analysis/selfcorrect.py`): 위반 칸 초기화 1047 / 무작위 같은 개수 1062 / **위반 초기화 + 결합 기억 1198** / 무작위 + 기억 1188 (3라운드, 오답 채택 0). 탐지의 기여 +10~15, 이득은 섭동 × 기억(재형성 억제).
- 굳힘 이득을 위반량으로 조절하는 반응(경계 게이트)은 학습 모델에서 분리 불가(붕괴) — 반응의 깨끗한 판은 상태 초기화.
- **기본 구조는 minimal(ψ 자유, 분리 없음)로 복귀**. 그 위에 STDP 충실형(증분 `a·⟨v̂,v̂⟩`, 전달 = `w` 전담 λ=1, η 학습) 학습 가능 확인 (`R1B8_min_stdp1`: 11,718 에서 .650/46, 70% 돌파). minimal 보다 ~1 eval 늦음(원인 미결: 지연 vs 일치 게이팅; 학습 절제 필요). EMA = 어텐션의 누수 레지듀얼(증분 vs 가중치) — STDP.md §6.12.
- **정정**: 정답 핀은 강하게 단조(무작위 K=1: 947→1219, K=15: 2047). "핀이 해가 된다" 는 오독. 진짜 발견은 **카오스 기저율**: 논리 동일 변형(숫자 치환/전치)만으로 완답 14~19% 가 뒤바뀜(등변성 실패).
- **증강 합집합**: 논리 동일 변형 10회의 유효 격자 합집합 947→1411(+464), 오답 채택 0 — 최대·최저비용 개입. 학습 중 등변성 정칙화가 다음 지렛대.

### 4.2 가설 (입증 대상)
- H1 결합 기억이 순환에 관성을 준다 — 원판에서 루프 실증(999→1198). 학습에 통합한 판(4단계)에서 같은지는 미확인.
- H2 라벨 없이 오답 칸(특히 초반에 굳은 안정 오답)을 짚는 신호가 4단계 구조 안에 있다(|w−Γ|, 칸별 에너지 기여).
- H3 그래프 조립은 입력 조건부다(그래프가 퍼즐마다 다른 과제에서도 8스텝 안에 조립된다).
- H4 과제 임베딩을 수송·커널에 걸면 ARC 규칙(변환)이 학습되고, 12시간 안 TTT 로 few-shot 이 된다.
- H5 규칙을 문맥에서 순전파로 쓸 수 있다(값 공간의 델타 규칙 fast weight = 문맥 내 제한 사상).
- H6 (장기) 에너지 구조 위에서 BPTT 없는 학습(EP / ReSuMe 식 STDP 지도)이 가능하다.

---

## 5. 입증 계획 — 주장별 실험과 통과 기준

### C1. 자기교정 (H1·H2) — 스도쿠, 4단계 체크포인트가 완답 수백을 낼 때부터
| 시험 | 방법 | 기준 |
|---|---|---|
| 탐지 | 라벨 없이 오답 칸 순위: `Σ_n|w_tn − Γ_tn|`, 칸별 `E_adj` 기여, 정착도 | 안정 오답(스텝 1~6 굳은 것)의 AUC. 원판의 정착도는 이걸 못 잡았다(V≈X) |
| 방향 | 짚은 칸을 ∇J 또는 `E_adj` 헤시안 무른 모드로 6스텝 이동 → 재실행 → 유효 격자 채택 | 무작위 대비 배율 (원판 3×) |
| 관성 | 정착 오답 초기화 후 복귀율; 자기일관 오답 비율 | 원판 14/18 보다 낮게 |
| 용량 대조 | **η=1(기억 없음, Γ 순간값)** 모델과 비교 | 같으면 STDP 는 3차 곱 용량이었던 것, 다르면 기억이 일을 한 것 |
| 루프 | 탐지→방향→재실행→유효 채택, 오답 채택 0 | 완답 증가가 원판의 무작위 개입(+100 안팎) 이상 |
추가 1: 규칙 파라미터 η·λ 의 긴 시평 학습 — e-prop 식 적격 흔적 `e_η ← (1−η)e_η + (Γ−w)`, `∂L/∂η = Σ ∂L/∂w·e_η` (세그먼트 detach 를 넘어 carry). 창 문제의 최소 해결.

### C2. 입력 조건부 그래프 (H3) — 지그소 스도쿠
박스 모양이 퍼즐마다 다르고 영역 id 가 칸 토큰으로 주어짐. 같은 코어·하네스. 판정: step-8 `a` 를 뽑아 영역 그래프와 대조(스도쿠와 같은 절차) + 학습 곡선. ARC M1 과 병행.

### C3. ARC v0 (H4) — ARC.md §1–2
- 데이터: URM 빌더(격자 900 토큰, vocab 12, EOS 로 크기 인코딩, 이면체×색치환×평행이동 증강, 과제 id = (과제, 증강)). ARC-1 training 400 + evaluation 데모, 필요 시 RE-ARC 생성기로 확장.
- 모델: 3단계 코어(분할·ψ=0) d=384, 64스텝. 과제 임베딩 `e`(128) 를 **수송**(`Wᵀdiag(1+tanh(U e))W`)과 **커널**(`θ += V e`, `ψ = P e` — 방향 부활)에 연결. 입력 덧셈(URM 방식)은 절제 대조군.
- M0 데이터 → M1 학습 가능성(같은 크기 과제부터; 재가속 여부) → M2 데모 포함 학습 후 pass@2(역증강 투표) → M3 임베딩 자리 절제 → M4 학습된 e 의 커널 변화 판독.
- **TTT 설계가 v0 의 핵심 질문**: 12시간 = 예산. 과제당 몇 분 × 120 과제 × 증강 수, 무엇을 학습하나(e 만 / 변조 / 전체), 언제 멈추나(데모 leave-one-out). HRM 이 Kaggle 안에서 실제로 한 것의 우리 판.
- 기준: M2 pass@2 가 우리 예산에서 5% 이상이면 구조 검증. URM 급(16%)은 사전학습 규모를 맞출 때의 목표.

### C4. ARC v1 (H5) — ARC.md §3
시퀀스 = [데모 K쌍 | 시험]. 그래프 기억 `w`(격자 간 간선) + **수송 기억 `S_h`**(델타 규칙 `S ← S + η(v_out − S v_in)v_inᵀ`, 데모 대응 칸에서 씀) → 시험 칸의 수송 `WᵀS_h v`. 과제당 gradient 0. 판정: 데모를 바꾸면 출력이 바뀌는가; v0 M2 와 같은 과제에서 동급인가.

### C5. (장기, 논문 뒤) BPTT 없는 학습 (H6)
자유/밀림 두 위상의 EP, 또는 ReSuMe 식 `Δw ∝ Γ(φ_목표−φ_n) − Γ(φ_실제−φ_n)`. 전제(대칭·에너지·고정점 수렴)는 2·3단계가 만들었고 고정점 수렴 여부만 미확인.

---

## 6. ARC Prize 2026 전략

### 6.1 규정 (확인, 2026-08-29)
- 외부 사전학습·외부 데이터·사전학습 모델 허용("freely and publicly available" 조건 → 가중치 공개).
- 제출: Kaggle 샌드박스 12시간, **인터넷 불가**(외부 API·외부 컴퓨트 불가), 시험 입력마다 예측 2개(pass@2).
- 오픈소스 필수(본인 코드 CC0/MIT-0 급). 제출 11/2, 논문 11/8, 결과 12/4.
- 논문 트랙 $450K(상위 $50K/$20K/$5K + 4.5 초과 풀 $375K). Kaggle 제출 연결 필수이나 점수는 낮아도 됨. 기준 6개 동일 가중: Accuracy·Universality·Progress·Theory·Completeness·Novelty. 형식 = 실제 논문(작년 1위 TRM, arXiv). "Shorter and clearer is always better."

### 6.2 두 트랙의 역할
- **논문 트랙 = 본체.** 우리의 강점(Theory·Novelty·Completeness·Universality)이 심사 기준의 4/6 이다. 스도쿠 = 통제된 시험대(재료 이미 있음), ARC = 확장 증거. Accuracy 는 낮아도 자격 유지.
- **점수 트랙 = 하한 + 기여분.** 공개 베이스라인(LLM+TTT)으로 하한을 깔고, 우리 코어를 보완 모듈(후보 검증·재순위·같은 크기 국소 규칙 과제의 교정)로 붙인다. 단독 제출 목표는 URM 급이며 v1 이 작동할 때 상향. 12시간을 우리 TTT 로 채우는 설계(§5 C3)가 여기서 결정적이다.

### 6.3 논문 골격 (심사 기준 대응)
1. Abstract/Intro — 한 문장(§1). [Novelty]
2. Prior work — HRM/TRM/URM(재귀 소형 모델), 선형 어텐션/DeltaNet(fast weight), neural sheaf diffusion, 적응 Kuramoto/위상 STDP(Aoki–Aoyagi, Seliger), differentiable plasticity, EP/e-prop. 무엇이 같고 다른가. [Novelty·Theory]
3. Approach — §2 의 구조를 알고리즘 수준으로; 활성화 0개의 귀결(조건부 선형성); 단계별 이유(STAGES). [Theory·Completeness]
4. Analysis on Sudoku — 오답의 실물, 그래프 이식, 결합 기억, 자기교정 루프(C1). [Theory·Universality]
5. ARC — v0 결과, TTT 설계, (되면) v1. [Accuracy·Progress]
6. Conclusion — 무엇이 85% 를 향해 열리는가(문맥 내 제한 사상·BPTT 없는 학습). [Progress]

---

## 7. 9주 일정 (9/1 → 11/8)

| 주 | 스도쿠 (A4500) | ARC (별도 인스턴스) | 문서·논문 |
|---|---|---|---|
| 1–2 | 4단계 계속 학습 → 완답 수백 시점에 C1 시험; η=1 대조 런; 지그소 데이터 빌드 | T=900 처리량 측정(d 384/512), M0 데이터, M1 학습 가능성(같은 크기 과제) | 논문 골격·스도쿠 장 초고 |
| 3–4 | C1 루프 완성, C2 지그소 결과 | M2: TTT 프로토콜(12h 예산) + 공개 eval pass@2; 오프라인 Kaggle 노트북 → **제출 자격 확보** | Approach 장 |
| 5–6 | e-prop 흔적(선택), C5 고정점 수렴 측정 | M3 절제, v1 `S_h` 시도; 사전학습 규모 확대(RE-ARC) | ARC 장, 그림 |
| 7–8 | 재현 스크립트 정리 | 하이브리드(베이스라인 + 모듈) 제출 실험; 가중치 공개 | 집필·교정·재현성 |
| 9 | — | 최종 제출 11/2 | 논문 제출 11/8 |

**결정점**: 2주차 말 M1 실패 → ARC 는 최소 제출(자격용)로 축소하고 논문은 스도쿠+이론 중심. 4주차 말 M2 < 5% → v1 대신 TTT 개선에 집중. 6주차 말 v1 미작동 → v0 로 제출.

---

## 8. 위험

- **T=900 계산량**: 어텐션 [B,H,900,900] 이 블록마다. d 를 줄이면 스도쿠 1248 판과 거동이 다름. M1 실패 시 첫 의심.
- **성능 대가**: 대칭·분할이 완답을 1/5 로 줄였다. ARC 에서는 ψ(e)·과제 조건부 수송으로 되돌리지만, 스도쿠 4단계 자체가 원판을 못 따라갈 수 있다 — 논문에서는 "해석·교정을 위해 지불한 것" 으로 정직하게 쓴다.
- **STDP 가 기억이 아니라 용량일 위험**: η=1 대조군이 가른다. 논문 주장 3 의 성립 여부가 여기 걸려 있다.
- **시간**: 실험이 병목. 집필은 병행 가능.
- **출력 크기·새 색·크기 다른 과제의 대응 칸**(ARC.md §5).

---

## 9. 자원

- A4500 20GB: 대화형 연구(스도쿠 판정·해석). 학습 6~9 it/s.
- 임대 인스턴스(다중 GPU): ARC 사전학습·처리량 측정. URM 규모(수백 H100-시간)까지 허용.
- Kaggle: 제출 실행 12시간(GPU 사양은 대회 페이지에서 재확인).
- 데이터: Sudoku-Extreme(HF), ARC-1/ARC-2(URM `kaggle/combined`), RE-ARC 생성기(확장용).
- 코드: `core/minimal.py`(4단계 구조, 플래그 `psi_zero·addr_dim·stdp·stdp_target`), `sudoku/`(하네스 어댑터·발사·재개), `analysis/`(진단·귀속·STDP 시험), `refs/URM`(하네스·ARC 빌더).
