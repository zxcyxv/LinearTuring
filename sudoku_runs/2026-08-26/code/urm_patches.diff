diff --git a/data/build_sudoku_dataset.py b/data/build_sudoku_dataset.py
index 9fa8476..1d2f7e6 100644
--- a/data/build_sudoku_dataset.py
+++ b/data/build_sudoku_dataset.py
@@ -62,7 +62,11 @@ def convert_subset(set_name: str, config: DataProcessConfig):
     inputs = []
     labels = []
     
-    with open(hf_hub_download(config.source_repo, f"{set_name}.csv", repo_type="dataset"), newline="") as csvfile:
+    # [LT] 로컬 CSV 우선 — HF 접근 불가 환경 대비. 없으면 기존대로 HF 폴백.
+    _local = os.path.join(os.path.dirname(__file__), "sudoku_csv", f"{set_name}.csv")
+    _src = _local if os.path.exists(_local) else hf_hub_download(config.source_repo, f"{set_name}.csv", repo_type="dataset")
+    print(f"[data] {set_name}: {_src}", flush=True)
+    with open(_src, newline="") as csvfile:
         reader = csv.reader(csvfile)
         next(reader)  # Skip header
         for source, q, a, rating in reader:
@@ -159,6 +163,7 @@ def convert_subset(set_name: str, config: DataProcessConfig):
 
 @cli.command(singleton=True)
 def preprocess_data(config: DataProcessConfig):
+    np.random.seed(int(os.environ.get("LT_SEED", "0")))   # [LT 2026-08-24; 08-26 env LT_SEED] 서브샘플·증강 재현성 고정 (어제 '시드 없이 리샘플' 문제 제거)
     convert_subset("train", config)
     convert_subset("test", config)
 
diff --git a/models/layers.py b/models/layers.py
index 5c2b648..8465636 100644
--- a/models/layers.py
+++ b/models/layers.py
@@ -11,7 +11,10 @@ import math
 try:
     from flash_attn_interface import flash_attn_func
 except ImportError:
-    from flash_attn import flash_attn_func
+    try:
+        from flash_attn import flash_attn_func
+    except ImportError:
+        flash_attn_func = None  # [LT] 미설치 시 SDPA 폴백 (수치 동등, 커널만 다름)
 
 from models.common import trunc_normal_init_
 
@@ -126,13 +129,17 @@ class Attention(nn.Module):
             cos, sin = cos_sin
             query, key = apply_rotary_pos_emb(query, key, cos, sin)
 
-        # flash attn
-        attn_output = flash_attn_func(q=query, k=key, v=value, causal=self.causal)
-        if isinstance(attn_output, tuple):  # fa2 and fa3 compatibility
-            attn_output = attn_output[0]
+        # flash attn (미설치 시 SDPA 폴백)
+        if flash_attn_func is not None:
+            attn_output = flash_attn_func(q=query, k=key, v=value, causal=self.causal)
+            if isinstance(attn_output, tuple):  # fa2 and fa3 compatibility
+                attn_output = attn_output[0]
+        else:
+            attn_output = scaled_dot_product_attention(
+                query.transpose(1, 2), key.transpose(1, 2), value.transpose(1, 2),
+                is_causal=self.causal).transpose(1, 2)
 
-        # attn_output: [batch_size, num_heads, seq_len, head_dim]
-        attn_output = attn_output.view(batch_size, seq_len, self.output_size)  # type: ignore
+        attn_output = attn_output.reshape(batch_size, seq_len, self.output_size)  # type: ignore
         return self.o_proj(attn_output)
 
 
diff --git a/models/losses.py b/models/losses.py
index f3af55b..c043964 100644
--- a/models/losses.py
+++ b/models/losses.py
@@ -142,10 +142,11 @@ def softmax_cross_entropy(logits, labels, ignore_index: int = -100):
 
 
 class ACTLossHead(nn.Module):
-    def __init__(self, model: nn.Module, loss_type: str):
+    def __init__(self, model: nn.Module, loss_type: str, q_weight: float = 0.5):
         super().__init__()
         self.model = model
         self.loss_fn = globals()[loss_type]
+        self.q_weight = q_weight   # [절제] 0 이면 q(BCE) 손실 제거 — q_head 동결 (logits=init −5, 조기 halt 불가)
 
     def initial_carry(self, *args, **kwargs):
         return self.model.initial_carry(*args, **kwargs)  # type: ignore
@@ -227,7 +228,7 @@ class ACTLossHead(nn.Module):
             if k in outputs:
                 returned_outputs[k] = outputs[k].detach()
 
-        total_loss = lm_loss + 0.5 * (q_halt_loss + q_continue_loss)
+        total_loss = lm_loss + self.q_weight * (q_halt_loss + q_continue_loss)
         if aux_loss is not None:
             total_loss = total_loss + aux_loss
 
diff --git a/models/urm/urm.py b/models/urm/urm.py
index 4791a3d..a845fe9 100644
--- a/models/urm/urm.py
+++ b/models/urm/urm.py
@@ -36,6 +36,7 @@ class URMConfig(BaseModel):
     loops: int
     L_cycles: int
     H_cycles: int
+    act: bool = True   # [절제] False 면 학습 중 조기 halt(ACT) 제거 — 결정론적 loops 소진
     forward_dtype: str = "bfloat16"
 
 
@@ -217,7 +218,7 @@ class URM(nn.Module):
             new_steps = new_steps + 1
             halted = (new_steps >= self.config.loops)
 
-            if self.training and (self.config.loops > 1):
+            if self.training and self.config.act and (self.config.loops > 1):
                 halted = halted | (q_halt_logits > 0)
 
                 # Exploration
diff --git a/pretrain.py b/pretrain.py
index b523632..5495666 100644
--- a/pretrain.py
+++ b/pretrain.py
@@ -229,7 +229,16 @@ def create_model(config: PretrainConfig, train_metadata: PuzzleDatasetMetadata,
                 world_size=world_size,
             ),
             AdamATan2(
-                model.parameters(),
+                # [LT] 동역학 상수(ψ·θ·α·γ·b·게이트)와 1차원 파라미터는 wd 제외 —
+                # wd=1.0 이 약한 그래디언트의 커널을 (0.9999)^t 로 잠식 (loss 역행의 기전)
+                [{"params": [p for n, p in model.named_parameters()
+                             if p.ndim <= 1 or n.endswith(".b")
+                             or any(k in n for k in ("psi", "theta", "alpha_raw", "gamma_raw", "inj_gate"))],
+                  "weight_decay": 0.0},
+                 {"params": [p for n, p in model.named_parameters()
+                             if not (p.ndim <= 1 or n.endswith(".b")
+                             or any(k in n for k in ("psi", "theta", "alpha_raw", "gamma_raw", "inj_gate")))],
+                  "weight_decay": config.weight_decay}],
                 lr=0,  # Needs to be set by scheduler
                 weight_decay=config.weight_decay,
                 betas=(config.beta1, config.beta2),
@@ -289,7 +298,7 @@ def init_train_state(
     return train_state
 
 
-def save_train_state(config: PretrainConfig, train_state: TrainState):
+def save_train_state(config: PretrainConfig, train_state: TrainState, extra: Optional[dict] = None):
     if config.checkpoint_path is None:
         return
 
@@ -300,6 +309,8 @@ def save_train_state(config: PretrainConfig, train_state: TrainState):
         "optimizer_states": [optim.state_dict() for optim in train_state.optimizers],
     }
 
+    if extra:
+        state.update(extra)
     state["rng_state"] = torch.random.get_rng_state()
     if torch.cuda.is_available():
         try:
@@ -466,6 +477,13 @@ def load_checkpoint(train_state: TrainState, config: PretrainConfig, rank: int):
     if step is not None:
         train_state.step = int(step)
 
+    # [LT 2026-08-26] 재개: raw 가중치가 있으면 그것으로 덮어쓰고, EMA shadow 는 train_state 에 보관해 launch 에서 복원
+    if isinstance(checkpoint, dict) and checkpoint.get("raw_model_state_dict") is not None:
+        train_state.model.load_state_dict(checkpoint["raw_model_state_dict"], strict=config.load_strict)
+        if rank == 0:
+            print("Resumed from raw (non-EMA) weights")
+    train_state.ema_shadow_to_restore = checkpoint.get("ema_shadow") if isinstance(checkpoint, dict) else None
+
     # Reset carry since we do not serialize it
     train_state.carry = None
 
@@ -702,6 +720,14 @@ def evaluate(
 
         # Reduce to rank 0
         if metric_values is not None:
+            # [LT] eval 지표 콘솔 출력
+            if rank == 0:
+                _mv = metric_values.cpu().numpy()
+                for _si, _sn in enumerate(set_ids):
+                    _d = dict(zip(metric_keys, _mv[_si]))
+                    _n = max(_d.get("count", 1.0), 1.0)
+                    print(f"[EVAL] step {train_state.step}  set {_sn}  "
+                          f"acc {_d.get('accuracy',0)/_n:.4f}  exact {int(_d.get('exact_accuracy',0))}/{int(_n)}", flush=True)
             if world_size > 1:
                 dist.reduce(metric_values, dst=0)
 
@@ -885,6 +911,11 @@ def launch(hydra_config: DictConfig):
             print("Setup EMA")
         ema_helper = EMAHelper(mu=config.ema_rate)
         ema_helper.register(train_state.model)
+        _sh = getattr(train_state, "ema_shadow_to_restore", None)   # [LT 2026-08-26]
+        if _sh:
+            ema_helper.load_state_dict({k: v.to(next(train_state.model.parameters()).device) for k, v in _sh.items()})
+            if RANK == 0:
+                print("Restored EMA shadow from checkpoint")
 
     # Progress bar and logger
     progress_bar = None
@@ -900,7 +931,13 @@ def launch(hydra_config: DictConfig):
             settings=wandb.Settings(_disable_stats=True),
         )
         wandb.log({"num_params": sum(x.numel() for x in train_state.model.parameters())}, step=0)
-        save_code_and_config(config)
+        # [LT] 원 레포 버그: save_dir 인자 누락 — checkpoint_path 로 보정
+        if config.checkpoint_path is not None:
+            os.makedirs(config.checkpoint_path, exist_ok=True)
+            try:
+                save_code_and_config(config, config.checkpoint_path)
+            except Exception as e:   # [LT] ListConfig 등 직렬화 불가 필드 — 저장은 비필수
+                print(f"[LT] config 저장 생략 ({type(e).__name__})", flush=True)
 
     # Training Loop
     for _iter_id in range(total_iters):
@@ -929,6 +966,13 @@ def launch(hydra_config: DictConfig):
             if RANK == 0 and metrics is not None:
                 wandb.log(metrics, step=train_state.step)
                 progress_bar.update(train_state.step - progress_bar.n)
+                # [LT] wandb 비활성 환경용 콘솔 로그 — 배치 exact 는 개수/카운트로 표시
+                if train_state.step % 200 == 0:
+                    # 주의: train_batch 가 이미 count 로 정규화한 비율을 준다 — int() 절단 금지
+                    _m = {k.split("/")[-1]: (v.item() if hasattr(v, "item") else v) for k, v in metrics.items()}
+                    _ex = _m.get('exact_accuracy', 0.0)
+                    print(f"[LT] step {train_state.step}  lm_loss {_m.get('lm_loss', float('nan')):.4f}  "
+                          f"acc {_m.get('accuracy', 0):.4f}  exact {_ex:.4f} (~{_ex*128:.0f}/128)", flush=True)
 
         ############ Evaluation
         if eval_loader is not None and eval_metadata is not None:
@@ -976,7 +1020,11 @@ def launch(hydra_config: DictConfig):
                 # 临时拷贝一个带 EMA 权重的 state 来保存
                 ts_to_save = copy.deepcopy(train_state)
                 ts_to_save.model = ema_helper.ema_copy(ts_to_save.model)
-                save_train_state(config, ts_to_save)
+                # [LT 2026-08-26] 재개용 원시(raw) 가중치 + EMA shadow 를 함께 저장 — model_state_dict 는 EMA 그대로(평가·호환)
+                save_train_state(config, ts_to_save, extra={
+                    "raw_model_state_dict": {k: v.detach().cpu() for k, v in train_state.model.state_dict().items()},
+                    "ema_shadow": {k: v.detach().cpu() for k, v in ema_helper.state_dict().items()},
+                })
                 del ts_to_save
             else:
                 save_train_state(config, train_state)
