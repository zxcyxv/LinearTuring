# 다음 세션 계획 — 폭 축소 · 정합 깊이 (2일차 마감 시점)

> 결론 먼저: 폭은 널 손잡이로 판정 기울음, 깊이는 μ 와 결합해야만 정합적으로 늘어남.
> 주행 중: d=832 SwiGLU 완주판 (체크포인트·자동 분해 축적 중 — 내일 첫 판독).

## 판정 근거 (당일 실측)

| 관측 | 수치 | 함의 |
|---|---|---|
| 폭 널 | d768→832(+19%) 초반 페이스 무차 (0.659 vs 0.660 @4.5k) | 폭은 현 병목 아님 |
| 깊이 실패 (τ 단독) | τ=2: 셀 0.29@6k (τ=1 은 0.66) | e^{λτ} 그래디언트 잡음화 — DAMPING.md |
| URM 격차 | 동일 파라미터(±3%)에서 0.66 vs ~0.87 @5k | 총깊이 6배(48 vs 8/세그)가 유력 인자 |
| 조기 평형 | 세그 ~3 고정점, 평형 질만 상승 | 평형 질의 천장 = 유닛 표현력 |

## 프로그램 (순서 고정)

1. **μ 프로브** (DAMPING §4): 동일 가중치에서 τ8/16 × μ∈{0,.3,.7} 그래디언트 SNR — 5분, 학습 아님
2. μ 확정 시 **폭 축소 + 깊이 이전**: d=512 로 좁히고(−8M) μ + 스텝 16~32(τ=2~4) —
   폭 예산을 깊이로 전환. 여기가 "정합적 깊이"의 본체
3. **K-커널** (KCYCLE §7 저위험 진입로) 적층 — K 는 μ 와 한 몸 (KCYCLE §3 회계:
   유닛 반복 유지 필수, dt 축소는 평균화 함정)
4. 병행 대조: **R=16·τ=1** (스텝 수 vs τ 분리 판정 — τ=2 실패 원인의 확정 증거)

각 단계 판정 기준: SwiGLU d768 (0.659@4.8k) / d832 완주 곡선 대비 전선·평형 질.

## 미결

- d832 완주 판독 (폭 널 가설의 완주 검증) · URM 재실행 (고친 프린트로 배치 exact 급등 관찰)
- 스도쿠 판 λ 실측 (attractor.py 이식) — μ 조준값의 근거
