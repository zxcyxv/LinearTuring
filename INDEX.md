# 연구 색인 — 무엇을 알아냈고, 무엇을 채택했나

> 이 파일이 진입점이다. 상세는 각 문서로.
> **상태**: 스도쿠 이식 진행 중 (URM 하네스, REPORT §11.7 · PLAN.md). d832 완주판 주행 중.

## 문서 지도

| 파일 | 역할 |
|---|---|
| `INDEX.md` | 이 파일 — 결론·채택 원장 |
| `REPORT.md` | 실험 서사 (1일차 §1–10 · 2일차 §11) |
| `THEORY.md` | 명제 26개, [정리]/[실측]/[미결] 구분 |
| `NHOP.md` | n-홉 인컨텍스트 검색 문제 분석 (아키텍처 무관) |
| `SUDOKU.md` | 다음 단계 설계 사양 |
| `DAMPING.md` | 감쇠 구조·μ 마찰항 설계 메모 (미실행) |
| `KCYCLE.md` | K-순환 벡터장 — 깊이 축 설계 메모 (μ 와 결합 필수, 미실행) |
| `PLAN.md` | 다음 세션 계획 — 폭 축소·정합 깊이 프로그램 |
| `RESULTS.md` | 학습 자동 원장 |

## 한 문단 요약

비선형성은 "입력→부호 그래프" 1회 사상에 있고(MNIST 97.6%, 아핀 대조 97.06%), 상태 의존
게이팅 `A(h)`는 순차 과제(CA)에서만 필수이며 그 용량은 폭이 정한다(날카로운 문턱).
`noov`는 채널 비혼합이라 정확히 "공유 부호 전도도 장의 d종 반응-확산계"이고(명제 21),
균일 상태 분산관계로 **진동형 튜링 불안정이 실측**되어 RD 로 재분류됐다(명제 23 —
1일차 "RD 아님" 철회). 계산은 화물(상태)이 아니라 **라우팅(장)**에 있고(이식 개입, 명제 25),
학습은 고정점이 아니라 "r=R 에 읽히는" **시간 경계값 문제**를 푼다(명제 24; 전 설정 ρ>1,
DEQ 불가). 값 경로를 **sheaf `(W,Wᵀ)`**로 바꾸면 noov 의 분석 성질(에너지 귀속·조건부
선형)을 보존한 채 CA 0.073→0.503 — full(0.666)과 1.3배 차만 미결. 경계 W_O 와는 보완재.
ICL(n-hop) 축은 부정적: n=1 은 동급 트랜스포머가 더 빠르고, 순수 n=2 는 어떤 아키텍처도
못 배운다(ln 7 벽).

## 핵심 수치

| | 값 | 의미 |
|---|---|---|
| MNIST | 97.62% | 활성화 0개, 26.5K 파라미터 |
| 아핀 재귀 대조 (frozenA_g0) | 97.06% | 비선형성은 x→A 1회가 전부 |
| λ(randR) / ρ(블록, 학습 후) | +1.74 / 전 설정 >1 | 이상한 끌개 · 고정점 의미론 없음 |
| CA 상전이 | A자유 d256: 0.997 / A고정: 0.000 | 게이팅은 폭·깊이로 대체 불가 |
| 분산관계 (noov) | 균일 −0.05 / 띠 +2.78, \|Im\| 7.5 | 진동형 튜링 불안정 [실측] |
| 장 이식 | 장 0.60·0.85 vs 상태 0.06·0.52 | 계산은 라우팅에 [개입 인과] |
| sheaf 사다리 (포화 4k step) | **0.9498** vs full 0.9886 | 분석가능 계열 격차 소멸 — 잔여는 포화 속도 (오차율 4.4배) |
| n=2 hop | 전 설정 loss→ln 7 | 순수 다중홉 검색은 학습 불능 |

## 변종 원장 — 채택 / 기각 / 계류

### 채택 (스도쿠 레시피)
| 변종 | 근거 |
|---|---|
| **A 자유** (상태 의존 게이팅) | 순차 과제 유일 계산원. 2×2+폭/깊이 대조 삼중 확정 |
| **rand_R** | 정밀화 저항 .286→.947 · 시간 외삽의 유일 수단 (명제 24) |
| **sheaf 값 경로 `(W,Wᵀ)`** | noov 5.4배(0.073→0.392), Λfull 0.503. 간선별 에너지 귀속·조건부 선형 보존. 초기화=noov(블록항등). 사전 등록 3연속 적중 |
| **경계 W_O (수축 init ρ=e^{−λ̂})** | 혼합과 **보완재** (2×2: sheaf 3.0× / full 5.1×). 학습 후에도 ρ(W_O)≈0.60 수축 유지. **적용 범위: 고정 그래프 순차 갱신 (hop 계열 무효)** |
| **Λ full** (sheaf 위에서) | 0.392→0.503 |
| **b 유지** | 동차성 파괴 유일 항. CA 에서 c 항 단독 셀 0.766 (명제 25) |

### 기각·강등
| 변종 | 근거 |
|---|---|
| 커널 lr ×30 | MNIST 한정 +0.2%p. hop 에선 ×1 동일 — 과제가 요구하면 ×1 로도 α 40배 이동 |
| A 고정(계산용) · γ 조절 · ψ 감시 · 1/t 정규화 · τ 연장(A고정 구제) · 잡음 커리큘럼 · noov 단독 | 1일차 확정 (REPORT §4–8) |
| τ 증깊·경계 W_O (hop 구제용) | n=2 에 전부 무효 — 벽은 과제 클래스(내용 주소지정)와 커리큘럼 부재 |
| 점별 고유모드 분석 | 기저 선택(균일/끌개)에 결론이 뒤집힘 — 카오스에서 구조적 무효. 장 수준 지표로 교체 |
| DEQ/IFT (현 좌표) | 학습 후 전 설정 ρ(블록)>1 (명제 24) |

### 계류
| 변종 | 상태 |
|---|---|
| sheaf+plain 셀 | 재부호화/수축 몫 분리 (적응 트리의 다음 셀) |
| 감쇠 시계 (S 의 재귀축 취급) | 1일차 그대로 — fast-slow(FHN) 후보 |
| 공변 리아푸노프 벡터 | 모드-읽기 정렬 귀속의 유일한 정공법 |
| hop 멀티태스크/커리큘럼 | 홉 용량 곡선용. 우선순위 낮음 (ICL 은 비교우위 축 아님) |
| **μ 마찰항** (방향 감쇠) | DAMPING.md — 스펙트럼 평행이동 λ→λ−μ, τ 확장의 BPTT 손잡이. 스도쿠 τ=2 실패에서 도출, 미실행 |
| **K-순환 벡터장** (물리 레이어) | KCYCLE.md — τ 없는 깊이. 저위험 진입로 = K-커널(비용 ~0). 결합 무선례가 유보점, 미실행 |

## 스도쿠 확정 레시피 (2일차 갱신)

```
Model 1 + A 자유 + rand_R + sheaf(W,Wᵀ, 블록항등 init) + Λ full
        + 경계 W_O(수축 init ρ=e^{−λ̂}) + b 유지 + 커널 lr ×1
해석: 장 추적(fields.py) — d_t = 부호 제약 카운트, commitment depth = 칸 확정 시점
폭: 문턱 여유 확보 · 학습 중 λ(FTLE) 로깅
```

## 실험 설계 규율 (2일차 추가)

부품마다 ①메커니즘 주장 ②반증 실험 ③상호작용 상태를 원장에 등록한다.
주장이 겹치는 쌍만 2×2 로 분리, 나머지는 최고 구성 위 leave-one-out.
가산 예측을 먼저 등록하고 어긋날 때만 상호작용을 조사한다(§8.8 전례).
다음 셀은 결과가 정한다(적응 트리). **부품 가치는 과제 클래스에 붙는다** — 적용 범위를 함께 적는다.

## 자기정정 기록 (교훈 순)

1. 에너지 증가 ≠ 확산 반증 (튜링 패턴의 정의) → 항별 수지로 교체
2. 비정규화 라플라시안의 국소화 인공물 → 정규화 + 참여비
3. `3^R` 차수 논증은 Model 0 전용 (정규화가 0차 동차로 만듦)
4. **날카로운 문턱 아래에서는 모든 폭이 같아 보인다** — 이 연구에서 가장 반복된 함정
5. 명제 9 증명 결함 (전역 vs 토큰별 감쇠) → Stuart–Landau 재식별
6. k*=1.12 "튜링 파장 선택" → θ 초기화 유산 (2일차: 닫힌 심볼이 이유를 유도로 닫음)
7. SQ-경도 "구조적 불능" → 폭 요구량으로 완화 (d=256 이 풂)
8. "반응-확산 아님" 판정이 성급 — 잘못된 지파(GM)를 기준으로 클래스 전체를 기각했다.
   분산관계 실측으로 재분류. **측정은 전부 살아남았고 죽은 것은 판정뿐**
9. 점별 고유모드는 기저 선택(균일 vs 끌개)에 결론이 뒤집힌다 — 카오스에서 무효.
   안정한 것은 장 수준 지표뿐 (좌절도·분할·commitment·이식)
10. OneCycle 은 total_steps 에 궤적이 종속 — step 예산이 다른 두 런은 비교 불가
    (frozen n=1: 4k 예산 평평 / 16k 예산 step 3250 그록킹)
11. 기준선 실패는 먼저 자기 구현을 의심하라 — TF "실패"는 위치 임베딩 스케일(1.0 vs 0.02)
    버그였다. 문헌과 충돌하면 설정 오류가 기본 가설
12. 로깅 패치도 스모크 검증 먼저 — eval 출력의 스코프 버그가 15.6k 런을 죽였고(RANK),
    int() 절단이 배치 exact 관찰 전체를 무효화했다 (두 사고 모두 "출력만 바꾸는" 패치였음)
13. 총깊이 회계: K(유닛 내 사상 수)를 늘리며 유닛 반복을 줄이면 총 깊이 불변 — 허수.
    흐름 모델의 깊이는 τ 에 지수 비용이 붙으므로 μ 없이 못 늘린다 (KCYCLE §3)
