# 스도쿠 — 서브프로젝트의 진짜 목표 (준비 메모)

> 상태: 레퍼런스 대기 중. 구현 착수 전 설계 메모.
> 상위 목표: Transformer 디코더 중간에 이 재귀 구조를 삽입, 가벼운 연산으로 미래 N개
> 예측 은닉상태를 자기회귀 생성 → 이후 디코더가 concat 은닉상태를 캐주얼 마스크로
> 병렬 소비. "기존보다 가볍고, 토큰 간 그래디언트가 안 끊기고, 더 정확할 가능성."

## 왜 스도쿠인가 (지금까지의 결과로부터)

- **(b)형 과제**: 제약 그래프 고정 + 상태 비선형 갱신(후보 소거). A 고정(1회 게이팅)으로
  원리적으로 불가 → 재귀의 상태 의존성이 실제로 필요한 첫 과제.
- CA 실험이 사실상 최소 모형이었다. 폭-라운드 교환(d=64→4라운드, d=192→8라운드)이
  스도쿠 설계의 사전 지식.
- MNIST 는 잘못된 목표였음이 증명됨(frozenA_g0 = 아핀 재귀로 97.06%).
- **과제 클래스: 고정 그래프 순차 갱신** (제약 그래프는 위치가 결정, CA 동형 — NHOP.md §4.1).
  2일차 hop 부정 결과(내용 주소지정 클래스)와 무관하고, W_O·sheaf 의 검증 범위 안이다.
- **동역학 대응 (불합의형 튜링)**: 색칠 = 반강자성 Potts 바닥상태. 균일(동일 값) 모드는 죽고
  분화 모드만 자라야 한다 — 명제 23 이 정확히 그 조건. 등록 예측: 스도쿠에서 a<0 비율이
  MNIST(~48%)보다 높아지고, 흡수구 상한(1일차 5배 헐거움)이 타이트해진다 (양성 간선 상쇄 감소).

## λ = BPTT 안정성 상수 (상위 LM 목표와의 다리)

역전파 야코비안 곱 노름 ~ e^{λτ}.
- λ=+1.7 (기본): N스텝 롤아웃 그래디언트 e^{1.7N/R} 폭발 → 미래 N개 생성에 부적합.
- λ≈0 (frozenA8 준주기): 보존 — "안 끊기고 안 터지는" 지점.
- 이미 아는 λ 손잡이: A 상태 의존성(±), Λ 스펙트럼, τ.
- **스도쿠의 긴 재귀가 이 문제를 먼저 노출시킨다 → 안정성 연구의 자연 시험대.**

## 설계 결정 (현재 결과가 시사)

| 항목 | 결정 | 근거 |
|---|---|---|
| A | 자유 (상태 의존) 필수 | (b)형 과제, frozen 은 τ 에도 평평 (0.0000→0.0007) |
| **값 경로** | **sheaf (W,Wᵀ), 블록항등 init + Λ full** | 2일차 채택 (명제 26): noov 분석 성질 보존, CA 0.503. 경계 W_O 와 보완재 (2×2) |
| dt | rand_R 무작위화 기본 | 정밀화 저항 .286→.947, 비용 0.3%p |
| 커널 lr | **×1** (×30 강등) | hop 에서 ×1 동일 — 과제가 요구하면 ×1 로도 α 40배 이동 |
| 커널 좌표 | **헤드별 좌표계 분담**: 행-거리 / 열-거리 / 박스-소속 | 사양의 2D 파수 벡터의 자연 확장. 다중 헤드가 처음으로 구조적 의미를 가짐 |
| 폭 | d\*(라운드) 교환 곡선 고려. 9×9 는 전파 라운드가 많음 → d 여유 있게 | CA 폭 문턱 (222K/395K 사이) |
| 입력 | T=81, vocab=10 (0=빈칸), 토큰별 9-way 로짓 (pool=False) | ca_task 와 동일 골격 |
| 커리큘럼 | 주어진 칸 수(givens)로 난이도 제어 | parity/CA 의 그록킹·문턱 경험 |
| **경계 사영 W_O** | `h←W_O h`, **수축 초기화 ρ=e^{−λ̂}·I** (모드 절제 우승: 0.666 vs plain 0.414) | λ̂ 는 학습 초반 FTLE 실측으로 갱신 가능. 직교(exp skew)는 이론 보장 필요시 대안 |
| 해석 | **장 추적(fields.py)**: d_t = 부호 제약 카운트(hidden-single 축), commitment depth = 칸 확정 시점, 장 이식 = 인과 검증 | 2일차 교리 (명제 21·25) |
| 안정성 로깅 | 학습 중 λ(FTLE) 추적 — 스도쿠에서 λ 가 어디로 가는지가 상위 목표의 데이터 | attractor.py 재활용 |

## 열린 결정 — 감쇠 시계 축 (사용자 제기)

현재 구조는 매 마이크로스텝 A 를 처음부터 재계산 (= 스캔을 S=0 에서 새로 돎).
재귀축에서 감쇠가 누적되는 대상이 없다. 캐주얼 스캔 판에서는 결정 필요:

| | 감쇠 시계 | 구현 |
|---|---|---|
| A. 매 스텝 리셋 (현재) | 스텝마다 0부터 | 기본 |
| B. 계속 이어감 | 재귀 전체 누적 — S 가 두 번째 재귀 상태 | `A_eff^(r) = (1−β)A(h^(r)) + β A_eff^(r−1)` (fast-weight EMA) |
| C. 블록 경계 리셋 | 블록 내 누적, W_O 경계에서 플러시 | B + 경계 리셋 |

**B 의 이론적 의미**: 시간척도 1/α 의 느린 변수 도입 = FHN 의 회복 변수 w 에 해당.
사양이 원한 fast–slow 스파이킹 동역학이 원리적으로 가능해지는 최소 수선 후보.
단 상태공간이 (h, S) 로 확장 → 카오스·흡수구 분석 재수행 필요.

## URM 인터페이스 채택 표 (2일차 확정 — refs/URM, arXiv 2512.14693)

| 컴포넌트 | 판정 | 근거 |
|---|---|---|
| 학습 하네스 (pretrain.py: 상각 carry·detach 세그먼트·세그먼트별 손실) | **채택** | detach 가 e^{λτ} 문제의 구조적 해법 — 그래디언트 노출이 블록 1개로 고정 |
| 옵티마이저 | **AdamATan2** (기본값 확인: use_muon=False) + 퍼즐 임베딩 전용 희소 옵티마이저 | Muon 은 정체 파악 전까지 보류 |
| EMA (rate 0.999) | **채택** | 스도쿠 스크립트 기본 |
| 데이터 증강 (숫자 치환·전치·밴드/스택 셔플) | **그대로 복제** | 비교 가능성 |
| ACT / q_head | **제거** — loops 고정 (halting 자동 소진, q 손실 스텁) | 사용자 결정 |
| 삼중 루프 (loops×H×L) | **이중으로 축소**: 경계 W_O 블록(외곽, detach·감독·재부호화) × 내부 R 재귀 | H_cycles(no-grad 워밍업)는 carry 가 흡수 |
| **input injection** | **경계 주입 기본**: h ← W_O h + Embed(x) | 실측: 학습 깊이 안 입력 복호 0.86–1.0 (안 잊음), 밖 0.55 (카오스가 지움) + detach 가 블록 간 보존 학습을 차단 → 주입 필요. 절제 셀: 무주입 / 매스텝 주입 |
| 손실 | stablemax CE 여부 arch cfg 확인 후 결정 | losses.py 에 활성 코드 존재 |

## 열린 결정 (레퍼런스 후)

- 벤치마크: URM 스크립트 기준 Sudoku-Extreme 파이프라인 (build_sudoku_dataset.py) — 4×4 워밍업 불요 판단
- 기준선: URM 원본 (동일 하네스·동일 예산) — 코어만 교체한 1:1 비교
