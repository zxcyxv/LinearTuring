# n-홉 인컨텍스트 검색 — 문제 자체에 대하여

> 이 문서는 **문제만** 다룬다. 특정 아키텍처를 전제하지 않는다.
> 왜 `n=1` 과 `n≥2` 사이에 질적 도약이 있는지, 그리고 이 문제가
> parity·셀룰러 오토마타 같은 기존 탐침들과 어떤 난이도 축에서 다른지를 정리한다.

---

## 1. 문제 정의

알파벳 `Σ`, `|Σ| = V`. 인스턴스마다:

1. `S ⊆ Σ`, `|S| = N` 을 뽑는다.
2. `S` 위의 치환 `π` 를 뽑는다 (길이 `N` 의 단일 순환).
3. `π` 의 **그래프를 토큰열로 제시**한다 — 쌍 `(s, π(s))` 를 무작위 순서로:
   ```
   s_{σ1} π(s_{σ1})   s_{σ2} π(s_{σ2})   …   s_{σN} π(s_{σN})   q_1 … q_Q
   └──────────── 2N 토큰 ────────────┘        └── 질의 ──┘
   ```
4. 각 질의 `q_i ∈ S` 에 대해 **`π^n(q_i)` 를 출력**한다.

핵심 조건: **`π` 는 인스턴스마다 새로 뽑힌다.** 따라서 가중치에 저장할 수 있는 것은
`π` 자체가 아니라 "제시된 그래프를 읽고 `n` 번 따라가는 절차" 뿐이다.

`n=1` 은 표준 induction head / 연상 회상이다. `n≥2` 가 이 문서의 관심사다.

### 1.1 우연 수준과 채점

정답은 항상 문맥 안의 `N` 개 심볼 중 하나이므로 우연 수준은 `1/N` 이다
(모델이 "문맥 안 심볼로 제한" 을 배웠다는 전제. 배우기 전에는 `1/V`).
질의별 정확도와 "한 인스턴스의 `Q` 개 질의를 전부 맞춤"(전체일치)을 나누어 보는 것이 좋다.
후자가 `p_query^Q` 보다 유의하게 낮으면 **질의 간 간섭**이 있다는 신호다.

---

## 2. 왜 `n≥2` 가 질적으로 다른가

`n` 을 1에서 2로 올리는 것은 "같은 일을 한 번 더" 가 아니다. 최소 다섯 가지가 동시에 바뀐다.

### 2.1 주소가 데이터가 된다 (간접 참조의 도입)

`n=1`: 질의 `q` 는 **입력에 존재하는 토큰**이다. 찾을 대상의 주소가 입력에 직접 주어진다.

`n≥2`: 두 번째 조회의 주소는 `π(q)` 인데, 이것은 **입력에 주어진 것이 아니라 계산된 값**이다.
즉 1홉의 *출력* 이 2홉의 *주소* 로 쓰인다. 이것이 포인터 추적(pointer chasing)이며,
"값 공간" 과 "주소 공간" 이 정렬되어 있어야만 성립한다.

> **`n=1` 은 키→값 사상 하나만 요구한다. `n≥2` 는 추가로 값→키 정렬을 요구한다.**
> 검색된 값이 다시 검색어로 기능할 수 있어야 한다. 이것은 표현 수준의 제약이며
> `n=1` 에는 존재하지 않는다.

### 2.2 순차 의존성 — 병렬화가 원리적으로 막힌다

`π^n(q)` 의 계산 그래프는 길이 `n` 의 **사슬**이다. `k` 번째 조회는 `k−1` 번째 결과 없이는
시작할 수 없다. 이것은 구현의 문제가 아니라 문제의 구조다.

대조: parity `x_1 ⊕ … ⊕ x_T` 는 결합법칙이 성립해 깊이 `log T` 의 이진 트리로 접힌다.
`n` 홉 추적에는 그런 접기가 **일반적으로** 없다 (단 §3.2 의 포인터 배가는 예외).

### 2.3 오차가 곱셈적으로 누적된다

홉당 검색 성공률을 `1−ε` 라 하면 `n` 홉 성공률은 대략 `(1−ε)^n` 이다.
그런데 더 나쁜 것은 **실패의 성질**이다: 1홉에서 잘못된 심볼을 얻으면
2홉은 *올바르게 수행되어도* 틀린 답을 준다. 부분적으로 맞은 주소는
부분적으로 맞은 답을 주지 않는다 — 완전히 다른 곳을 가리킨다.

> **`n=1` 은 부드러운(soft) 검색을 허용한다. `n≥2` 는 사실상 허용하지 않는다.**
> 후보들에 대한 가중 평균으로 얻은 "흐릿한" 심볼은 다음 홉의 주소로 쓸 수 없다.
> 중간 결과가 **거의 이산적으로 깨끗해야** 한다.

이것이 `n=1` 에서 `n=2` 로의 성능 붕괴가 완만하지 않고 절벽인 이유다.

### 2.4 부분 점수가 없다 (저차 상관의 부재)

`π` 가 무작위 치환이므로, `π^n(q)` 와 입력의 어떤 **개별** 토큰 사이의 상관은
`n≥2` 에서 사실상 0이다. 목표를 맞히려면 합성을 실제로 수행해야 하고,
"조금 맞추는" 중간 지대가 없다.

이는 parity 의 SQ-경도 논의와 같은 성격이다 — 목표가 여러 입력의 결합에만 의존하고
저차 통계에는 신호가 없으면, 경사 기반 학습은 신호를 찾을 때까지 평지를 헤맨다.
**따라서 학습 곡선은 점진적 상승이 아니라 그록킹(평지 → 급등) 형태를 띨 것으로 예상된다.**
평평한 구간을 보고 "학습 불가" 로 판정하면 안 된다.

### 2.5 상태 용량이 두 번 요구된다

`n=1` 을 풀려면 `N` 개의 쌍을 어떤 형태로든 접근 가능하게 유지해야 한다 (검색 용량).
`n≥2` 는 거기에 더해 **중간 결과를 유지**하면서 같은 검색 구조를 재사용해야 한다.
순차적 상태를 가진 모델에서 이는 상태 예산의 경쟁을 뜻한다.

정보이론적 하한: 제시된 그래프를 상태 `s` 에 요약해 나중에 임의 질의에 답하려면
`s` 는 사상 `π` 를 사실상 인코딩해야 하므로 `Ω(N log N)` 비트가 필요하다.
`N` 이 커질 때 이것이 실질적 병목이 된다.

---

## 3. 계산 복잡도 관점

### 3.1 포인터 추적의 라운드 하한

이 문제는 **포인터 추적(pointer chasing)** 의 인스턴스다. 통신 복잡도에서 고전적으로
연구된 문제이며, 핵심 결과의 형태는:

> `k` 라운드 포인터 추적은 `k−1` 라운드의 (작은 메시지) 프로토콜로 풀 수 없다.

즉 **라운드 수 자체가 자원**이며 다른 자원으로 자유롭게 교환되지 않는다.
고정 깊이 아키텍처에서 "깊이" 가 곧 "라운드" 라면, `n` 이 커질수록 깊이 요구가 따라 커진다.

> ⚠ 정확한 정리 진술과 조건(메시지 길이, 결정/무작위)은 원 논문을 확인해야 한다.
> 여기서는 "라운드가 교환 불가능한 자원" 이라는 정성적 함의만 사용한다.

### 3.2 예외: 포인터 배가 (pointer doubling)

`π^n` 은 반복 제곱으로 `O(log n)` 라운드에 계산할 수 있다:
```
π → π² → π⁴ → π⁸ → …
```
단 이 경로는 매 라운드에서 **사상 전체**(`N` 개 항목)를 유지·갱신해야 한다.
따라서 라운드를 줄이는 대가로 **폭(상태 용량)** 을 지불한다.

> **`n` 홉 문제는 깊이–폭 교환을 명시적으로 내장한 문제다.**
> 순차 추적: `n` 라운드 × `O(1)` 상태.  배가: `log n` 라운드 × `O(N)` 상태.
> 어떤 모델이 어느 쪽 해를 찾는지는 그 모델의 귀납 편향을 드러낸다.

이 사실은 실험 해석에 직접적이다 — 폭을 올려서 `n` 홉이 풀렸다면
그것은 "용량이 늘어서" 가 아니라 **모델이 배가 알고리즘 쪽으로 옮겨갔을** 가능성이 있다.

### 3.3 고정 깊이 아키텍처에 대한 함의

트랜스포머 계열에 대해 `k`-홉 유도(k-hop induction)의 깊이 요구를 다룬 결과들이 있으며
(예: 병렬 계산·로그 깊이 관점의 표현력 분석), 대략적인 그림은:

- 층 수가 홉 수에 대해 **최소 로그** 로 자라야 한다 (배가를 쓸 수 있을 때)
- 사고 사슬(chain-of-thought)처럼 중간 결과를 **출력으로 되먹이면** 깊이 제약을 우회할 수 있다
  — 되먹임이 곧 추가 라운드이기 때문

> ⚠ 구체적 정리와 상수는 원 문헌 확인 필요. 여기서는 방향만 취한다:
> **고정 깊이 모델에서 홉 수는 자유 변수가 아니다.**

---

## 4. 난이도 클래스 — 기존 탐침들과의 비교

네 개의 축으로 나누면 차이가 선명해진다.

| 과제 | ① 프로그램이 문맥에? | ② 순차 라운드 | ③ 상호작용 그래프 | ④ 오차 누적 |
|---|---|---|---|---|
| MNIST 분류 | 아니오 (고정) | ~1 | 위치 고정, 정적 | 없음 |
| parity (길이 `T`) | 아니오 (고정) | `log T` 로 접힘 | 전역, 정적 | 없음 (결합적) |
| CA rule `R`, `k` 스텝 | **아니오** (규칙 고정) | 정확히 `k` | **위치가 결정, 정적** | 셀별 독립 |
| 연상 회상 (`n=1`) | **예** (사상이 입력) | 1 | **내용이 결정** | 없음 |
| **`n`-홉 (`n≥2`)** | **예** | **`n` (또는 `log n`)** | **내용이 결정 + 추적** | **곱셈적** |

### 4.1 CA 와의 결정적 차이 — 그래프가 주어지느냐

CA 는 "고정 프로그램, 변하는 입력" 이다. 규칙 110 은 모든 인스턴스에서 같으므로
**규칙을 가중치에 구울 수 있다.** 변하는 것은 초기 상태뿐이다.
그리고 이웃 관계는 **위치가 결정**한다 — `i` 번 셀의 이웃은 언제나 `i±1` 이다.
즉 모델은 상호작용 그래프를 **공짜로 받는다.**

`n`-홉은 "변하는 프로그램, 변하는 입력" 이다. 사상 `π` 가 입력의 일부이므로
가중치에 구울 수 없고, 무엇이 무엇의 이웃인지를 **내용으로부터 추론**해야 한다.
게다가 그 그래프를 따라 `n` 번 **이동**해야 한다.

> **같은 라운드 수에서 `n`-홉은 CA 보다 엄격히 어렵다.**
> CA 는 라운드만 요구하고, `n`-홉은 라운드 + 그래프 추론 + 내용 주소지정을 함께 요구한다.
> CA 의 `k` 는 "몇 번 갱신하나", `n`-홉의 `n` 은 "몇 번 **찾아서** 갱신하나" 다.

### 4.2 parity 와의 차이 — 결합법칙

parity 는 라운드 수가 커 보이지만 결합적이라 `log T` 깊이로 접힌다.
그래서 parity 의 어려움은 **깊이가 아니라 저차 상관의 부재**(SQ-경도)에 있다.
`n`-홉은 저차 상관 부재를 parity 와 **공유하면서** 순차 의존성까지 갖는다.
두 종류의 어려움이 곱해진 문제다.

### 4.3 `n=1` 과 `n≥2` 의 위치

`n=1`(induction head, MQAR)은 이미 표준 벤치마크로 확립돼 있고,
특히 **효율적(부차이차) 시퀀스 모델의 검색 용량**을 가르는 시험대로 쓰인다 —
상태 크기가 작은 순환 모델이 `N` 이 커질 때 무너지는 지점을 잡아낸다.

`n≥2` 는 거기에 §2.1 의 값→키 정렬과 §2.3 의 이산성 요구를 얹는다.
따라서 `n=1` 을 푸는 모델이 `n=2` 를 못 푸는 것은 **정상적인 결과이지 이상 현상이 아니다.**

---

## 5. 아키텍처 무관 필요조건

어떤 모델이든 `n≥2` 를 풀려면 다음을 **모두** 갖춰야 한다.

1. **내용 주소지정** — 위치가 아니라 심볼 일치로 후보를 고를 수 있어야 한다.
2. **거의 이산적인 선택** — 후보들의 흐릿한 혼합이 아니라 하나를 뽑아야 한다.
   (부드러운 평균은 §2.3 에 의해 다음 홉에서 무너진다.)
3. **값→키 재주입** — 검색 결과가 다음 검색의 질의로 기능할 수 있어야 한다.
4. **`n` 라운드(또는 배가 시 `log n` 라운드 + `O(N)` 상태)의 순차 계산 예산.**
5. **`Ω(N log N)` 규모의 검색 용량** — 제시된 사상을 유지할 수 있어야 한다.

이 중 하나라도 없으면 `n≥2` 는 우연 수준에 머문다. 그리고 다섯 개가 서로 다른 자원이므로,
**실패했을 때 어느 조건이 깨졌는지를 따로 진단해야 한다.** 폭을 올려서 풀렸다면 5(또는 4-배가),
학습을 늘려서 풀렸다면 §2.4 의 평지 문제, 구조를 바꿔서 풀렸다면 1–3 중 하나였다는 뜻이다.

---

## 6. 실험 설계 시 주의점

문제 자체에서 따라 나오는 것들.

- **평지를 실패로 읽지 말 것.** §2.4 에 의해 그록킹이 예상되는 문제다.
  학습률 스케줄이 전체 스텝 예산에 종속되는 경우(예: OneCycle) 예산이 다른 두 런은
  비교 불가능하다 — **모든 비교군의 스텝 예산을 통일해야 한다.**
- **우연 수준을 `1/N` 로 잡을 것.** `1/V` 로 잡으면 "문맥 안 심볼로 제한" 만 배운 모델이
  성공한 것처럼 보인다.
- **전체일치를 함께 볼 것.** 질의별 정확도만 보면 질의 간 간섭을 놓친다.
- **`N` 과 `n` 을 독립적으로 쓸 것.** `N` 은 용량(조건 5), `n` 은 라운드(조건 4)를 압박한다.
  둘을 함께 올리면 어느 쪽이 병목인지 알 수 없다.
- **`n=1` 을 반드시 먼저 통과시킬 것.** `n=1` 이 안 되는 상태에서 `n=2` 를 비교하면
  모든 조건이 우연 수준에서 구별되지 않아 아무 정보도 얻지 못한다.
- **날카로운 전이를 가정할 것.** §2.3 의 곱셈적 오차와 §2.4 의 평지 때문에
  문턱 아래에서는 모든 설정이 똑같이 실패해 보인다.

---

## 7. 한 문단 요약

`n`-홉 인컨텍스트 검색은 **인스턴스마다 새로 주어지는 사상을 읽고 그것을 `n` 번 따라가는**
문제다. `n=1` 은 내용 주소지정 한 번이면 되지만, `n≥2` 는 (i) 계산된 값이 다음 조회의
주소가 되는 간접 참조, (ii) 병렬화되지 않는 순차 사슬, (iii) 흐릿한 중간 결과를 허용하지 않는
곱셈적 오차 누적, (iv) 저차 상관의 부재라는 네 가지 어려움을 **동시에** 갖는다.
셀룰러 오토마타가 "고정 규칙 · 위치가 정한 정적 그래프 · 정확히 `k` 라운드" 로 라운드 수
하나만을 압박하는 데 비해, 이 문제는 그래프 추론과 내용 주소지정을 라운드 요구 위에 얹는다.
parity 가 저차 상관 부재만을 압박하는 데 비해, 이 문제는 거기에 순차 의존성을 곱한다.
따라서 **같은 라운드 수에서 CA 보다, 같은 통계적 경도에서 parity 보다 엄격히 어렵다.**
유일한 완화 경로는 포인터 배가이며, 그것은 라운드를 폭으로 사는 거래이므로
**이 문제는 깊이–폭 교환을 문제 정의 안에 내장하고 있다.**
