# τ-정합 블록 적층 — 3층 재구조화 (설계 메모, 미실행)

> 제안자: 사용자 (2일차 말). τ=2 실패의 회계 분석에서 도출.
> 한 줄: **W_O 사영의 주기를 τ=1 에 고정**하고, 깊이는 [흐름 τ=1 → W_O] 블록의 개수 M 으로 쌓는다.

## 1. 구조

```
지금 (2층):   하네스 loop [= W_O = detach·감독·주입]  ──  τ 흐름 (R 스텝)
τ=2 실패판:   경계 간격만 2배 (분모 유지) → 무방비 구간 2배·수축 밀도 절반

제안 (3층):   하네스 loop (detach · 감독 · 주입 1회)          ← 최상위
                └─ [ τ=1 흐름(R 스텝) → W_O/SwiGLU 사영 ] × M   ← 중간: W_O 주기 = τ=1 고정
                      └─ 마이크로스텝 (dt=1/R)                  ← 최하위
세그먼트 총 흐름시간 = M,  BPTT 창 = M·R 스텝 (수축 M회 삽입됨)
```

## 2. 원리적 근거 3겹

1. **수축 초기화 보정 복원** — ρ=e^{−λ̂} 는 "τ=1 당 수축 1회"로 캘리브레이션된 상수
   (1일차 설계). ρ·e^{λ·1}≈1. τ=2 는 이 등식을 e^{2λ}·ρ≈5.5 로 깨뜨렸다.
2. **μ 마찰의 이산 구현** (DAMPING.md 의 검증된-부품 판): 블록당 그래디언트 배율
   e^λ≈5.5 → ρ_eff·e^λ (SwiGLU carry 0.6 → ~3.3, contract 0.18 → **~1.0 중립**).
   깊이 M 을 조건수 유지한 채 적층 — "외상 없이 현찰로 사는 깊이".
3. **주름 회계 정합** — 접기(표현력)는 τ 를 따라 쌓이고(총 τ=M), 비용을 블록마다 정산.

## 3. 판별 실험 (M=2 vs 죽은 τ=2 — 단독 변수 A/B)

M=2 는 τ=2 실패판과 연산·스텝 수 동일, **중간 W_O 하나만 추가**.
- M=2 가 τ=1 급으로 배우면 → "경계 밀도가 관건" 확정, M 이 확장 축으로 성립
- M=2 도 기어가면 → M·R 스텝 역전파 자체가 문제 → 연속 μ(DAMPING) 또는 블록 간 detach 로

기준선: SwiGLU d832 (셀 0.664@15.6k, exact 51/2048, 세그 활용 16/16) — 현재 최량 판.

## 4. 구현 스케치 (~15줄)

lt.py 에 `blocks_per_seg: M`:
```
h ← h + β·inj                        # 주입: 세그 시작 1회 (최상위)
for m in range(M):
    for r in range(R): micro-step    # τ=1 흐름
    h ← boundary(h)                  # W_O/SwiGLU — 블록마다 (τ 와 정합)
loss; detach                          # 하네스 (최상위)
```
순서 자유도(주입 위치·마지막 블록 경계 유무)는 발사 시 등록.

## 5. 확장 접점

- **K-순환(KCYCLE)과 직교 결합**: 블록마다 다른 벡터장 f_m → K=M 자연 실현
  (유닛 반복 유지 조건 §3 회계를 M 이 자동 충족)
- 경계 carry 이득을 e^{−λ} 에 조준(학습 가능)하면 M 대형화 시 그래디언트 중립 유지
- 스도쿠 판 λ 실측(PLAN 미결)이 그 조준값의 근거

## 6. 상태 — M=2 판별 실행됨 (2일차 말, 조기 중단)

d832·M=2, step ~2.4k 까지 실행 후 사용자 판단으로 중단. 같은 step 삼자 비교:

| step ~2000 | loss | 셀 |
|---|---|---|
| τ=1 (d832) | ~1.1 | ~0.55 |
| **M=2** | **1.95** | **0.17** |
| 죽은 τ=2 | 3.02 | ~0.1대 |

판독: **부분 복원** — 중간 경계 하나로 죽은 τ=2 대비 크게 앞섬 (경계 밀도 효과 실재)
그러나 τ=1 페이스 미달 (M·R=16스텝 BPTT 창 비용 잔존). step 1800 loss 스파이크 1회.
유보: 깊은 판의 느린 출발 후 역전 전례(포화 sheaf·d832) 있어 2.4k 조기 판정임.

다음 분기 (§3 의 두 번째 갈래): **블록 간 detach** (BPTT 창을 블록 1개=8스텝으로 고정,
깊이 M 은 carry 로만 — 하네스 deep supervision 의 세그 내부 재귀 적용) 또는 연속 μ 병용.
