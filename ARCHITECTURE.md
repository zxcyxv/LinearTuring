# 아키텍처

두 층으로 나뉜다. **코어**(`core/model1.py`)는 활성화 함수가 없는 어텐션 벡터장과 그 적분기이고,
**스도쿠 판**(`sudoku/lt.py`)은 코어의 한 스텝을 경계 사상·입력 주입과 묶어 블록으로 쌓고 URM 학습 하네스에 태운 것이다.

## 1. 코어 — 부호 어텐션 벡터장

상태 `H ∈ R^{T×d}` (토큰 `t` 마다 `h_t ∈ R^d`), 헤드 `m = 1..H`, `d_h = d/H`, `p = d_h/2`.
토큰마다 2D 위치 `pos_t ∈ Z²`, `Δ_tn = pos_t − pos_n`.

### 1.1 복소 주소 투영 — 진폭과 방향의 분리

```
z_t^(m) = W_C^(m) h_t ∈ C^p          W_C^(m) = [A_m; B_m] ∈ R^{d_h×d},  행-직교 (exp(skew) 의 상단 d_h 행)
ẑ_t     = z_t / (‖z_t‖₂ + ε)         ← 진폭을 버리고 방향만 남긴다
q̂_t = e^{+iψ/2} ⊙ ẑ_t,   k̂_t = e^{−iψ/2} ⊙ ẑ_t
```
`ẑ` 정규화가 이 모델의 첫 번째 설계 선택이다. 어텐션 계수가 `h` 의 크기에 무관해지고(0차 동차, THEORY §2),
그 결과 `|a_tn| ≤ 1` 이 구조적으로 보장된다 — softmax 도 `1/t` 정규화도 필요 없다.

### 1.2 감쇠 Gabor 커널 (위치 의존, 학습 가능)

```
U_j(t←n) = exp(−α_j ‖Δ_tn‖₁ − i θ_j·Δ_tn)        α_j = softplus(·) > 0,  θ_j ∈ R² (2D 파수)
a_tn^(m) = Σ_j Re[ conj(q̂_{t,j}) U_j(t←n) k̂_{n,j} ] ∈ [−1, 1]
```
`α` 는 수용장 폭, `θ` 는 위치 차에 따른 위상 회전, `ψ` 는 q/k 간 위상차(비대칭성). 구현은 `alpha_per_head=True`
로 감쇠를 헤드 단위로 묶어 `e^{−α_h‖Δ‖}` 를 `[H,T,T]` 마스크로 분리하고, 위상은 RoPE 처럼 q/k 를 각각 회전시켜
행렬곱 2회로 계산한다 (`model1.py: kernel_fast / attn_fast`, 느린 경로와 수치 동치).

**부호**: `a_tn` 은 음수가 될 수 있다. softmax 어텐션과의 근본 차이이며, 학습된 스도쿠 모델에서 피어 칸(같은 행·열·박스)
사이 간선이 음수(억제)로 조직되는 것이 관찰된다 (RESULTS §3).

### 1.3 벡터장

```
f_t = Λ h_t                                          선형항 (d×d 자유)
    + Σ_m W^(m)ᵀ Σ_n a_tn^(m) W^(m) h_n               sheaf 값 경로: v = W^(m)h, 출력 = W^(m)ᵀ(Σ a v)
    + ( Σ_m d_t^(m) ) b,   d_t^(m) = Σ_n a_tn^(m)     값 편향 (부호 행합 × 학습 벡터)
    − γ ‖h_t‖² h_t                                    4차 소산 (적분기에서 해석적으로 처리)
```
sheaf 값 경로의 수송 행렬 `W^(m)ᵀW^(m)` 은 대칭 PSD 다. 초기화는 블록 항등(헤드 슬라이스를 제자리로) 이라
시작점은 채널 비혼합이고, 학습이 헤드별 기저를 회전시키며 채널 혼합이 생긴다. `b` 는 벡터장에서 유일하게
동차성을 깨는 항이다 (THEORY §2).

### 1.4 적분기 — 분할과 4차항의 닫힌 해

```
Φ_τ(h) = h / √(1 + 2γ‖h‖²τ)                        ḣ = −γ‖h‖²h 의 정확한 해
h ← Φ_{dt/2}( Φ_{dt/2}(h) + dt · f(Φ_{dt/2}(h)) )    반응 반스텝 · 오일러 · 반응 반스텝
```
`Φ` 는 어디서나 실해석적이고 분모가 `1+` 로 시작해 전역 정의된다. 이것이 노름 층 없이 진폭을 통제하는 유일한 장치다:
학습된 모델에서 `‖h_t‖` 는 γ 가 정하는 반경 근처에 정착한다 (THEORY §5, RESULTS §2).

**활성화 함수는 없다.** `softplus` 두 곳은 α·γ 를 양수로 재매개화하는 것이고 `h` 에 닿지 않는다.
비선형성의 원천은 (i) `ẑ` 정규화의 분모, (ii) `a(h)` 가 `h` 로부터 매 스텝 다시 계산된다는 것, (iii) `Φ` 의 반경 포화, 셋뿐이다.

### 1.5 코어 옵션 (model1.py)

| 인자 | 채택값 | 대안 |
|---|---|---|
| `sheaf` | True — 값 경로 `(W, Wᵀ)` | `use_ov` 자유 `W_OV` / `noov` 블록 항등 |
| `lam_mode` | `full` | `diag`, `none` |
| `alpha_per_head` | True (고속 경로) | 채널별 α |
| `boundary_wo` | 스도쿠 판에선 미사용 (파라미터만 존재) | 흐름판(R>1)에서 R 스텝마다 `h ← W_O h` |
| `freeze_A` | False | True 면 `a` 를 초기 상태에서 1회만 계산 (상태 의존성 제거 대조군) |
| `R` | 스도쿠 1, MNIST/CA 8 | dt = 1/R |

## 2. 스도쿠 판 — R1B8 (`sudoku/lt.py`)

URM 하네스(arXiv 2512.14693)의 학습 루프(세그먼트 detach · 세그먼트별 감독 · AdamATan2 · EMA · 증강)를 그대로 쓰고
모델만 교체한다. 세그먼트(하네스 loop 1회, 16회 반복) 안은:

```
세그먼트 = [ 경계 → 주입 → 스텝 ] × 8 블록,  전 블록 가중치 공유
  경계:  h ← h + W_d [ (W_g h)/2 ⊙ (W_u h) ]        토큰 내 쌍선형 사상 (활성화 0개). W_d 영 초기화 → 시작 시 항등
  주입:  h ← h + β · Embed(x)                        β 학습 스칼라 (init 0.25, 학습값 ~0.24)
  스텝:  h ← Φ_{1/2}( Φ_{1/2}(h) + f(Φ_{1/2}(h)) )   코어 1스텝, dt = 1 (R = 1)
세그먼트 끝: h.detach() → 다음 세그먼트 carry.  로짓 = W_cls h (토큰별 9-way)
```
- `T = 81`, 2D 위치 `(행, 열)`, vocab 11 (0 PAD, 1 빈칸, 2..10 = 1..9). 절대 위치 임베딩은 없고 커널의 상대 위치뿐.
- `d = 832, H = 8`. 파라미터 14.08M (코어 `w_ov`·`w_bo` 1.38M 은 이 경로에서 미사용 — 체크포인트 호환용으로 남김, 실사용 12.70M).
- ACT/halting 제거: 16 세그먼트 결정론. `q_head` 는 손실 함수 호환용 더미.
- 학습: lr 1e-4 상수, wd 1.0 (ψ·θ·α·γ·b·β·1차원 파라미터는 wd 0), 배치 128, bf16 autocast, EMA 0.999.
  1k 퍼즐 × 증강 1000 (숫자 치환·전치·밴드/스택 셔플), 1 iter = 250 에폭 = 1,953 step, 계획 200 iters.

### 2.1 각 부품이 이렇게 생긴 이유

| 부품 | 근거 |
|---|---|
| 세그먼트 detach (URM) | 코어의 야코비안은 스텝당 `e^{λ}` 로 늘고(λ>0 실측), BPTT 창을 세그먼트 1개로 자르는 것이 유일하게 학습이 된 방식. τ 를 늘린 흐름판은 학습 실패 |
| **dt = 1 완보 + 블록마다 경계·주입** | R=8 흐름판(τ=1 흐름 후 경계 1회)은 셀 0.66 에서 평형. 같은 8스텝을 dt=1 로 바꾸고 경계·주입을 스텝마다 넣으면 벽을 넘는다. 경계 주기만 8스텝으로 둔 판은 0.42 로 죽음 — 변수는 dt 가 아니라 경계 주기 (RESULTS §1) |
| 블록마다 재주입 | 끄면 0.565 → 0.104. detach 가 세그먼트 간 입력 보존 학습을 막으므로 입력은 매번 다시 넣어야 한다 |
| **경계 = 토큰 내 채널곱** | 선형 경계(`h + Wh`, 비잔차 `W_O h`, 공유/비공유 전부)는 셀 0.64 벽 · 완답 0. 곱 `g ⊙ u` 가 있으면 넘는다. 곱에 씌우는 함수는 silu 든 `g/2` 든 2차 다항이든 성능 동일 — 학습된 게이트 사전활성이 `|g| < 1` 구간에 머물러 silu 가 그 구간에서 `g/2 + g²/4` 와 수치적으로 같기 때문 (RESULTS §2) |
| sheaf 값 경로 | 1D CA 에서 채널 비혼합 0.07 → sheaf 0.50 (Λfull). 대칭 PSD 수송이라 장을 고정했을 때 자기수반 |
| ε = 1e-4 | `‖z‖ ≈ 10` 에서 상대 섭동 1e-5. 0 근처 `z` 의 특이성 방지 |

### 2.2 흐름판과 이산판

`R > 1` 이면 세그먼트 안이 dt = 1/R 의 흐름 근사가 되고(`R → ∞` 에서 벡터장의 흐름 `φ_1` 로 수렴), MNIST·CA 는
이 판(`R = 8`)으로 학습했다. 스도쿠 판은 `R = 1` — 스텝 간 상태 점프가 O(1) 이라 흐름의 극한이 아니며,
어텐션 그래프 `a(h)` 가 스텝마다 실질적으로 다른 그래프가 된다. 두 판은 같은 코드(`Model1.field/phi`)를 쓰고 `R` 만 다르다.
