# 추론 시점 비지도 학습 — 해결해야 할 것 목록

작성 2026-08-31 · 개정 2026-08-31(4→방침 확정, 5→보류, 6→비용 항목으로 재분류). 대상 = `core/minimal.py` 구조.

근거 표시: `[코드]` 파일에서 직접 확인 · `[유도]` 수식에서 유도 · `[검증]` 수치로 확인.
이 문서는 벤치마크 수치를 근거로 쓰지 않는다. 각 항목의 "해결 판정"도 성능이 아니라 **구조적 조건**으로 적는다.

---

## 목표

추론 시점에, 라벨 없이, 인스턴스마다 결합 기억 `w` 를 쌓아서 그 인스턴스에 대해 모델이 스스로 나아지게 한다.
`w` 는 세그먼트 경계 detach 를 넘어 carry 되고 `fresh` 에서 인스턴스마다 초기화되므로 `[코드]`,
**정적 가중치가 원리적으로 담을 수 없는 인스턴스별 관계**를 담을 수 있는 유일한 통로다.

전제: ARC 목표. 도메인 오라클(스도쿠의 "동료 칸은 달라야 한다" 같은 고정 제약)은 쓸 수 없다.
오라클이 없다 = 뉴런이 처한 상황 = STDP 의 인과 장치가 존재하는 이유. 그 노선이 맞는 노선이다.

### 이미 확보된 것 (다시 만들 필요 없음)

- **짝짓기 커널**: `a_tn = e^{−α‖Δ‖₁} Σ_j r_tj r_nj cos(u_tn,j − ψ_j)`, `u = Δφ − θ_j·Δ` `[검증: 닫힌 형태 오차 3e-16]`
  STDP 창이 요구하는 성질(쌍 위의 유계·부호 있는 함수)을 모두 만족. `|a| ≤ 1` `[유도: 코시-슈바르츠]`
- **계량 시간이 필요 없다는 것**: 짝짓기는 계량 시간을 요구하지만 누적은 **순서**만 요구한다.
  순서는 이미 있다 — 세그먼트 16 × 블록 8 = 스텝 128. `minimal` 에서 블록 = 스텝, 내부 루프 없음 `[코드: 267행이 유일한 루프]`
- **RoPE 와 STDP 는 경쟁 서술이 아니다**: RoPE = 커널(누가 누구와 말을 거는가), STDP = 누적 규칙(무엇이 남는가).
  다른 역할이고, 더 근본적으로 둘 다 **"차이 변수에 대한 커널"** 이라는 같은 수학 가족이다
  (RoPE 는 내적이 상대 위치에만, STDP 는 변화가 상대 시각에만 의존). 차이 변수가 위치냐 시각이냐만 다르다.
- **대칭/반대칭 다이얼**: `a` 의 대칭부 ∝ cos ψ, 반대칭부 ∝ sin ψ `[검증: ψ₀ 스윕에서 1e-15]`
  ψ=0 → `a_tn = a_nt` 정확히 성립 `[검증: 오차 0]`. q/k 구분이 오직 ψ 에서 나오는 설계(단일 내용 사영 `W_C`) 덕.

---

## 해결해야 할 것 (열려 있음)

### 1. 순서 좌표는 존재하지만 Γ 의 정규화가 그것을 누적기에서 배제한다 — 가장 결정적
*(2026-08-31 전면 재작성. 초판 "위상이 순서를 담지 않는다 → 순서를 만들 방법이 없다" 는 틀렸다.)*

**순서 좌표는 이미 있다** `[측정]` — `2026-08-31/analysis/carry_order.py`, R1B8_min_stdp1 228501, 완답 60/128
`‖W h_t‖` (칸이 보내는 메시지 크기) 가 정착도를 추적한다:

| 범주 | seg1 | seg4 | seg8 | seg12 | seg16 |
|---|---|---|---|---|---|
| 단서 | 81.1 | 58.6 | 76.2 | 87.1 | 88.6 |
| 빈칸 → 최종 정답 | 41.5 | 37.5 | 71.0 | 89.6 | **93.2** |
| 빈칸 → 최종 오답 | 39.5 | 33.0 | 32.4 | 32.1 | **31.2** |

풀릴 빈칸은 오답 빈칸과 같은 값에서 출발해 단서 칸을 넘어설 때까지 자란다. 최종 비율 **2.99배**.
블록마다 갱신되고 진행에 대해 단조 → **해결 판정 1·2 를 이미 만족.**

**왜 위상이 아니어도 되는가** `[유도][검증]`
- `a_tn` 은 칸 크기에 정확히 불변 (100배 → 2e-5) → 크기가 주소 배정을 오염시키지 않는다
- 수송 `f_t = Σ_n a_tn (WᵀW) h_n` 은 **정규화 안 된** `h_n` 을 쓴다 → 크기가 메시지 세기를 직접 정한다
- 상대 영향비 = `(‖h_n‖/‖h_t‖)²` → **순서가 부호 비대칭이 아니라 진폭 비대칭으로 구현돼 있다**

**위상 = 배선(누가 누구와), 진폭 = 권위(누구 말이 세냐).** 두 채널로 분리돼 있다.
반대칭 부호가 순서를 담지 않아도 됐던 이유이며, 실제로 담고 있지 않다 `[측정]`
(`sign_symmetry.py`: 반대칭 성분 평균 ≈ 0 — 피어 −0.0000, 비피어·정답값 +0.0003, 대칭 성분은 −0.0488 / +0.0094).

**남은 문제 — 누적기만 그 신호를 못 본다**

    Γ_tn = a_tn · ⟨v̂_t, v̂_n⟩       a_tn: 크기 불변    v̂ = v/‖v‖: 정규화됨
    ⇒ Γ 는 칸 크기에 완전히 불변

순서를 나르는 양(`‖Wh‖`, 3배 차이)이 누적기에 닿기 직전 나눠서 버려진다.

| | 순서 정보 |
|---|---|
| 수송 `f` | ✅ 사용 (정규화 안 된 `h_n`) — 그래서 모델이 절반을 푼다 |
| 누적 `w` | ❌ 버림 (정규화된 `v̂`) — **권위로 가중된 기억을 만들 수 없다** |

목표가 누적으로 인스턴스 구조를 배우는 것인데 누적기만 눈이 멀어 있다.
`w` 는 "n 이 권위 있을 때 이 짝이 성립했다" 를 기록하지 못하고 방향만 기록한다.

**해결 판정**
`Γ` 가 칸 크기(또는 그에 준하는 정착도)에 의존할 것. `v̂` 정규화 하나에 걸린 문제다.

**참고** `[측정]` γ 가 init 0.1 → **0.0001** (1000배 감소). 흡수구 반지름 3.16 → 102.7,
실제 `‖h‖` 는 80~93 으로 그 80~90% 지점. 모델이 진폭 여유를 적극 확보했고, 진폭이 정보를 나른다는 위 그림과 맞물린다.

### 2. 양의 대칭 결합이 증거 채널이자 확증 채널이다 — 잔여
*(2026-08-31 축소. 초판 "정적 ψ 배합은 정착과 확증 차단 중 하나를 항상 지불한다" 는 **전제가 틀렸다**.)*

**초판이 틀린 이유** — 양의 대칭만 가정했다. 확증은 `a_tn = a_nt > 0` 일 때 일어난다.
**음의 대칭 결합**(`a_tn = a_nt < 0`)은 상호 강화가 아니라 **상호 배척**이라 확증이 아니고,
소산 흐름이라 **정착도 한다.** 반대칭만이 확증 방지 장치라는 것은 틀렸다.

| 방식 | 확증 차단 | 정착 |
|---|---|---|
| 반대칭 결합 | O 한쪽이 양보 | X 회전 흐름, 골짜기 없음 |
| **음의 대칭 결합** | O 상호 배척 | O **소산 흐름, 골짜기 있음** |

그리고 학습된 모델이 실제로 그 길을 갔다 `[측정]` — 피어 `w_tn = −0.0489`, `w_nt = −0.0488`,
반대칭 성분 평균 **−0.0000**. 피어 제약은 본질적으로 대칭 관계이므로 대칭 인코딩이 옳다.
따라서 배합비 트레이드오프는 구속하지 않는다.

**남는 잔여**
비피어·정답값 쌍은 `+0.0094` 로 **양의 대칭**이다 `[측정]`. 이게 증거 채널인데,
같은 형태가 곧 상호 확증의 형태이기도 하다. 두 칸이 **틀린 값에 함께 합의**하면 서로 강화한다.

**미확인** — 오답 칸들이 실제로 서로 확증하는지는 측정하지 않았다.
확인법: 최종 오답 빈칸 중 **같은 (틀린) 값을 든 비피어 쌍**의 `w` 를 안 든 쌍과 비교. 양이고 크면 실재.

**해결 판정**
증거와 확증을 가르는 것이 정의될 것. 1번(권위 가중)이 그 후보다 —
권위 있는 칸이 보낸 합의만 강화하면 두 칸이 함께 약할 때의 상호 확증은 안 쌓인다.

## 해소된 것

### (구 2번) 기하 위상과 순서 위상이 한 스칼라에 섞인다 — **구조적 장애가 아님** (2026-08-31)

초판에서 `u = Δφ − θ_j·Δ_tn` 의 뒷항이 쌍마다 고정 상수라 순서 신호와 분리 불가능하고,
STDP 에서 필터가 실패하는 "지연 오프셋 Δ₀" 와 같은 역할이라고 적었다. **틀렸다.**

**기하항은 분리 가능(separable)하다** `[유도][검증]`

    θ_j·Δ_tn = (θ_j·pos_t) − (θ_j·pos_n)      ← 쌍의 양이 아니라 칸별 양의 차

`φ` 도 칸별 양이므로 `φ_tj = −θ_j·pos_t + χ_tj` 로 두면

    u_tn,j = (φ_nj − φ_tj) − θ_j·Δ_tn = Δχ_j       ← 기하항 정확히 소거

수치 확인: 흡수 시 커널이 기하와 무관해짐 (max|차| **2.2e-16**), 흡수 없으면 1.04.
그리고 흡수는 **위치를 버리지 않는다** — `χ` 가 위치를 담으면 커널은 다시 위치에 반응한다(차이 0.89).
고정 사전분포가 학습 가능한 상태 의존 형태로 바뀔 뿐이며, 표현력은 순증한다.

**부트스트랩**: 상태에는 위치가 없지만(위치 임베딩 없음, `init_hidden` 은 전 칸 동일) 블록 0 의 커널이 넣어준다.
블록 1 부터 `W_C` 가 `−θ_j·pos_t` 를 뽑아낼 재료가 있다.

**생물과의 차이**: STDP 의 Δ₀ 는 물리적 전달 지연이라 뉴런이 뺄 수 없다. 우리 것은 뺄 수 있다.
따라서 "STDP 가 실패하는 경우가 구조적으로 항상 켜져 있다" 는 초판 서술을 철회한다.

**남는 것**: 흡수는 경쟁자를 없앨 뿐 순서 신호를 만들지 않는다 → **1번에 흡수됨.**

## 방침이 정해진 것

### 4. 누적기의 이득이 1이다 → `(1−δ)w + κΓ` 로 교체

**현상** `[유도]` `w ← w + η(Γ−w) = (1−η)w + ηΓ` 의 고정점은 `⟨Γ⟩`. 이득 정확히 1인 저역통과 필터다.
결합이 순간 증거보다 강해질 수 없다 — **역사는 주지만 강도는 못 준다.**

**방침** `w ← (1−δ)w + κΓ`, 고정점 `(κ/δ)⟨Γ⟩`, `κ/δ > 1`.
부수 효과로 `|a| ≤ 1` 정리가 소멸한다(w 가 κ/δ 까지 감). `Φ` 의 흡수구가 상태를 계속 묶으므로 발산은 없고,
보장의 성격만 바뀐다. 순전히 기계적인 변경.

---

## 보류

### 5. 학습 지평 8, 사용 지평 128

**현상** `[코드][검증]` `h`·`w` 둘 다 세그먼트 경계 detach → 그래디언트 창 8스텝, 추론 지평 128스텝.
누적 파라미터(η·λ·κ·δ)는 8스텝만 보고 학습된다.
δ=0.008(시상수 125스텝)이면 8스텝 안에 효과의 **6.2%** 만 나타나고, 그래디언트가 잘 보는 δ=0.5 는
시상수 2스텝이라 128스텝에 걸쳐 아무것도 적분하지 않는다.

**상태** 문제 삼지 않기로 함 (판단: 사용자, 2026-08-31). **해결된 것이 아니라 보류다** — 4번 도입 후
느린 시상수가 필요해지면 다시 올라올 수 있으므로 지워두지 않는다.

---

## 비용 제약 (해결 대상이 아니라 설계에서 지불할 것)

### 6. ARC 규모에서 `w` 의 메모리

**연산은 문제가 아니다** `[유도]`
`w` 갱신은 이미 계산한 `[B,H,T,T]` 텐서에 대한 원소별 연산이다(`G=a*agree`, `w←…`, `a_eff=…`).
무거운 einsum(`a` 생성, `o=einsum(a,v)`)의 개수는 그대로다. FLOPs 증가는 무시할 수준.

**양방향이라 괜찮은 것이 아니다** `[유도]`
- `w` 가 대칭이면 절반만 저장 가능하지만, `a_tn=a_nt` 는 ψ=0 일 때뿐이다. ψ≠0 이 3번(확증 방지)의 근거이므로
  `w` 는 본질적으로 비대칭이어야 한다. 게다가 대칭이어도 2배 절약일 뿐.
- 어텐션이 양방향(비인과)인 것은 완화 요인이 아니라 **비용의 출처**다. 인과 마스크가 있으면 `T²/2` 로 준다.

**메모리 — 추론과 학습이 다르다** `[코드][계산]`
`[B,H,T,T]` 텐서 1개 = 샘플당 0.10 MB (T=81) → **12.36 MB** (T=900).

- **추론(no_grad)**: `w` 는 상주 버퍼 1개. B=128, T=900 에서 **1.55 GB**. 상수배, 감당됨.
- **학습**: autograd 가 블록마다 ~6개(`a` 전/후, `agree`, `G`, `w`, `a_eff`)를 붙잡고 세그먼트는 8블록 → ~48개.

| | T=81 (스도쿠) | T=900 (ARC 30×30) |
|---|---|---|
| stdp 없음 (~16개) | 1.60 MB/샘플 | 197.8 MB/샘플 |
| stdp product (~48개) | 4.81 MB/샘플 | **593.3 MB/샘플** |

B=128 · T=900 · stdp → **74 GB**. B=32 → 18.5 GB. B=8 → 4.6 GB.

**핵심**: T=900 은 stdp 없이도 이미 메모리 한계다(B=128 에서 24.7 GB). stdp 가 없던 문제를 만드는 것이 아니라
**이미 빡빡한 것을 3배로** 만든다. 완화 수단이 존재하므로 막다른 길이 아니다 —
배치 축소 / 그래디언트 체크포인팅(구 `lt.py` 의 `ckpt` 플래그, `minimal` 이 제거) / `w` 저랭크화.

## 해결하지 않아도 되는 것

- **계량 시간(Δt, τ)의 도입** — 짝짓기 커널이 요구하는 성질을 위상차가 이미 만족한다.
  누적에 필요한 것은 순서뿐이고 순서는 이미 있다. 시간 축을 다시 만들 이유가 없다.
- **스파이크·발화율 유비 전반** — 짝짓기와 누적의 역할만 가져오면 되고, 그 둘이 같은 지수함수여야 한다는 정리는 없다.
  생물에서 합쳐진 것은 삭는 자취 하나로 둘을 동시에 얻는 게 싸기 때문이다.
- `stdp_target="value"` — 코드에 이미 거부됨으로 표시 `[코드]`.

## 새로 생긴 것

### 7. 측정이 전부 스도쿠다 — ARC 일반성 미검증

1·2 번의 근거(`‖W h_t‖` 가 정착도를 추적, 피어 음·증거 양의 부호 구조)는 모두 스도쿠에서 나왔다 `[측정]`.
스도쿠는 칸마다 답이 하나로 수렴하고 제약이 고정된 특수 구조라 "크기가 자란다" 가 자연스럽다.
ARC 는 과제마다 관계가 다르고 출력 크기도 다르다. **진폭이 정착도를 나른다는 그림이 ARC 에서도 서는지 모른다.**

**확인 경로** — 2D CA (ARC 형식, `2026-08-26/code/build_ca2d_dataset.py`) 로 같은 측정 반복.
빌더가 이미 있고 하네스는 `data_path` 와 `grid` 만 바꾸면 된다. 다만 학습을 새로 돌려야 한다.

---

## 결정이 필요한 것 (아직 정하지 않음)

- **ARC 데모 쌍을 추론 시점에 쓸 것인가.** 데모는 ARC 에서 라벨 없이 쓸 수 있는 유일한 지도 신호지만,
  현재 구조에서 그 통로는 `puzzle_emb` 의 **그래디언트 기반** 경로다. 이 누적 기계와는 다른 기제이므로
  둘을 함께 갈 것인지, 누적만으로 갈 것인지 결정해야 한다.
- **`w` 를 어느 층위에 둘 것인가** — 6번과 얽혀 있다. 쌍 행렬 그대로인지, 요약인지.
