# 이론 — 증명된 성질

여기에는 모델 정의(`ARCHITECTURE.md` §1)로부터 **대수적으로 따라오는 것만** 적는다. 실험 관찰에 대한 해석은 싣지 않는다.
관찰된 수치가 필요한 곳은 `RESULTS.md` 의 해당 절을 가리킨다.

기호: `a_tn^(m)` 어텐션 계수, `D_j(Δ) = e^{−α_j‖Δ‖₁} ∈ (0,1]`, `ẑ = z/(‖z‖+ε)`.

## 1. 유계성

**명제 1.** `|a_tn| ≤ 1`.
`|D_j| ≤ 1`, `|e^{−iΘ}| = 1` 이므로 `|a_tn| ≤ Σ_j |ẑ_{t,j}||ẑ_{n,j}| ≤ ‖ẑ_t‖‖ẑ_n‖ ≤ 1` (Cauchy–Schwarz; `‖ẑ‖ = ‖z‖/(‖z‖+ε) ≤ 1`). ∎

**명제 2 (흡수구).** `M(τ) = max_t ‖h_t(τ)‖`, `κ₀ = max_t Σ_n e^{−α_min‖Δ_tn‖₁}`, `κ = max_m ‖W^(m)ᵀW^(m)‖₂ κ₀` 라 하면
최댓값 토큰에서 Dini 미분으로
```
Ṁ ≤ (λ_max(Λ_sym) + κ) M + κ‖b‖ − γ M³
```
따라서 `M(τ) ≤ max(M(0), R*)`, `R*` 는 우변 = 0 의 양의 근. 조건 없이 성립한다 — 4차 소산이 항상 이긴다. ∎
(이 상한은 `|a_tn| ≤ 1` 을 모든 간선에 동시 적용한 최악치라 실제 반경보다 수 배 헐겁다.)

## 2. 동차성

**명제 3.** `ε = 0` 일 때 `a(c·H) = a(H)` (`c > 0`). `ẑ(ch) = ẑ(h)` 에서 즉시. ∎

**따름.** 벡터장의 항별 동차 차수:

| 항 | 차수 |
|---|---|
| `Λh` | 1 |
| `Σ_m Wᵀ Σ_n a_tn W h_n` | **1** (`a` 가 0차이므로) |
| `(Σ d_t) b` | **0** |
| `−γ‖h‖²h` | 3 |

어텐션 항은 "3차 다항식"이 아니라 **1차 동차의 방향 의존 게이팅**이다. `b` 와 4차항을 빼면 벡터장은 양의 동차 1차 —
편향 없는 ReLU 망과 같은 원뿔 구조이고, `b` 가 동차성을 깨는 유일한 항이다.

**명제 4 (함수 클래스).** `a_tn = P₂(h_t,h_n) / [(√Q(h_t)+ε)(√Q(h_n)+ε)]`, `P₂` 쌍선형, `Q(h) = ‖W_C h‖²` 양정부호 2차형식.
따라서 `a` 는 유리함수가 아니라 `√Q` 를 첨가한 **대수함수**다. `Φ` 는 모든 곳에서 실해석적(근호 인자 ≥ 1). `‖z‖` 는 `z = 0`
에서 `C¹` 이되 `C²` 가 아니며, 그 예외집합 `{h : W_C^(m) h = 0}` 은 여차원 `d_h` — 측도 0. ∎

## 3. γ 는 스케일 게이지

**명제 5.** `b = 0, ε = 0` 이면 `h = u/√γ` 치환으로 벡터장에서 γ 가 소거된다:
```
u̇ = Λu + Σ_m a(u) WᵀW u − ‖u‖²u
```
(`a(u/√γ) = a(u)` 는 명제 3.) ∎

**따름.** (i) 끌개 반경 `∝ γ^{−1/2}`. (ii) 리아푸노프 스펙트럼은 γ 에 불변 (선형 변수변환). (iii) γ 는 동역학의
손잡이가 아니라 `h` 의 **단위** 다. 게이지를 깨는 항은 `b`(0차라 `√γ(Σd)b` 가 남음)와 `ε` 뿐.
(iv) 최적화는 게이지 불변이 아니다 — AdamW 의 스케일 적응·wd·초기화 스케일 때문에 γ 를 바꿔 학습하면 다른 해에 도달한다.

**스도쿠 판에의 함의.** 경계 `W_d[(W_g h)/2 ⊙ W_u h]` 는 `h` 에 2차이므로 게이지 아래 `√γ` 배로 스케일된다.
게이트 사전활성 `g = W_g h` 의 크기는 γ 가 정하는 진폭 반경에 종속되며, 이것이 silu 가 다항식 구간에 머무는 이유의 수학적 근거다 (§5, RESULTS §2).

## 4. 조건부 선형성

**명제 6.** 스칼라 장 `{a^(m)_tn, d_t^(m), ‖h_t‖}` 를 궤적에서 기록해 고정하면, 코어 한 스텝은 `h` 에 대해 **정확히 아핀**이다:
`Φ` 는 `‖h‖` 가 고정되면 스칼라 곱, 어텐션 항은 `a` 가 고정되면 선형, `(Σd)b` 는 상수. ∎

**따름.** (i) 세그먼트 전체는 아핀 사상의 합성 → `logits = W · L_장[h₀] + c_장` 으로 정확히 분해된다.
(ii) 기록된 장으로 재생하면 오차가 0 이다(부동소수점 한계). (iii) 특정 간선·스텝의 `a_tn` 을 기록에서 지우고 재생하면
**무근사 반사실**이 된다 — softmax+MLP 망에서는 MLP 가 `h` 에 비선형이라 어떤 동결로도 성립하지 않는 성질이다.

스도쿠 판에서는 경계의 게이트 벡터 `g_t = W_g h_t` 를 장에 추가해야 같은 성질이 복원된다 (`(g/2) ⊙ (W_u h)` 는 `g` 고정 시 선형).

**명제 7 (대칭성).** `ψ = 0 ⟺ a_tn = a_nt` (모든 `H` 에 대해). `X_tn = Δφ_tn − θ·Δ_tn` 로 두면 `a_tn ∝ cos(X_tn − ψ)`,
`a_nt ∝ cos(−X_tn − ψ)`, 차이는 `sin X_tn sin ψ`. ∎
`A` 가 대칭이어도 벡터장은 gradient field 가 아니다: `f = A(h)h` 의 야코비안 `Df = A + (∂A/∂h)h` 의 둘째 항이 비대칭.
gradient flow 는 `A` 를 상수로 볼 때만 성립한다.

## 5. 진폭 정착

**명제 8.** `Φ_τ(h) = h/√(1+2γ‖h‖²τ)` 는 `‖h‖ ≫ (2γτ)^{−1/2}` 에서 `‖Φ_τ(h)‖ → (2γτ)^{−1/2}` 로 포화한다.
따라서 dt = 1 판에서 반스텝(`τ = ½`)마다 반경 `(γ)^{−1/2}` 가 상한처럼 작용한다. ∎

실측(RESULTS §2): 123k 체크포인트에서 `γ = 0.0102`, 상한 9.9, 정착 `‖h_t‖ ≈ 6–7.5`. 그 진폭에서 게이트 사전활성은
`std ≈ 0.2–0.3`, `|g| > 2` 비율 < 0.1%. 이 구간에서 `silu(g) − (g/2 + g²/4) = O(g⁴/48) ≈ 1e-4` — 두 게이트는 같은 함수다.

## 6. 적분기의 차수

구현된 분할 `Φ_{dt/2} ∘ (I + dt·f) ∘ Φ_{dt/2}` 는 중간 스텝이 전진 오일러(비대칭)라 **1차**다 (Strang 2차가 아님).
4차항을 해석적으로 푸는 이득은 정확도가 아니라 가짜 폭발 차단이다. 스도쿠 판(dt = 1)은 애초에 흐름의 근사가 아니므로 차수는 무의미하다.
