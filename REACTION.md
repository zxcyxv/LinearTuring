# 반응항 설계 메모 — 쌍선형을 퍼텐셜의 경사로 (2026-08-28, 미실행)

> 상태: 설계만. 학습 결과 없음. 오늘(8/28)의 해체·측정에서 도출.
> 한 줄: 확산항을 라플라시안 꼴로, 결합을 대칭으로, 반응항을 숫자 우물 퍼텐셜의 경사로 두면
> 블록이 `a` 고정 하에 한 퍼텐셜 `U` 의 정확한 경사 스텝이 되고, 반응확산→튜링→CA→스도쿠 사슬이 유비가 아니라 한 식이 된다.


## 기호표 (이 문서에서 쓰는 것 전부)

| 기호 | 뜻 | 모양 |
|---|---|---|
| `h_t` | 칸 t 의 상태 | 832 |
| `x_t` | 칸 t 의 입력 토큰 (주어진 숫자 또는 '빈칸') | 정수 |
| `E(x_t)` | **입력 임베딩** — 토큰을 상태 공간에 더하는 벡터 (`embed.weight[x_t]`) | 832 |
| `r_v` | **판독 방향** — 숫자 v 의 로짓을 내는 벡터 (`w_cls.weight[v]`, `logit_v = r_v·h_t`). 아래 퍼텐셜의 우물 방향. (초안의 `e_v` 를 이 기호로 바꿈 — 임베딩과 혼동 방지) | 832, v=1..9 |
| `a_tn` | 어텐션 가중치 = 간선 (t,n) 의 스칼라, 부호 있음 | 스칼라 |
| `W` | 값 경로 사영 (`w_sh`), 되돌림은 `Wᵀ` | 104×832 (헤드별) |
| `W_C` | 주소 사영, 행직교 | 104×832 (헤드별) |
| `ψ, θ, α` | 위상차, 2D 파수, 감쇠 (Gabor 커널) | 채널별 |
| `γ` | 4차 소산 계수. `Φ(h)=h/√(1+γ‖h‖²)` | 스칼라 |
| `β` | 반응 퍼텐셜의 우물 깊이 (제안의 손잡이) | 스칼라 |
| `β_inj` | 주입 게이트 (`inj_gate`) | 스칼라 |
| `W_g, W_u, W_d` | 쌍선형 경계의 세 행렬 `h + W_d[(W_g h)/2 ⊙ (W_u h)]` | 2304×832, 2304×832, 832×2304 |
| `E_a(h)` | 디리클레 에너지 `½ Σ_{t,n} a_tn ‖W h_t − W h_n‖²` | 스칼라 |
| `V(h_t)` | 칸 하나의 반응 퍼텐셜 | 스칼라 |
| `U(h)` | 블록 전체 퍼텐셜 `E_a + Σ_t V(h_t) − β_inj Σ_t ⟨E(x_t), h_t⟩` | 스칼라 |
| `d_t` | `Σ_n a_tn`, 부호 행합 | 스칼라 |
| `Δ` | 위치 차 `pos_t − pos_n` | 2 |

## 0. 오늘 확정된 사실 (이 메모의 전제)

- 모델은 제약 전파 CA 다. 출처별 정확 분해(조건부 선형성)로 개별 메시지 수준에서 확인:
  피어 → "내 숫자는 아니다" (자기 숫자 자리 −6.8), 비피어 → "내 숫자일 수 있다" (+1.07). naked/hidden single·naked pair 가 한 규칙의 특수 경우.
- 오답 = 규칙 오류가 아니라 **틀린 커밋이 규칙을 거쳐 서로를 정당화하는 순환**. 상태 이식은 안 남고(17%), 입력 핀만 남는다(100%).
- 제약이 정하지 않은 곳에서도 커밋한다 (해 다수 입력에서 유효 격자 99% 중 하나로 붕괴, 엔트로피 0.002).
- 현재 벡터장은 어떤 에너지의 경사도 아니다: (i) `Σ a h_n` 은 라플라시안이 아니라 인접행렬 (차수항 `d_t WᵀW h_t` 잔존),
  (ii) `a` 비대칭 (0.3~1.3), (iii) Λ·`d_t·b` 는 근거 없는 잔존 항, (iv) 자유 쌍선형은 야코비안 비대칭.
- 원 사양의 "3차 어텐션 동역학"은 `ẑ` 정규화(명제 2)로 Model 1 에서 소멸. 살아남은 원리: 활성화 0개, 부호 어텐션 + 위상 정규화, 4차 소산 흡수구.
  비선형성의 자리는 어텐션 자기항이 아니라 **경계의 2차 곱 `g⊙u`** 로 옮겨졌다.

## 1. 최소 구조 (`core/minimal.py`, 7.15M — 작성·forward 검증됨, 미학습)

```
h ← h + W_d[(W_g h)/2 ⊙ (W_u h)]                    쌍선형 경계 (자유)
h ← h + β_inj·E(x)                                   재주입
a_tn = e^{−α‖Δ‖}·Re[conj(q̂_t) k̂_n]                   위상 정규화 + Gabor 회전 (W_C 는 QR 행직교, ψ·θ·α 유지)
h ← Φ(h + Σ_n a_tn WᵀW h_n),  Φ(h)=h/√(1+γ‖h‖²)      (W,Wᵀ) 값 경로 + 4차 소산
```
버린 것: `wc_skew`(5.5M, exp(skew) → QR), `w_ov`·`w_bo`(1.38M 유령), Λ full, `d_t·b`, R/dt/rand_R/Strang 반스텝, q_head.
남긴 형식: `puzzle_emb`(하네스 요구, 내용상 전역 편향 1개), 상수 q 로짓.

## 2. 이 메모의 제안 — 세 가지 수정

### 2.1 확산항을 라플라시안 꼴로
```
Σ_n a_tn WᵀW h_n   →   Σ_n a_tn WᵀW (h_n − h_t)
```
항등식 `Σ a h_n = Σ a (h_n − h_t) + d_t WᵀW h_t` 의 차수항을 제거. 이러면 이 항이 정확히
`−∇_{h_t} E_a`,  `E_a(h) = ½ Σ_{t,n} a_tn ‖W h_t − W h_n‖²`.  라플라시안 적용 = 디리클레 에너지의 경사 한 스텝.

### 2.2 결합을 대칭으로
`∂E_a/∂h_t = Σ_n (a_tn + a_nt) WᵀW (h_t − h_n)` 이므로 2.1 이 정확한 경사이려면 `a_tn = a_nt`.
명제 7: `a` 대칭 ⟺ `ψ = 0`. 따라서 `ψ ≡ 0` 고정 (또는 수송에 `(a+aᵀ)/2`). 비용 = 방향 있는 간선 포기.
부수 효과: 대칭 결합 + 동기 갱신은 카오스가 아니라 고정점/2-주기로 감(Goles; 단 `a=a(h)` 라 정확히는 조건부).
이음매의 비주기 흔들림(오늘 실측 98% 비주기)이 사라지는 방향.

### 2.3 반응항 = 숫자 우물 퍼텐셜의 경사 (묶인 쌍선형)
요구 조건: ① 후보의 곱(AND) — 선형 경계 3형식 전멸이 근거 ② 제약이 정하지 않으면 고르지 않음 ③ 국소 퍼텐셜의 경사 ④ 다항식.
```
V(h)  = −(β/3) Σ_{v} (r_v · h)³  +  (γ/4) ‖h‖⁴          r_v: 우물 방향 = 숫자 v 의 판독 벡터 `w_cls.weight[v]` (또는 학습되는 M개)
−∇V   =  β Σ_v (r_v · h)² r_v  −  γ ‖h‖² h
```
경사가 `h` 에 2차 = 쌍선형과 같은 차수·같은 꼴. 현재 `W_d[(W_g h)⊙(W_u h)]` 에서 `W_g = W_u = W_d = [r_1; …; r_9]` 로 **묶은** 것.
3차항이 각 판독 방향 `r_v` 에 우물(다중안정), 4차항이 가둠(Φ 가 이미 하는 일).

### 2.4 결과: 블록 = 하나의 퍼텐셜의 경사 스텝
```
U(h) = ½ Σ_{t,n} a_tn ‖W h_t − W h_n‖²  +  Σ_t V(h_t)  −  β_inj Σ_t ⟨E(x_t), h_t⟩
블록:  h ← Φ( h − ∇U )     (a 고정, Φ = 4차항의 정확한 흐름)
```
`a` 고정 하에 `U` 는 리아푸노프 함수 (4차항이 이겨 아래로 유계). `a = a(h)` 재계산이 유일한 비경사 요소 = 결정 기제.

## 3. 사슬이 한 식이 되는 방식

| 고리 | 식에서의 자리 |
|---|---|
| 반응확산 | `−∇E_a` (부호 확산) + `−∇V` (국소 반응). 정리. |
| 튜링 | 균일 상태 `h̄`(init_hidden) 에서 확산항 = 0. 안정성은 `I + J_V + λ_k WᵀW` 의 부호: 균일 모드(`λ_0=0`) 안정 / 음수 간선이 만드는 `λ_k>0` 모드 불안정 → 분화. 2일차 분산관계 실측(균일 −0.047 / 띠 +2.78)이 이 조건. |
| 셀룰러 오토마타 | dt=1 동기 갱신 = 경사계의 이산판. 고정점 = `U` 의 국소 최소. |
| 전파 / 결정 분리 | **`β` 하나로**: `β=0` → 우물 없음 → 순수 부호 확산 → 후보가 분수로 남음(전파). `β>0` → 우물로 낙하(결정). 스케줄 `β(k)` 가 "전파를 끝내고 결정". |
| 해석 | 오답 순환 = `U` 의 다른 국소 최소, 이음매 = 안장점, 확신 = 우물 깊이. 전부 `U` 의 값. |
| 교정 | 재시작·담금질(오늘 +108·+82)이 원리가 됨: 국소 최소 탈출. `β` 를 내렸다 올리는 것이 정당한 재가열, 헤시안의 음의 고유벡터가 흔들 칸을 지정. |

## 4. 비용·위험 (정직하게)

- `ψ = 0`: 방향 간선 포기. 스도쿠에 필요했는지 데이터 없음 (MNIST psi0 무해).
- 묶인 쌍선형: 자유 쌍선형(은닉 2304, 돌파의 변수)보다 표현력 감소. 완화: 우물 방향을 판독 벡터 9개가 아니라 학습되는 `M` 개로, `W_g=W_u=W_d` 만 묶고 폭 유지.
- 전부 가설. 학습 결과 0. 첫 판정 = 5지점 곡선(기준선 .509/.599/.661·48/.677·53/.690·119) + 밴드 입력에서 β=0 일 때 분수 잔류 여부.

## 5. 판정 (사용자, 2026-08-28)

"그냥 쌍선형이면 충분하다" → **X** (목표가 완전 해석·교정이면). 성능만이면 O.
이유: 자유 쌍선형은 어떤 퍼텐셜의 경사도 아니라 블록에 에너지가 없고, 에너지 없이는 위 표의 어느 행도 정리가 아니다.

## 6. 오늘 폐기한 것 (재발 방지)

- 유닛 hyperedge 층 (`U` 27×81 하드코딩): 스도쿠 전용, sheaf 가 아님. 파일럿 결과 초반 가속(완답 2.2×@9.8k) 후 17k 에 기준선과 수렴 — 증강 대신 등변성으로 산 것, 천장 불변.
- gain 우회로 (`stalk_transport`), 연결 라플라시안 수송 (`conn_sheaf`, 어텐션 ψ·θ 공유): 후자는 scratch 에서 기준선 대비 2 eval 지연·완답 53 정체.
- 세 번 다 "이름을 구조로 착각해 구현" — 이 메모의 제안도 같은 위험이 있으며, 판별 실험 전에는 가설이다.
